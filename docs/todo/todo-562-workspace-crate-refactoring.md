---
id: TODO-562
title: Refactor single-crate monolith into Cargo workspace sub-crates
severity: HIGH
phase: S
priority: P1
status: OPEN
created: 2026-07-24
depends_on: [TODO-561]
---

# TODO-562: Refactor Single-Crate Monolith into Cargo Workspace Sub-Crates

## Current execution gate (2026-09-23)

The six-crate design and monolith description below are the original 2026-07
proposal, retained as history. Current `Cargo.toml` already declares the root
package and **36** leaf workspace members, including `qf-hpke`; production
owners have been extracted behind root compatibility projections. Do not
create the proposed `qf-platform`/`qf-core` families or repeat the old module
moves merely to satisfy this historical checklist. The last retained seam
evidence below reported no product-module SCC in August; produce a fresh seam
graph at the implementation revision before treating that as current proof.

The remaining local work can start now; this task is OPEN for acceptance
reconciliation. Enumerate every current member, dependency edge, target, and
forwarded feature; prove each leaf's isolated `cargo check -p` boundary; run
the exact workspace test,
strict Clippy, formatting, release binary/CLI, incremental-rebuild,
documentation, and protected-path gates against the current revision. Record
which Linux, Windows, x86_64, macOS ARM64, privileged TUN, and hosted CI
cells actually ran, with run/commit/artifact IDs and outcomes. A failed or
unavailable native/CI cell blocks closure; a historical green run is
not proof for today's source. Create a separate defect task only for a
current failed cell. TODO-1089 owns the stale AGENTS.md leaf-count wording.
This gate supersedes the original six-crate acceptance numbers and proposed
module map, without discarding their historical evidence.

## Why

The entire Rust core (~136k LoC, 146 files) lives in one crate. This causes:

- Full recompilation of 136k lines on any single-file change (dev iteration >60s cold).
- No isolation of unsafe/SIMD crypto machine room from safe transport/stealth logic.
- Feature flags are global (enabling `io_uring` recompiles stealth, FEC, brain, etc.).
- External consumers, including future SDKs, must depend on the entire monolith.
- Test parallelism is limited to one compilation unit.
- Cognitive load: no enforced module boundary; any module can reach into any other.

Splitting into a Cargo workspace with focused sub-crates gives incremental compilation,
enforced API boundaries, independent feature gating, and faster CI parallelism.

## Acceptance

- The repository root becomes a Cargo workspace with at least 6 member crates plus the root binary crate.
- Each sub-crate compiles independently with `cargo check -p <crate>`.
- The root binary `quicfuscate` re-exports the same public CLI behavior with zero behavioral regression.
- All existing tests (`cargo test --workspace --all-targets --features rust-tests`) pass unchanged.
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` passes.
- A warmed single-leaf incremental rebuild is measured on the named Apple Silicon host with toolchain, cache state, command, median and spread recorded; compare it with the root rebuild on the same host. The original 15-second target is historical and must not override measured feasibility or correctness.
- Current feature forwarding is verified against actual leaf owners and `Cargo.toml`; the proposed `qf-platform` and bench-crate split is not a required target.
- Existing service and packaging consumers depend only on the focused crates they actually use; do not create additional crates merely to satisfy the original proposal.
- The protected Svelte/Tauri reference paths remain byte-identical; this structural refactor must not edit their manifests, sources, assets, generated bundles, or lockfiles.
- No `pub` item that was previously crate-internal becomes unnecessarily public; inter-crate APIs are minimal and documented.
- CI workflows updated to leverage workspace parallelism (per-crate check/test where beneficial).
- Documentation (DOCUMENTATION.md, MAP.md) updated to reflect the new crate structure.

## Completion Gates

- Structure gate: workspace layout is committed with clear crate boundaries and a root `Cargo.toml` workspace manifest.
- Compilation gate: `cargo check --workspace` and per-crate `cargo check -p` pass; incremental touch-rebuild measured and recorded.
- Test gate: full workspace test suite passes with `rust-tests` feature; no test removed or weakened.
- Clippy gate: workspace-wide Clippy with `-D warnings` passes on stable.
- Feature gate: feature flags are per-crate; enabling one feature does not force recompilation of unrelated crates.
- Binary gate: `cargo build --release --bin quicfuscate` produces a functionally identical binary (CLI help, server start, client connect all work).
- Protection gate: pre- and post-task SHA-256 manifests prove every protected Svelte/Tauri reference path remains byte-identical.
- CI gate: GitHub CI and Clippy Matrix pass on the workspace structure.
- Documentation gate: MAP.md, DOCUMENTATION.md, and AGENTS.md reflect the new crate layout.

## Sub-Tasks

The following checklist is the original migration plan and is historical;
the current executable gate is above. Its unchecked boxes do not authorize
repeating an already extracted owner.

- [x] Audit current module dependency graph; identify natural seam lines and circular dependencies.
- [x] Extract the measured cycle-free REALITY boundary as an independently buildable workspace leaf.
- Historical proposal: design crate split (`qf-crypto`, `qf-transport`, `qf-fec`, `qf-stealth`, `qf-core`, `qf-platform`, root `quicfuscate` bin).
- Historical proposal: break cycles by extracting shared traits/types into `qf-types` or `qf-common` if needed.
- Historical proposal: move modules into sub-crate directories and update module/import paths.
- Historical proposal: scope `io_uring`, `benches`, and SIMD features to the proposed families.
- Historical proposal: update root workspace, service and packaging dependencies, CI, full test/Clippy/release/incremental gates, and documentation.
- [ ] Inventory the current 36-leaf workspace, isolated package builds, dependency/SCC graph, feature forwarding, targets and protected paths at one revision; classify any actual current defect separately.
- [ ] Run the current workspace, release CLI, measured incremental-rebuild, documentation and available native/hosted gates; record pass/fail/unavailable per exact cell without repeating the historical six-crate migration.

## Notes

- Proposed crate boundaries (subject to dependency audit):
  - `qf-crypto`: `src/crypto/`, `src/simd.rs`, `src/simd/` (AEAD ciphers, SIMD dispatch, GF tables used by crypto).
  - `qf-transport`: `src/transport/` (QUIC connection, packets, frames, CC, recovery, H3, version, NAT, path).
  - `qf-fec`: `src/fec/` (adaptive FEC, wire framing, fountain codes, GF arithmetic, interleaving).
  - `qf-stealth`: `src/stealth/`, `src/brain.rs`, `src/qftls.rs`, `src/reality.rs` (stealth stack, brain, TLS cover, reality proxy).
  - `qf-core`: `src/core.rs`, `src/engine/`, `src/compress.rs` (orchestration, engine lifecycle, compression).
  - `qf-platform`: `src/optimize/`, `src/interface/`, `src/firewall/`, `src/privilege/`, `src/dns/`, `src/pki/`, `src/audit/` (OS services, TUN, firewall, privilege, DNS, PKI, audit).
  - `quicfuscate` (root bin): `src/main.rs`, `src/lib.rs`, `src/harness.rs`, `src/implementations/` (CLI entry, server/client runtime wiring).
- Circular dependency risk: `core.rs` orchestrates transport+fec+stealth+crypto. Solution: define trait interfaces in a shared types crate or use dependency inversion.
- The `optimize/` module has cross-cutting helpers (sort, string, iter, random). These may need a small `qf-util` crate or stay in `qf-platform`.
- Existing `[[test]]` and `[[bench]]` targets in root Cargo.toml must be redistributed to owning crates.
- This is a pure structural refactoring: zero behavioral change, zero feature addition, zero feature removal.
- `apps/svelte-admin/`, `apps/svelte-desktop/`, `apps/tauri/`, `packages/ui/`, `packages/theme/`, and `assets/web-admin/` are protected reference and rollback surfaces and are outside this task's editable scope.

## Current Reconciliation (2026-08-07)

- The proposed six-crate split remains a design-only future refactor. The current monolithic Cargo dependency graph, target ownership, feature redistribution, and cycle-breaking interfaces were not changed or proven. This scope is audited and explicitly excluded from the current read-only remediation pass.

## Current Reconciliation (2026-08-08)

- TODO-561 is closed and this backend-only structural refactor is now the active task. No source or manifest move has started. The first required deliverable is a complete dependency and target inventory, followed by a seam design that proves cycle ownership before any edit.
- Frontend and Tauri paths remain protected and outside scope: `apps/svelte-admin/`, `apps/svelte-desktop/`, `apps/tauri/`, `packages/ui/`, `packages/theme/`, and `assets/web-admin/`.

## Split Reconciliation (2026-08-08)

- The read-only inventory exposed a 16-module strongly connected component across the proposed crate boundaries, 234 Rust files, 203,361 lines, 50 dependencies (48 normal and 2 dev), 29 features, and the root package's 95 targets (1 lib, 1 custom-build, 9 bins, 9 examples, 71 tests, and 4 benches). A one-shot six-crate move would require simultaneous cycle-breaking API changes and would not be safely reviewable or independently verifiable.
- This task is therefore blocked for decomposition, not abandoned. TODO-880 owns the seam graph and migration contract; TODO-881 owns the first real `qf-common` leaf-crate extraction. No product/runtime, dependency behavior, release, server, or frontend change was made by this audit.

## Common Contract Child Reconciliation (2026-08-08)

- TODO-881 extracts `env_utils`, `time_source`, `rng`, and the zeroizing secret owners into the independently buildable `qf-common` workspace member. The root retains explicit compatibility re-export modules, and the root unit-test harness includes the moved source only under `cfg(test)` so test-only controls remain scoped without a second production owner.
- The updated seam audit inventories both workspace source trees: `quicfuscate` depends on `qf-common` through one normal edge, `qf-common` has no reverse dependency, and the product graph remains 159 edges with the same 16-module SCC. The current source inventory is 239 Rust files and 203,446 lines; root has 51 dependencies (49 normal and 2 dev), 29 features, and 95 targets, while qf-common has only `getrandom`, `log`, and `zeroize`.
- TODO-562 remains blocked because the higher-order cycle families and six-crate target are not decomposed by this leaf task. The next child must be selected from measured graph evidence; no frontend or Tauri path is part of this migration.

## Control-plane Child Reconciliation (2026-08-08)

- `crates/qf-control-plane/` now owns the former `src/control_plane.rs` implementation as a second independently buildable leaf. The root module remains a compatibility re-export, and all existing client/server/runtime callers retain the `crate::control_plane` path.
- The extracted crate has no dependencies beyond `std`; Cargo records one normal edge from `quicfuscate` to `qf-control-plane` and no reverse edge. Its codec and receiver unit tests remain attached to the moved source.
- Workspace check, strict all-target Clippy with `rust-tests`, formatting, and the full all-target `rust-tests` suite pass. The suite reports 28 `qf-common` tests, 8 `qf-control-plane` tests, 2,655 root library tests, and green integration targets.
- This child removes one measured leaf boundary but does not claim the higher-order 16-module SCC or the full six-crate acceptance target. The next decomposition must again be selected from refreshed graph evidence.

## Error Contract Child Reconciliation (2026-08-08)

- `crates/qf-error/` now owns the former root `ConnectionError` contract as a third independently buildable std-only leaf. `quicfuscate::error` remains a compatibility re-export, while root-local adapters preserve conversions from `KeyMaterialError` and H3 errors without introducing a reverse dependency.
- The refreshed seam report records four workspace packages, the three one-way root-to-leaf edges (`qf-common`, `qf-control-plane`, and `qf-error`), 240 Rust files, 203,474 source lines, and the unchanged 16-module product SCC. Protected frontend/Tauri paths remain empty.
- Workspace check, strict all-target Clippy with `rust-tests`, formatting, and the complete all-target `rust-tests` suite pass. The package counts are 28 `qf-common`, 8 `qf-control-plane`, 2 `qf-error`, and 2,655 root library tests, with green integration targets.
- TODO-562 remains blocked for the higher-order cycle families and full six-crate target; this leaf does not claim either. No frontend field or UI projection was added.

## Instrumentation Child Reconciliation (2026-08-08)

- `crates/qf-instrumentation/` now owns the former root `instrumentation` module as a fourth independently buildable std-only leaf. The root module is a compatibility re-export, so all engine, client, and server metric producers keep their existing paths.
- The refreshed seam report records five workspace packages, the four one-way root-to-leaf edges, 240 Rust files, 203,476 source lines, the unchanged 16-module product SCC, and no protected frontend/Tauri changes.
- Workspace check, strict all-target Clippy with `rust-tests`, formatting, and the complete all-target `rust-tests` suite pass. Package counts are 28 `qf-common`, 8 `qf-control-plane`, 2 `qf-error`, 5 `qf-instrumentation`, and 2,650 root library tests, with green integration targets.
- TODO-562 remains blocked for the higher-order cycle families and full six-crate target. This metrics leaf does not claim either and adds no frontend field or UI projection.

## Privilege Child Reconciliation (2026-08-08)

- `crates/qf-privilege/` now owns the former root privilege module as a fifth independently buildable leaf. The root `quicfuscate::privilege` module is a compatibility re-export, so runtime and CLI callers keep their existing public paths.
- The refreshed seam report records six workspace packages, the five one-way root-to-leaf edges, 240 Rust files, 203,478 source lines, the unchanged 16-module product SCC, and no protected frontend/Tauri changes.
- Workspace check, strict all-target Clippy with `rust-tests`, formatting, and the complete all-target `rust-tests` suite pass. Package counts are 28 `qf-common`, 8 `qf-control-plane`, 2 `qf-error`, 5 `qf-instrumentation`, 23 `qf-privilege`, and 2,627 root library tests, with green integration targets.
- TODO-562 remains blocked for the higher-order cycle families and full six-crate target. This privilege leaf does not claim either and adds no frontend field or UI projection.

## PKI Child Reconciliation (2026-08-08)

- `crates/qf-pki/` now owns the former `src/pki/mod.rs` CA hierarchy, certificate-chain validation, safe PEM writers, quarantine handling, and PKI tests. The root `quicfuscate::pki` module is a compatibility re-export, so qftls and server callers keep their existing paths without a product dependency flowing back into the child.
- The child depends on `qf-common` for the canonical wall-clock source and keeps its explicit PKI boundary (`base64`, `libc`, `log`, `rustls`, `zeroize`, optional `rcgen` and `time`). Root `server` and `dev-certs` forward `qf-pki/rcgen`; no previously crate-internal PKI item was widened solely for the move. No frontend or Tauri path changed.
- The post-commit seam report is `scripts/out/audits/workspace-seams-20260808T212716Z/workspace-seams.json` at revision `e61c21ea6edeca608ce239d315ef941815f4cc21`: seven workspace packages (`quicfuscate` plus six leaves), 240 Rust files, 203,481 source lines, one unchanged 16-module product SCC, workspace edges `qf-pki -> qf-common` and root -> all six leaves, and an empty protected-path change set.
- Verification passes for every leaf `cargo check -p` target, qf-pki with and without `rcgen`, workspace all-target check, strict all-target Clippy with `rust-tests`, formatting, and the complete all-target `rust-tests` suite. Package counts are 28 `qf-common`, 8 `qf-control-plane`, 2 `qf-error`, 5 `qf-instrumentation`, 19 `qf-pki`, 23 `qf-privilege`, and 2,608 root library tests; all registered integration targets are green. A touched-file qf-pki dev rebuild measured 0.75 seconds real time.
- The acceptance `cargo clippy --workspace --all-targets --all-features -- -D warnings` lane remains platform-blocked by the existing Linux-only `rt-transport-uring` compile guard on macOS (`scripts/tests/rust/rt-transport-uring.rs:8`). The release binary gate is locally green: `cargo build --release --bin quicfuscate --locked --offline` succeeded, `target/release/quicfuscate --help` returned the canonical CLI, and the 9,815,328-byte binary hashed to `002a79251e7469930412e295c17306ced9996b282abb105c122f85ba2a14f61b`. A pre/post SHA-256 manifest over all 149 protected Svelte/Tauri paths is identical. Post-commit Graphify evidence remains fail-closed `BLOCKED` at `scripts/out/audits/graphify-20260808T212644Z/graphify-evidence.json`. CI workspace execution, native Linux/Omega proof, and the 16-module SCC decomposition remain open gates. TODO-562 stays `BLOCKED` rather than overstating this child slice.

## Reality Child Reconciliation (2026-08-08)

- `crates/qf-reality/` now owns the former `src/reality.rs` implementation: active-probe UDP fallback sessions, bounded target selection, REALITY configuration, cover-site TLS capture/cache, raw-flight parsing, and all 23 existing unit tests. The root `quicfuscate::reality` module is a direct compatibility re-export, so stealth runtime and manager callers retain their existing paths.
- The child depends only on `qf-common` plus its explicit Tokio, parking_lot, logging, and TLS capture libraries. Root lifecycle callers use the documented child hooks for snapshot construction, stale-session cleanup, and shutdown; no product dependency flows back into the child. `qf-common` exposes its deterministic `EnvSnapshot` test constructor only through the `rust-tests` feature, and the root compatibility surface now uses the single qf-common `EnvSnapshot` type in test and production builds.
- Isolated verification passes `cargo check -p qf-reality --all-targets --all-features`, qf-reality tests `23/23`, workspace all-target checking with default features and with `rust-tests`, formatting, and strict workspace Clippy with `rust-tests`. The 12-GiB target guard triggered `cargo clean` once during the feature matrix and removed 13.9 GiB; no protected frontend/Tauri path changed.
- Post-commit seam evidence is `scripts/out/audits/workspace-seams-20260808T235531/workspace-seams.json` at revision `88cbcb67fd916ec6b91ef1f191e947f5b2164546`: eight workspace packages, 240 Rust files, 203,473 source lines, eleven dependency edges, the unchanged 16-module product SCC, and `protected_changes=[]`. The full all-target `rust-tests` workspace run completed without failures with qf-common=28, qf-control-plane=8, qf-error=2, qf-instrumentation=5, qf-pki=19, qf-privilege=23, qf-reality=23, and root=2,570 tests, plus green integration targets. The release gate is green after release-only TLS warning cleanup: canonical `--help`, 9,832,176-byte binary, SHA-256 `086eb07262347ae0f686eb872831c44428dacec09282512bb299868a100e6bf5`, and an empty protected frontend/Tauri path diff. Graphify remains fail-closed `BLOCKED` at `scripts/out/audits/graphify-20260808T215553Z/graphify-evidence.json`. The 16-module product SCC remains intentionally unchanged; CI, native Linux/Omega, and the all-feature Linux-only integration lane remain separate gates.

## DNS Child Reconciliation (2026-08-09)

- `crates/qf-dns/` now owns the former `src/dns/mod.rs` implementation: bounded parser and wire-preserving synthetic responses, pre-resolved pooled DoH, authenticated UDP response matching, aggregate deadlines, and bounded admission control. The root `quicfuscate::dns` module remains a compatibility re-export for the client runtime.
- qf-dns depends on qf-common's canonical `ProtocolClock`; qf-common's manual clock and thread-local test override are feature-gated by `rust-tests`, so root and child tests use one clock type without a duplicate production owner. No frontend or Tauri path changed.
- Post-commit seam evidence is `scripts/out/audits/workspace-seams-20260809T001834/workspace-seams.json` at revision `26bc6bb08fa15d63c0b6c408d33382ed51ab447d`: nine workspace packages, 240 Rust files, 203,467 source lines, fourteen dependency edges, the unchanged 16-module product SCC, and `protected_changes=[]`. Isolated qf-dns checking and `41/41` tests pass, and the full all-target `rust-tests` workspace run completes without failures with qf-common=28, qf-control-plane=8, qf-dns=41, qf-error=2, qf-instrumentation=5, qf-pki=19, qf-privilege=23, qf-reality=23, and root=2,522 tests plus green integration targets. The release gate is green with canonical `--help`, a 9,865,392-byte binary, SHA-256 `23bf3bea7b26dc21c6db12b3669a6d9d44c561d3918a7bf3ad9c1efbd459480b`, and an empty protected frontend/Tauri path diff. All-feature Clippy remains blocked only by the existing Linux-only guard at `scripts/tests/rust/rt-transport-uring.rs:8`; Graphify is fail-closed `BLOCKED` at `scripts/out/audits/graphify-20260808T221834Z/graphify-evidence.json`.

## Firewall Child Reconciliation (2026-08-09)

- `crates/qf-firewall/` now owns the backend selection implementation and the root-independent serde/default `FirewallConfig` contract alongside iptables/nftables operations, platform cleanup contracts, and the existing firewall tests. Root `quicfuscate::firewall` and `engine::FirewallConfig` remain compatibility reexports; internal caller paths stay unchanged.
- The current configuration-owner verification passes `29/29` qf-firewall tests, root all-target checking, strict root Clippy, and root `EngineConfig` tests `39/39`. Fresh seam evidence is `scripts/out/audits/workspace-seams-20260809T-firewall-config/workspace-seams.json`: 35 workspace packages, 307 Rust files, 205,341 source lines, 128 product-module edges, 94 Cargo workspace dependency edges, the unchanged 9-module product SCC, and `protected_changes=[]`. No frontend field/API projection is required.
- The child has no frontend or Tauri dependency and is selected from the refreshed cycle-free seam inventory. Isolated qf-firewall all-target/all-feature checking, strict Clippy, workspace default/all-target and all-feature checks, and workspace `rust-tests` Clippy pass. The serial all-target `rust-tests` workspace run is green with qf-common=28, qf-control-plane=8, qf-dns=41, qf-error=2, qf-firewall=28, qf-instrumentation=5, qf-pki=19, qf-privilege=23, qf-reality=23, and root=2,494 tests, plus green registered integration targets. All-feature all-target Clippy remains blocked only by the existing Linux-only guards at `scripts/tests/rust/rt-io-hotpath-kernel-integration.rs:4` and `scripts/tests/rust/rt-transport-uring.rs:8`; no guard was weakened. The release gate is green with canonical `--help`, a 9,865,472-byte binary, and SHA-256 `921f2764de6714e4cfbb41651896b35805a45d7ac929cf765016b528e243417d`; protected frontend/Tauri paths are unchanged. Post-documentation-commit seam evidence is `scripts/out/audits/workspace-seams-20260808T224804/workspace-seams.json` at revision `2c318c43eca2b6646275c39432acf7879fbc5bd4`, and Graphify remains fail-closed `BLOCKED` at `scripts/out/audits/graphify-20260808T224826Z/graphify-evidence.json` with exit 2.

## Audit Child Reconciliation (2026-08-09)

- `crates/qf-audit/` now owns the former `src/audit/mod.rs` implementation and the root-independent serde/TOML `AuditConfig` projection: bounded hash-chained NDJSON persistence, rotation and checkpoint recovery, tamper verification, concurrent producer admission, durability watchdogs, and fail-closed Unix/Windows file hardening. The public audit event/config/stats/error model is physically isolated in `crates/qf-audit/src/model.rs`; `lib.rs` remains the runtime aggregate and compatibility re-export surface. The root `quicfuscate::audit` module remains a compatibility re-export, while `engine::AuditConfig` is a compatibility re-export and maps child validation errors into the historical engine error surface.
- The child depends on `qf-common::time_source` plus its explicit `crossbeam-channel`, `libc`, `log`, `serde`, `serde_json`, and Windows `windows-sys` boundary. Isolated qf-audit check, `41/41` historical tests, strict all-target Clippy, root compatibility tests `2,453/2,453`, workspace all-target check, workspace `rust-tests` Clippy, formatting, and protected-path checks pass; no frontend or Tauri path changed.
- The current configuration-owner verification adds `AuditConfig` coverage to qf-audit (`42/42` tests), root `EngineConfig` tests remain `39/39`, root all-target checking and strict root Clippy pass, and the child preserves the exact millisecond wire shape and shared option bounds. The model extraction keeps qf-audit green with `lib.rs` at 1,559 lines and `model.rs` at 337 lines; `cargo check --all-features`, strict qf-audit Clippy, formatting, diff hygiene, and the include/size structure gate pass. Fresh seam evidence is `scripts/out/audits/workspace-seams-20260809T-audit-config/workspace-seams.json`: 35 workspace packages, 307 Rust files, 205,397 source lines, 129 product-module edges, 94 Cargo workspace dependency edges, the unchanged 9-module product SCC, `qf-audit -> qf-common`, `quicfuscate -> qf-audit`, and `protected_changes=[]`. No frontend field/API projection is required.
- Post-code-commit seam evidence is `scripts/out/audits/workspace-seams-20260808T225943/workspace-seams.json` at revision `74d4a7afb079312094ada4c09175a705ae5edb52`: eleven workspace packages, 240 Rust files, 203,587 source lines, 154 product-module edges, the unchanged 16-module product SCC, seventeen Cargo workspace dependency edges including `qf-audit -> qf-common` and `quicfuscate -> qf-audit`, and `protected_changes=[]`. The serial all-target `rust-tests` workspace run is green with qf-audit=41, qf-common=28, qf-control-plane=8, qf-dns=41, qf-error=2, qf-firewall=28, qf-instrumentation=5, qf-pki=19, qf-privilege=23, qf-reality=23, and root=2,453 tests, plus green registered integration, binary, and example targets. The release gate is green: `cargo build --release --bin quicfuscate --locked --offline` succeeded, canonical `--help` returned, the binary is 9,915,456 bytes, and SHA-256 is `e84e06d257ae768c3770c58f8644505f1d55187740988459ed1215267854e404`. The all-feature all-target Clippy lane remains platform-blocked only by the existing Linux guard at `scripts/tests/rust/rt-transport-uring.rs:8`; no guard was weakened. Graphify remains fail-closed `BLOCKED` at `scripts/out/audits/graphify-20260808T225944Z/graphify-evidence.json` with exit 2. The target stayed below the 12-GiB cleanup threshold during this cycle.

## Logging Child Reconciliation (2026-08-09)

- `crates/qf-logging/` owns the former `src/logging.rs` implementation and the complete root-independent serde/default/validation `LoggingConfig`, `LoggingMode`, and `LogFormat` contracts: bounded producer admission, structured text/JSON output, rotating/reopenable private file sinks, RFC 5424 syslog, flush barriers, and worker statistics. The root `quicfuscate::logging` module keeps the historical generic projection trait as a compatibility adapter that clones the child-owned config; `engine::LoggingConfig`, `engine::LoggingMode`, and `engine::LogFormat` are compatibility reexports. `AdminLogBuffer` implements the child `LogSink` in the server runtime, so the child has no reverse dependency on implementations.
- The child depends on `qf-common::time_source`, `crossbeam-channel`, `log` with its explicit `std` feature, `serde`, and `serde_json`; Unix test hardening uses a target-only `libc` dev dependency. No frontend or Tauri path changed. Isolated qf-logging check, `20/20` historical tests, strict all-target Clippy, workspace all-target check, workspace `rust-tests` Clippy, root `2,433/2,433`, formatting, and the complete serial all-target `rust-tests` workspace suite pass; all registered integration, binary, and example targets are green.
- The current configuration-owner verification passes `22/22` qf-logging tests, root `EngineConfig` tests `39/39`, root all-target checking, and strict root Clippy. Seam evidence is `scripts/out/audits/workspace-seams-20260809T-logging-config-full/workspace-seams.json`: 35 workspace packages, 307 Rust files, 205,331 source lines, 128 product-module edges, 94 Cargo workspace dependency edges, the 9-module product SCC (`brain`, `core`, `engine`, `fec`, `implementations`, `interface`, `qftls`, `stealth`, `transport`), `qf-logging -> qf-common`, `quicfuscate -> qf-logging`, and `protected_changes=[]`.
- The release gate is green with canonical `--help`, a 9,932,512-byte binary, and SHA-256 `0c321b42971d41c358af586a07a60df6293cb2e15d00dec3ab0ef33a99d249e8`. All-feature all-target Clippy remains blocked only by the existing Linux guards at `scripts/tests/rust/rt-transport-uring.rs:8` and `scripts/tests/rust/rt-io-hotpath-kernel-integration.rs:4`; no guard was weakened. The target was proactively cleaned at 11,782,076 KiB before release, removing 12.8 GiB.
- Post-code-commit seam evidence is `scripts/out/audits/workspace-seams-20260808T233130/workspace-seams.json` at revision `634e1e34aac32329633795510d73dd40d88a98aa`: twelve workspace packages, 241 Rust files, 203,743 source lines, 152 product-module edges, the unchanged 16-module product SCC, nineteen Cargo workspace dependency edges including `qf-logging -> qf-common` and `quicfuscate -> qf-logging`, and `protected_changes=[]`. Graphify remains fail-closed `BLOCKED` at `scripts/out/audits/graphify-20260808T233131Z/graphify-evidence.json`; no semantic extraction or relationship proof is inferred.

## Metrics Child Reconciliation (2026-08-09)

- `crates/qf-metrics/` now owns the former `src/metrics.rs` telemetry HTTP server: bounded request reads, `/telemetry` classification, plain-text exporter responses, 404 handling, socket shutdown, and the 32-connection admission semaphore. The root `quicfuscate::metrics` module remains a compatibility wrapper with the original no-argument API and injects `crate::telemetry::export_telemetry_text` into the child, so the child has no reverse dependency on root telemetry.
- The child depends on `qf-common` for the captured environment snapshot, `tokio` for the bounded TCP server, and `log`; qf-common's `rust-tests` environment lock is used only by child tests. Cargo records normal and dev `qf-metrics -> qf-common` edges plus `quicfuscate -> qf-metrics`. No transport, crypto, implementation, frontend, or Tauri dependency crosses this boundary.
- Isolated qf-metrics all-target/all-feature check, `4/4` tests, strict all-target Clippy, workspace all-target check, workspace `rust-tests` Clippy, formatting, diff hygiene, and the protected frontend/Tauri check pass. The complete serial all-target `rust-tests` workspace suite is green with 96 result blocks and package counts qf-audit=41, qf-common=28, qf-control-plane=8, qf-dns=41, qf-error=2, qf-firewall=28, qf-instrumentation=5, qf-logging=20, qf-metrics=4, qf-pki=19, qf-privilege=23, qf-reality=23, and root=2,429; the dedicated `/telemetry` HTTP integration target is `1/1` green.
- The release gate is green: `cargo build --release --bin quicfuscate --locked --offline` succeeded, canonical `--help` returned, the binary is 9,949,040 bytes, and SHA-256 is `b83aa18d6a856cbd6ca6a491ffc833ac32c52147e6ef35b804dd189a4ae575b8`. All-feature all-target Clippy remains blocked only by the existing Linux-only guard at `scripts/tests/rust/rt-transport-uring.rs:8`; no guard was weakened. Post-code seam evidence is `scripts/out/audits/workspace-seams-20260808T235059Z/workspace-seams.json` at revision `4d98426e2d81cd01217c8e50c56e38c7aef70e88`, recording thirteen workspace packages, 242 Rust files, 203,750 source lines, 151 product-module edges, 22 Cargo dependency edges, the unchanged 16-module product SCC, and `protected_changes=[]`. Graphify is fail-closed `BLOCKED` with exit 2 at `scripts/out/audits/graphify-20260808T235107Z/graphify-evidence.json`; no semantic extraction or relationship proof is inferred. The target ended at 5,863,848 KiB, below the 12-GiB cleanup threshold.
- The release gate is green: `cargo build --release --bin quicfuscate --locked --offline` succeeded, canonical `--help` returned, the binary is 9,949,040 bytes, and SHA-256 is `b83aa18d6a856cbd6ca6a491ffc833ac32c52147e6ef35b804dd189a4ae575b8`. All-feature all-target Clippy remains blocked only by the existing Linux-only guard at `scripts/tests/rust/rt-transport-uring.rs:8`; no guard was weakened. Post-code seam evidence is `scripts/out/audits/workspace-seams-20260808T235059Z/workspace-seams.json` at revision `4d98426e2d81cd01217c8e50c56e38c7aef70e88`, recording thirteen workspace packages, 242 Rust files, 203,750 source lines, 151 product-module edges, 22 Cargo dependency edges, the unchanged 16-module product SCC, and `protected_changes=[]`. Graphify is fail-closed `BLOCKED` with exit 2 at `scripts/out/audits/graphify-20260808T235107Z/graphify-evidence.json`; no semantic extraction or relationship proof is inferred. The target ended at 5,863,848 KiB, below the 12-GiB cleanup threshold.

## Harness Child Reconciliation (2026-08-09)

- `crates/qf-harness/` now owns the former `src/harness.rs` developer CLI and benchmark orchestration for QPACK and UDP throughput. The root `quicfuscate::harness` module re-exports `Cli`, `Command`, and the backend contracts while preserving `run_cli`, `run_from_args`, and `run_from_env`; no root caller or registered harness target changes its API.
- The child depends only on `clap`. `QpackEncoder`, `UdpSender`, and `UdpSenderFactory` invert the two root-owned production backends, with the root adapter supplying SIMD QPACK dispatch and `transport::udpfast::UdpFastPath`. Cargo records only `quicfuscate -> qf-harness`; no transport, crypto, implementation, frontend, or Tauri dependency crosses into the child.
- Isolated qf-harness all-target/all-feature checking and strict Clippy pass. The real root harness integrations are green (`rt-harness-cli=1/1`, `rt-harness-udpfast=1/1`), workspace all-target checking and strict `rust-tests` Clippy pass, and the complete serial all-target `rust-tests` workspace suite is green with 97 result blocks and root `2429/2429`; all registered package, binary, example, and integration targets are green. Formatting, diff hygiene, and the protected frontend/Tauri check pass.
- The release gate is green: canonical `--help` returned, the binary is 9,949,232 bytes, and SHA-256 is `8cd9d37e8df25edb983af7874817b806bea1133cd1ebe7fb236e7615746c8ef3`. All-feature all-target Clippy remains blocked by the existing Linux-only guards at `scripts/tests/rust/rt-io-hotpath-kernel-integration.rs:4` and `scripts/tests/rust/rt-transport-uring.rs:8`; the current run stopped at the first guard and neither was weakened. Post-code seam evidence is `scripts/out/audits/workspace-seams-20260809T000524Z/workspace-seams.json` at revision `04d0b964d7befd586a4c2c77c422d2cbfc894cc8`, recording fourteen workspace packages, 243 Rust files, 203,783 source lines, 151 product-module edges, 23 Cargo dependency edges, the unchanged 16-module product SCC, and `protected_changes=[]`. Graphify remains fail-closed `BLOCKED` with exit 2 at `scripts/out/audits/graphify-20260809T000529Z/graphify-evidence.json`; no semantic extraction or relationship proof is inferred. The target ended at 9,195,100 KiB, below the 12-GiB cleanup threshold.

## Compatibility Seam Reconciliation (2026-08-09)

- `src/logging.rs` now exposes a generic `LoggingConfigProjection` contract. The engine implements the projection in `src/engine/config.rs`, so the root logging compatibility namespace no longer imports `engine`; existing `quicfuscate::logging::init(&engine.logging)` callers and logger behavior remain unchanged.
- `src/accelerate.rs` remains the public compatibility namespace for test and tooling consumers, while runtime owners import `crate::optimize` directly for brain, compression, FEC, stealth, transport, and core acceleration. No implementation is duplicated; the compatibility module is outside the product cycle.
- Post-code seam evidence is `scripts/out/audits/workspace-seams-20260809T002504Z/workspace-seams.json` at revision `99643a084a6167e510ba7f3a902576950a22cfa0`: fourteen workspace packages, 243 Rust files, 203,784 source lines, 145 product-module edges, 23 Cargo dependency edges, and a 14-module product SCC (`brain`, `compress`, `core`, `crypto`, `engine`, `fec`, `implementations`, `interface`, `memory_lock`, `optimize`, `qftls`, `simd`, `stealth`, `transport`). `accelerate -> optimize` is one-way, `engine -> logging` and `implementations -> logging` are non-cyclic, and `protected_changes=[]`.
- Workspace all-target checking, strict `rust-tests` Clippy, and the complete serial all-target `rust-tests` suite pass with exit status 0, 97 result blocks, no failures, and root `2429/2429`; formatting and diff hygiene pass. The release gate is green: canonical `--help` returned, the binary is 9,949,312 bytes, and SHA-256 is `ef9a6d84072683643a97eb6f8b57b0815c7bc9a8311eff1149019a7a69adc4bb`. All-feature all-target Clippy remains blocked only by the existing macOS-incompatible guard at `scripts/tests/rust/rt-transport-uring.rs:8`. Graphify is fail-closed `BLOCKED` with exit 2 at `scripts/out/audits/graphify-20260809T002535Z/graphify-evidence.json`; no semantic relationship proof is inferred.

## Common Compatibility Hook Reconciliation (2026-08-09)

- `src/rng.rs` and `src/secret.rs` now re-export qf-common in production and root test builds. qf-common's `rust-tests` feature owns deterministic entropy-failure injection and erasure observation, removing the former root `#[path]` duplicate implementations without widening the default production API.
- qf-common tests pass `28/28`; root library tests pass `2423/2423`; the complete workspace all-target run is green with `97` result blocks and no failure markers. The six duplicate root copies of qf-common's RNG/secret tests no longer compile because their canonical child tests already execute; no unique behavioral coverage was removed. Workspace checking, strict `rust-tests` Clippy, formatting, and diff hygiene pass.
- Working-tree seam evidence is `scripts/out/audits/workspace-seams-20260809T025044/workspace-seams.json` at base revision `d69e10446c92be684346387d613c28340f18b25f` plus the four compatibility-hook source edits: fourteen workspace packages, 243 Rust files, 203,765 source lines, 145 product-module edges, the same 14-module product SCC, and `protected_changes=[]`.
- The remaining backend seam audit finds no additional safe leaf: `profile.rs` is a one-way compatibility alias over `simd::CryptoAeadPlan`, so a separate crate would add a wrapper without removing a product-cycle node; the other non-SCC contracts are already extracted or canonicalized. The 14-module SCC therefore remains the next decomposition boundary and requires a coordinated cycle-breaking design.
- Release verification passes: canonical `--help`, 9,949,312-byte binary, SHA-256 `ef9a6d84072683643a97eb6f8b57b0815c7bc9a8311eff1149019a7a69adc4bb`; target cleanup at 12,004,968 KiB left 494,116 KiB after release. All-feature all-target Clippy remains blocked by the existing macOS-incompatible Linux guard at `scripts/tests/rust/rt-io-hotpath-kernel-integration.rs:4`.
- Latest Graphify evidence is fail-closed `BLOCKED` with exit 2 at `scripts/out/audits/graphify-20260809T005700Z/graphify-evidence.json`; semantic extraction, complete language coverage, and relationship proof remain unavailable.

## Memory-Lock Child Reconciliation (2026-08-09)

- `crates/qf-memory-lock/` now owns the former `src/memory_lock.rs` implementation: `MemoryLockFailurePolicy`, process-lock status and error contracts, `mlockall` limit classification, startup policy application, pooled-block lock state, and future-allocation coverage state. The root `src/memory_lock.rs` is a `SecurityConfig` compatibility adapter; engine and server implementation callers use the child directly, and qftls plus `MemoryPool` delegate compatibility setters to child-owned state.
- The post-code seam report is `scripts/out/audits/workspace-seams-20260809T011935Z/workspace-seams.json` at base revision `d69e10446c92be684346387d613c28340f18b25f`: fifteen workspace packages, 244 Rust files, 203,881 source lines, 141 product-module edges, 24 Cargo workspace dependency edges, and a 13-module product SCC. The only qf-memory-lock workspace edge is `quicfuscate -> qf-memory-lock`; `protected_changes=[]`. This is a real one-way boundary and removes `memory_lock` from the product SCC, but TODO-562 remains blocked for the remaining higher-order SCC and external acceptance gates.
- Isolated qf-memory-lock tests pass `11/11`; root library tests pass `2,413/2,413`; the complete serial all-target workspace `rust-tests` run passes 98 result blocks with 2,999 passed, 0 failed, and 6 ignored. Workspace checking, strict `rust-tests` Clippy, formatting, and diff hygiene pass. Release verification passes with canonical `--help`, a 9,949,296-byte binary, and SHA-256 `b2dbf04613e5ccb666e41a65b1442e91f54dfd402842ecc76be4af419ed18fa0`.
- Independent all-target/all-feature checks and strict all-target/all-feature Clippy pass for every extracted leaf: `qf-audit`, `qf-common`, `qf-control-plane`, `qf-dns`, `qf-error`, `qf-firewall`, `qf-harness`, `qf-instrumentation`, `qf-logging`, `qf-memory-lock`, `qf-metrics`, `qf-pki`, `qf-privilege`, and `qf-reality`. A real touch-rebuild of qf-memory-lock completed in `0.85s` real time, below the 15-second incremental target.
- All-feature all-target Clippy remains blocked by the existing Linux-only guard at `scripts/tests/rust/rt-io-hotpath-kernel-integration.rs:4`; no guard was weakened. Latest Graphify evidence remains fail-closed `BLOCKED` with exit 2 at `scripts/out/audits/graphify-20260809T011953Z/graphify-evidence.json`; semantic extraction and complete relationship proof are unavailable. Frontend and Tauri paths remain outside this backend slice.

## Telemetry Workspace Crate Reconciliation (2026-08-09)

- `crates/qf-telemetry/` now owns the former `src/optimize/telemetry.rs` implementation and the root-independent serde/default `TelemetryConfig` contract: atomic counters and gauges, text export, CPU-profile mask calculation, memory-usage refresh, and all telemetry tests. Root `engine::TelemetryConfig`, `quicfuscate::telemetry`, and `quicfuscate::optimize::telemetry` remain compatibility projections.
- The child depends only on `sysinfo`. A stable `CpuProfileId` boundary keeps the child independent of `optimize`; the root adapter maps the existing `CpuProfile`. Memory-pool refresh uses one owner-installed callback and retains the previous no-side-effect observation contract.
- The CPU profile mask contract is now physically owned by `crates/qf-telemetry/src/profile_mask.rs`; `lib.rs` retains the telemetry aggregate and public re-export boundary. The cut preserves `CpuProfileId`, all mask values, `cpu_profile_mask_for_id`, and `publish_cpu_profile_mask_for_id` without changing the root adapter or exported metric names. Current local proof: qf-telemetry `17/17` all-feature library tests, strict all-target/all-feature Clippy, `cargo check --all-features` including root, formatting, and diff hygiene pass. No frontend or Tauri path changed.
- The current configuration-owner verification adds serde/default `TelemetryConfig` coverage to qf-telemetry: `16/16` child tests, root `EngineConfig` tests `39/39`, root all-target checking, and strict root Clippy pass while preserving the exact engine schema and validation error. Fresh seam evidence is `scripts/out/audits/workspace-seams-20260809T-telemetry-config/workspace-seams.json`: 35 workspace packages, 307 Rust files, 205,413 source lines, 129 product-module edges, 94 Cargo workspace dependency edges, the unchanged 9-module product SCC, `qf-telemetry` inbound leaf edges only, and `protected_changes=[]`. No frontend field/API projection is required.
- Isolated qf-telemetry all-target/all-feature checking, `15/15` tests, strict all-target/all-feature Clippy, formatting, and root library compatibility tests `2,398/2,398` pass. Working-tree seam evidence is `scripts/out/audits/workspace-seams-20260809T014342Z/workspace-seams.json`: sixteen workspace packages, 245 Rust files, 203,986 source lines, the 13-module product SCC, `quicfuscate -> qf-telemetry`, and `protected_changes=[]`.
- The complete serial workspace all-target `rust-tests` run passes 99 result blocks with 2,999 passed, 0 failed, and 6 ignored; qf-telemetry contributes `15/15` and the root library contributes `2,398/2,398`. Workspace all-target checking and strict `rust-tests` Clippy pass. Release verification is green with canonical `--help`, a 9,949,296-byte binary, and SHA-256 `62b12fb50dc3aefd2bdb75c823f339e6e388d098ac20b3af886f3d002b6a0970`. Full all-feature/all-target Clippy remains platform-blocked by the existing Linux-only guard at `scripts/tests/rust/rt-transport-uring.rs:8`; no guard was weakened. The post-code seam report is `scripts/out/audits/workspace-seams-20260809T015627Z/workspace-seams.json` with sixteen workspace packages, 245 Rust files, 203,986 source lines, 141 product-module edges, 25 Cargo workspace dependency edges, the unchanged 13-module product SCC, and `protected_changes=[]`. Graphify remains fail-closed `BLOCKED` with exit 2 at `scripts/out/audits/graphify-20260809T015552Z/graphify-evidence.json`; semantic extraction and complete relationship proof remain unavailable. The target ended at 6,710,076 KiB, below the 12-GiB cleanup threshold. No frontend or Tauri path was changed.

## CPU Workspace Crate Reconciliation (2026-08-09, TODO-562)

- `crates/qf-cpu/` now owns the former `src/optimize/parts/cpu_dispatch.rs` implementation, the former `src/simd/planner.rs` module, the former compression SIMD helpers from `src/optimize/compress.rs` and `src/optimize/simd/compress.rs`, the former `src/optimize/iter.rs` and `src/optimize/sort.rs` implementations, and the runtime substring-search implementation formerly in `src/optimize/string.rs`: CPU feature detection, cache hierarchy, prefetch policy, SIMD dispatch contracts, FEC bitslice dispatch, AEAD planning, byte classification, histograms, pattern search, f32/u32/u64 iterator reductions, sorting, argsort, and accelerated `string_contains`. Root `optimize` and `simd` paths retain compatibility imports while test/rust-tests-gated Base64 helpers remain root-owned.
- The child depends only on `qf-common` for environment snapshots, `qf-telemetry` for selection counters, `libc`, and `log`. Root feature selectors forward `prefetch`, `aggressive_inline`, `internal_avx10_preview`, and `rust-tests` to the child; the obsolete root `cpufeatures` direct dependency was removed. No frontend or Tauri path changed.
- The sort slice is now included in qf-cpu. Isolated combined-leaf verification passes `58/58` all-target/all-feature tests with strict all-target/all-feature Clippy. Root all-target checking, root strict `rust-tests` Clippy, the complete workspace all-target `rust-tests` suite (`3,005` passed, `0` failed, `6` ignored), and formatting pass.
- The SIMD policy and dispatch contract is now physically owned by `crates/qf-cpu/src/simd_policy.rs`; `lib.rs` retains only the qf-cpu aggregate and compatibility re-export boundary. The cut preserves `SimdPolicy`, all marker types, `dispatch`, `dispatch_bitslice`, the FEC override hook, and test-only override helpers without changing caller paths. Current local proof on revision `60a67c9065e52c04dd2292ee50739c240c442ff6`: qf-cpu `87/87` all-feature library tests, strict all-target/all-feature Clippy, `cargo check --all-features` including qf-fec and root, formatting, root zero-copy/memory-pool proofs from the preceding cut, `git diff --check`, and the Rust module-structure gate all pass. The qf-cpu aggregate file is now `1,669` lines and the new policy owner is `322` lines; no frontend or Tauri path changed.
- Warning-free release verification with `RUSTFLAGS=-Dwarnings` passes: canonical `--help` returned, the binary is `10,016,880` bytes, and SHA-256 is `1d24166193651e304bfa580fa338ea4dde8889c834f8721ca95094dbf79ba87b`. The target ended at `10,831,616 KiB`, below the `12,582,912 KiB` cleanup threshold; qf-cpu still has only `qf-cpu -> qf-common` and `qf-cpu -> qf-telemetry` workspace edges.
- The qf-cpu extraction removes direct crypto-to-optimize coupling and preserves the root `crate::optimize`/`crate::simd` compatibility surface; runtime guardrails resolve qf-cpu, qf-crypto, qf-privilege, qf-memory-lock, and qf-instrumentation owners directly. The current guardrail audit is clean apart from the existing fail-closed AMX native proof blocker at `scripts/out/audits/runtime-guardrails-20260809T090900Z/results.json`; no guard was weakened. Full all-feature/all-target Clippy remains platform-blocked by the existing Linux-only guards, stopping at `scripts/tests/rust/rt-transport-uring.rs:8` in this run and also covering `scripts/tests/rust/rt-io-hotpath-kernel-integration.rs:4`, while the all-feature library/binary Clippy lane passes. Graphify remains fail-closed `BLOCKED` at `scripts/out/audits/graphify-20260809T015552Z/graphify-evidence.json`; semantic extraction and complete relationship proof remain unavailable. Current seam evidence is `scripts/out/audits/workspace-seams-20260809T082400Z/workspace-seams.json`: twenty-one workspace packages, 253 Rust files, 204,103 source lines, 134 product-module edges, 45 Cargo workspace dependency edges, and the unchanged 11-module product SCC; `protected_changes=[]`. No frontend or Tauri path changed.

## Crypto Workspace Crate Reconciliation (2026-08-09, TODO-562)

- `crates/qf-crypto/` now owns the former `src/crypto/` implementation: AEGIS, MORUS, AES, ChaCha20, AES-GCM, Poly1305, HKDF, QUIC key derivation, header protection, data-plane AEAD selection, the root-independent `DataAeadPreference` and `CryptoConfig` serde/default/validation contracts, and all crypto tests. The root `src/crypto/mod.rs` remains a compatibility adapter; transport-facing header protection and packet AEAD contracts are owned by qf-crypto, while `engine::AeadPreference` and `engine::CryptoConfig` are compatibility reexports and the root `DataAeadConfig` trait remains a thin adapter for the runtime selector.
- The child depends only on `qf-common`, `qf-cpu`, `qf-error`, and `qf-telemetry` for workspace contracts, plus its direct crypto dependencies. Root feature forwarding covers `rust-tests`, `benches`, `std`, `prefetch`, `aggressive_inline`, and `internal_avx10_preview`. No frontend or Tauri path changed, and no frontend field/API addition is required by this backend extraction.
- Isolated qf-crypto tests pass `136/136`; strict all-target/all-feature Clippy passes after the release-only `rng` compatibility alias was cfg-scoped to `test`/`rust-tests`. Root all-target compatibility checking and strict Clippy pass, and the complete serial workspace all-target `rust-tests` run previously passed 102 result blocks with `3,005` passed, `0` failed, and `6` ignored; qf-crypto contributed `135/135` before the configuration-owner test was added. Formatting and workspace `rust-tests` Clippy pass for the current code.
- The CI Clippy matrix now invokes `cargo clippy --workspace --all-targets` for every covered feature combination, so extracted backend leaves are linted together with the root package instead of relying on root-package-only matrix invocations.
- Release verification is warning-free with `RUSTFLAGS=-Dwarnings`: `cargo build --release --bin quicfuscate --locked --offline` succeeded, canonical `--help` returned, the binary is `9,966,912` bytes, and SHA-256 is `b833e97790379c46eaffa531e771dacce8850a5859fd77aa6ba6a9063aa49857`. The target ended at `8,359,384 KiB`, below the `12,582,912 KiB` cleanup threshold; no additional clean was required in this slice.
- The current seam evidence is `scripts/out/audits/workspace-seams-20260809T062828Z/workspace-seams.json`: nineteen workspace packages, 247 Rust files, 204,125 source lines, 136 product-module edges, 35 Cargo workspace dependency edges, and a 12-module product SCC (`brain`, `compress`, `core`, `engine`, `fec`, `implementations`, `interface`, `optimize`, `qftls`, `simd`, `stealth`, `transport`). The root `crypto` adapter has only one-way incoming compatibility edges; `protected_changes=[]`. This is the first measured cycle reduction after the qf-crypto extraction.
- The current crypto configuration-owner seam is independently verified by `scripts/out/audits/workspace-seams-20260809T-crypto-config/workspace-seams.json`: 35 workspace packages, 307 Rust files, 205,427 source lines, 129 product-module edges, 94 Cargo workspace dependency edges, and the unchanged 9-module product SCC (`brain`, `core`, `engine`, `fec`, `implementations`, `interface`, `qftls`, `stealth`, `transport`); `protected_changes=[]`. Isolated qf-crypto tests pass `136/136`, root `EngineConfig` tests pass `39/39`, root all-target checking and strict Clippy pass, and no frontend field/API addition is required.
- Full all-feature/all-target Clippy remains platform-blocked by the existing macOS-incompatible Linux guards, currently stopping at `scripts/tests/rust/rt-io-hotpath-kernel-integration.rs:4` and also covering `scripts/tests/rust/rt-transport-uring.rs:8`; no guard was weakened. The latest Graphify evidence remains fail-closed `BLOCKED` at `scripts/out/audits/graphify-20260809T015552Z/graphify-evidence.json`; semantic extraction and complete relationship proof remain unavailable. TODO-562 remains blocked on coordinated decomposition of the 12-module SCC and external Linux/native/CI acceptance gates.

## Transport Types Workspace Leaf (2026-08-09, TODO-562)

- `crates/qf-transport-types/` owns the dependency-free transport value contracts formerly defined at the root of `src/transport.rs`: `ConnectionId`, `RecvInfo`, `SendInfo`, `EcnMark`, `CongestionControlAlgorithm`, `Frame`, `EcnCounts`, `Stats`, `PathStats`, and `MAX_CONN_ID_LEN`. The root `quicfuscate::transport` namespace re-exports these types unchanged, so packet, FEC, brain, engine, and implementation callers retain their existing paths.
- The child has no dependencies on product, crypto, FEC, stealth, or frontend/Tauri modules. Its six unit tests cover inline CID bounds/identity, oversized-input rejection, datagram metadata/flags, borrowed frame payloads, ECN counters, and statistics defaults. This is the shared contract seam required before a future transport/FEC split; it does not claim to extract the remaining cyclic transport implementation.
- Isolated qf-transport-types all-target/all-feature checking, `6/6` tests, strict all-target/all-feature Clippy, formatting, root compatibility checking, and root strict Clippy pass. The complete workspace `rust-tests` run passes 102 result blocks with 3,005 passed, 0 failed, and 6 ignored; workspace strict `rust-tests` Clippy is green. The post-cycle-break release gate passes with canonical `--help`, a 9,966,912-byte binary, and SHA-256 `b833e97790379c46eaffa531e771dacce8850a5859fd77aa6ba6a9063aa49857`; the target ended at 8,359,384 KiB, below the 12-GiB cleanup threshold. Current seam evidence is `scripts/out/audits/workspace-seams-20260809T062828Z/workspace-seams.json`: nineteen workspace packages, 247 Rust files, 204,125 source lines, 136 product-module edges, 35 Cargo workspace dependency edges, a 12-module product SCC, `quicfuscate -> qf-transport-types`, and `protected_changes=[]`.
- Full all-feature/all-target Clippy remains platform-blocked by the existing macOS-incompatible Linux guards, currently stopping at `scripts/tests/rust/rt-io-hotpath-kernel-integration.rs:4` and also covering `scripts/tests/rust/rt-transport-uring.rs:8`; no guard was weakened. Graphify remains fail-closed `BLOCKED` at `scripts/out/audits/graphify-20260809T015552Z/graphify-evidence.json`. No frontend field/API addition was needed.

## Memory-Pool Workspace Leaf (2026-08-09, TODO-562)

- `crates/qf-memory-pool/` now owns the former `src/optimize/parts/memory_pool.rs` implementation: adaptive pooled allocation, ownership/accounting ledgers, thread-local and locked block caches, NUMA policy/classification, and Unix/Windows zero-copy buffer contracts. The zero-copy error, transfer, ABI-helper, and buffer-wrapper definitions are physically owned by `crates/qf-memory-pool/src/zero_copy_buffers.rs`; `lib.rs` remains the pool/ledger owner and re-exports the public contract unchanged. The root `quicfuscate::optimize` namespace re-exports the pool and zero-copy types unchanged, while its UDP batch compatibility wrapper remains root-owned because it depends on the transport UDP implementation.
- The child depends on `qf-common`, `qf-cpu`, `qf-memory-lock`, and `qf-telemetry` plus its explicit allocation/socket libraries. Its root-facing snapshot constructors and guard methods are hidden compatibility surface only; no reverse dependency flows from the child into the product crate. NUMA classification proof tests remain attached to the moved owner.
- Isolated `qf-memory-pool` tests pass `23/23`, and strict all-target/all-feature Clippy passes. Root all-target checking with `rust-tests`, root strict Clippy with `rust-tests`, and formatting pass. The initial explicit `cargo clean` removed `9.8 GiB`; the target ended at `1,170,376 KiB`, below the `12,582,912 KiB` cleanup threshold.
- Current local extraction proof on revision `60a67c9065e52c04dd2292ee50739c240c442ff6`: `cargo fmt --all -- --check`, `cargo test -p qf-memory-pool --all-features --lib` (`23/23`), strict all-target/all-feature qf-memory-pool Clippy, `cargo check --all-features`, root zero-copy focus (`5/5`), root memory-pool focus (`2/2`), `git diff --check`, and `scripts/tests/audits/audit-rust-module-structure.sh` all pass. The cut preserves the root re-export paths and introduces no frontend or Tauri surface.
- Seam evidence is `scripts/out/audits/workspace-seams-20260809T050634Z/workspace-seams.json`: twenty workspace packages (root plus nineteen leaves), 247 Rust files, 204,066 source lines, 136 product-module edges, 40 Cargo workspace dependency edges, the unchanged 12-module product SCC (`brain`, `compress`, `core`, `engine`, `fec`, `implementations`, `interface`, `optimize`, `qftls`, `simd`, `stealth`, `transport`), and `protected_changes=[]`. The qf-memory-pool workspace edges are only its four backend dependencies plus the root-to-leaf edge.
- The move is backend-only and does not require frontend or Tauri fields, API projections, or UI changes. TODO-562 remains blocked until the measured cyclic product core and external Linux/native/CI gates are resolved; no frontend path changed.

## Compression Workspace Leaf (2026-08-09, TODO-562)

- `crates/qf-compress/` now owns the former `src/compress.rs` implementation: compression analysis and policy, zstd pooled and dictionary frames, entropy/chunk heuristics, persona dictionary registry/persistence, and the body pool. The root `quicfuscate::compress` module is a compatibility projection, including the private root wrapper for snapshot policy.
- The child depends on `qf-common`, `qf-cpu`, `qf-memory-pool`, and `qf-telemetry` plus `zstd` and `log`. The SIMD byte classification, histogram, and pattern-search implementations are owned by qf-cpu; qf-compress has no reverse dependency on the product crate.
- Isolated qf-compress all-target/all-feature checking, `17/17` tests, strict all-target/all-feature Clippy, root all-target compatibility checking with `rust-tests`, root strict `rust-tests` Clippy, and formatting pass. The final guarded target is `10,831,616 KiB`, below the `12,582,912 KiB` cleanup threshold; the mandatory initial `cargo clean` remains recorded above.
- The complete workspace all-target `rust-tests` suite passes `3,005` tests with `0` failed and `6` ignored; qf-compress contributes `17/17` and qf-cpu contributes `58/58` after the sort slice. Isolated qf-compress checking, strict Clippy, root compatibility checking, root strict `rust-tests` Clippy, and formatting pass.
- The final warning-free release verification is canonical `--help` with a `10,016,880`-byte binary and SHA-256 `1d24166193651e304bfa580fa338ea4dde8889c834f8721ca95094dbf79ba87b`; the target ended at `10,831,616 KiB`, below the `12,582,912 KiB` cleanup threshold.
- Current seam evidence is `scripts/out/audits/workspace-seams-20260809T082400Z/workspace-seams.json`: twenty-one workspace packages (root plus twenty leaves), 253 Rust files, 204,103 source lines, 134 product-module edges, 45 Cargo workspace dependency edges, and an 11-module product SCC (`brain`, `core`, `engine`, `fec`, `implementations`, `interface`, `optimize`, `qftls`, `simd`, `stealth`, `transport`). qf-compress has only its four backend-leaf dependencies plus the root-to-leaf edge; `protected_changes=[]`.
- This is backend-only. No frontend or Tauri field/API projection is part of the extraction.

## Transport Anti-Replay Workspace Leaf (2026-08-09)

- `crates/qf-transport-anti-replay/` now owns the former `src/transport/anti_replay.rs` strike-register implementation. The root module is a compatibility projection, so server bootstrap, transport configuration, receive handling, and registered anti-replay tests keep their existing `crate::transport::anti_replay` paths.
- The child depends on `qf-common::time_source::ProtocolClock`, `parking_lot`, and `sha2` only. Isolated all-target/all-feature tests pass `11/11`; strict all-target/all-feature Clippy, root compatibility gates, workspace `rust-tests` Clippy, workspace all-feature library/binary Clippy, the complete workspace all-target `rust-tests` suite, and the warning-free release gate pass. Root library tests are `2,154/2,154`; all registered test targets report zero failures.
- Release evidence is `target/release/quicfuscate --help` exit 0, binary size `10,017,024` bytes, SHA-256 `b422f25c19e3fc7ccc9eddbe914682488d4cbb06df2687d69ae16d11d31fe357`, target `5,912,068 KiB`, and free space `7,522,288 KiB`. Seam evidence is `scripts/out/audits/workspace-seams-20260809T065128Z/workspace-seams.json`: twenty-two workspace packages, 254 Rust files, 204,107 source lines, 134 product-module edges, 47 Cargo workspace dependency edges, the unchanged 11-module product SCC, and `protected_changes=[]`.
- The workspace extraction moved CPU dispatch and AMX capability ownership into qf-cpu; `scripts/audits/verify-amx-proof-contract.sh` now checks those canonical child paths. The refreshed runtime guardrail audit is fully green with zero critical failures and zero warnings at `scripts/out/audits/runtime-guardrails-20260809T065928Z/audit-runtime-guardrails.log`; no platform guard was weakened and no frontend/Tauri path changed.
- The complete workspace all-feature/all-target Clippy lane remains platform-blocked at the explicit Linux-only integration guard `scripts/tests/rust/rt-io-hotpath-kernel-integration.rs:4` on macOS ARM64 (exit `101`); the all-feature library/binary Clippy lane passes and no guard was weakened.

## CPU Profile Compatibility Owner (2026-08-09)

- `crates/qf-cpu/src/profile.rs` now owns the former root `src/profile.rs` `Aegis128Profile` implementation. The root `profile` module is a compatibility re-export, preserving the existing public path without a second implementation owner.
- The move stays inside qf-cpu's existing backend boundary and adds no frontend or Tauri surface. Isolated qf-cpu all-target/all-feature tests pass `64/64`; strict all-target/all-feature Clippy, root all-target checking with `rust-tests`, root strict Clippy with `rust-tests`, and the complete workspace all-target `rust-tests` suite exit 0 with root `2,148/2,148` and zero registered-target failures. Formatting, diff hygiene, and the protected-path check pass.
- The guarded workspace test run ends at `8,260,072 KiB` target usage with `4,115,300 KiB` free, below the 12-GiB cleanup threshold.
- Working-tree seam evidence is `scripts/out/audits/workspace-seams-20260809T071500Z/workspace-seams.json`: twenty-two workspace packages, 255 Rust files, 204,112 source lines, 134 product-module edges, 47 Cargo workspace dependency edges, and the unchanged 11-module product SCC with `protected_changes=[]`. The remaining cyclic transport core remains an explicitly coordinated boundary.
- Warning-free release verification for this code state passes: `target/release/quicfuscate --help` exits 0, the binary is `10,017,024` bytes, SHA-256 is `0551220247adb5954ebee8dc5bb48e42c7e802a5d8256e9054a18e9cd3a8226c`, and the target ends at `7,052,724 KiB` with `6,365,800 KiB` free.
- The complete all-feature/all-target workspace Clippy lane remains intentionally platform-blocked at `scripts/tests/rust/rt-transport-uring.rs:8` on macOS ARM64; the all-feature library/binary lane is green and no Linux guard was weakened. The guarded blocked run ended at `8,696,056 KiB` target usage with `3,677,400 KiB` free.

## ASCII Classifier Compatibility Owner (2026-08-09)

- `qf-cpu::count_ascii_printable` now owns the former scalar implementation from `src/accelerate.rs`; the root `accelerate::count_ascii_printable` path is a compatibility re-export and no duplicate implementation remains.
- The move is backend-only and adds no dependency or frontend/Tauri surface. Isolated qf-cpu all-target/all-feature tests pass `66/66`, the root `rt-stealth-ascii-count` target passes `2/2`, and root all-target checking plus strict `rust-tests` Clippy pass.
- Working-tree seam evidence is `scripts/out/audits/workspace-seams-20260809T073000Z/workspace-seams.json`: twenty-two workspace packages, 255 Rust files, 204,122 source lines, 134 product-module edges, 47 Cargo workspace dependency edges, and the unchanged 11-module product SCC with `protected_changes=[]`.
- The complete workspace all-target `rust-tests` suite exits 0 with root `2,148/2,148` and zero registered-target failures; the guarded run ends at `8,973,544 KiB` target usage with `4,355,628 KiB` free.
- Warning-free release verification for the combined profile and ASCII-owner state passes: `target/release/quicfuscate --help` exits 0, the binary is `10,017,024` bytes, SHA-256 is `0d43551fbf1de91721fa93d335d515456613ce77218ce9b3d59b69b2799232e4`, and the target ends at `8,973,568 KiB` with `4,312,636 KiB` free.
- The refreshed runtime guardrail audit is fully green with zero critical failures and zero warnings at `scripts/out/audits/runtime-guardrails-20260809T074500Z/audit-runtime-guardrails.log`; no platform guard was weakened and no frontend/Tauri path changed.

## Transport Version Workspace Leaf (2026-08-09, TODO-562)

- `crates/qf-transport-version/` now owns QUIC v1/v2 constants and support selection, Version Information transport-parameter encoding/decoding, downgrade-safe `VersionNegotiationState`, GREASE/reserved-version generation, and long-header type-bit mapping. The root `src/transport/version.rs` is a compatibility adapter that translates the child `LongHeaderPacketType` into the established root `PacketType` enum; no packet, connection, or frontend API was changed.
- The child depends only on `qf-common` for secure GREASE entropy and `qf-error` for the shared connection-error contract. Its private scalar varint codec is limited to the low-frequency Version Information parameter path; the transport packet-number and H3 hotpaths retain the existing SIMD-dispatched root codec.
- Isolated qf-transport-version all-target/all-feature tests pass `5/5`; strict all-target/all-feature Clippy passes. Workspace all-target checking with `rust-tests`, workspace strict `rust-tests` Clippy, workspace all-feature library/binary Clippy, the complete workspace all-target `rust-tests` suite, formatting, diff hygiene, and the warning-free release gate pass. The complete all-feature/all-target Clippy lane remains platform-blocked at the repository-owned Linux-only guard `scripts/tests/rust/rt-io-hotpath-kernel-integration.rs:4` on macOS ARM64; no guard was weakened.
- Release evidence is `target/release/quicfuscate --help` exit 0, binary size `10,017,104` bytes, SHA-256 `93b4d4ae832af30c57788d196b99f93bfed772ce64b3571faf764f4113f30bca`, and target usage `1,424,012 KiB` after the guarded cleanup cycle. The seam report is `scripts/out/audits/workspace-seams-20260809T074026Z/workspace-seams.json`: twenty-three workspace packages, 256 Rust files, 204,247 source lines, 134 product-module edges, 50 Cargo workspace dependency edges, the unchanged 11-module product SCC, and `protected_changes=[]`.
- The refreshed runtime guardrail audit is fully green with zero critical failures and zero warnings at `scripts/out/audits/runtime-guardrails-20260809T074519Z/audit-runtime-guardrails.log`. This backend slice has no frontend or Tauri field/API projection; the protected frontend paths remain untouched.

## Transport Congestion-Control Workspace Leaf (2026-08-09, TODO-562)

- `crates/qf-transport-cc/` now owns the former `src/transport/cc/` implementation: Reno, CUBIC, BBR2, BBR3, the `CongestionController` contract, enum dispatch, browser-profile stealth shaping, and all 92 existing controller tests. The root `src/transport/cc/mod.rs` is a compatibility projection, so `Recovery` and existing `quicfuscate::transport::cc` callers retain their paths.
- The child now also owns the root-independent `Algorithm` serde/default contract used by engine configuration; `src/engine/config.rs` removes the duplicate `CcAlgorithm` enum and re-exports `qf_transport_cc::cc::Algorithm` as `engine::CcAlgorithm`. Its backend boundary is `qf-common`, `qf-transport-types`, `serde`, and `log`; the `rust-tests` feature forwards the qf-common test entropy hook. No frontend, Tauri, or visible API field projection is required.
- Isolated qf-transport-cc all-target/all-feature checking, `92/92` tests, and strict all-target/all-feature Clippy pass. Workspace all-target checking, workspace strict `rust-tests` Clippy, full workspace all-target `rust-tests` (exit 0, qf-transport-cc `92/92`, root `2051/2051`), workspace all-feature library/binary Clippy, formatting, diff hygiene, and runtime guardrails pass. Full all-feature/all-target Clippy remains platform-blocked at `scripts/tests/rust/rt-io-hotpath-kernel-integration.rs:4` on macOS ARM64; no guard was weakened.
- Current seam evidence is `scripts/out/audits/workspace-seams-20260809T101650/workspace-seams.json`: 24 workspace packages, 258 Rust files, 204,266 source lines, 134 module edges, 52 Cargo workspace dependency edges, and the unchanged 11-module product SCC. The only qf-transport-cc workspace dependencies are `qf-transport-cc -> qf-common` and `quicfuscate -> qf-transport-cc`; `protected_changes=[]`.
- Runtime guardrail evidence is `scripts/out/audits/runtime-guardrails-20260809T101650/audit-runtime-guardrails.log` with Critical 0 and Warnings 0. Warning-free release verification passes with `target/release/quicfuscate --help` exit 0, a 10,017,344-byte binary, SHA-256 `92f290a44b67e4f227e58e1c391cdda1834d1586b58e166391647371bd151fe3`, and current target usage of 1,238,596 KiB, below the 12-GiB cleanup threshold. Frontend/Tauri paths remain untouched.
- Fresh seam evidence after the configuration-owner cut is `scripts/out/audits/workspace-seams-20260809T-cc-config/workspace-seams.json`: `35` packages, `307` Rust files, `205,437` source lines, `129` module edges, `94` Cargo workspace dependency edges, unchanged 9-module product SCC, and `protected_changes=[]`; qf-transport-cc tests remain `92/92`, root EngineConfig tests are `39/39`, and target usage is `11,159,236 KiB` with `6,981,680 KiB` free. No frontend/Tauri path changed.

## Transport Path Workspace Leaf (2026-08-09, TODO-562)

- `crates/qf-transport-path/` now owns the former `src/transport/path.rs` and `src/transport/path_scheduler.rs` implementations: `PathManager`, `PathState`, path scoring, validation-aware selection, and round-robin, lowest-latency, and weighted-proportional scheduling, with all 27 existing path tests. The root modules remain compatibility projections, so existing `quicfuscate::transport::path` and `quicfuscate::transport::path_scheduler` paths retain their APIs.
- The child depends only on `qf-common` for the protocol clock and `qf-transport-cc` for the congestion-controller trait and Reno test fixture. Its `rust-tests` feature forwards the test hooks of both backend leaves. No frontend, Tauri, or visible API field projection is required.
- Isolated qf-transport-path all-target/all-feature checking, `27/27` tests, strict all-target/all-feature Clippy, root compatibility checking, root strict `rust-tests` Clippy, formatting, and diff hygiene pass. The complete workspace all-target `rust-tests` suite exits 0 and the all-feature library/binary Clippy lane passes; qf-transport-path contributes `27/27` tests. Full all-feature/all-target Clippy remains platform-blocked at `scripts/tests/rust/rt-io-hotpath-kernel-integration.rs:4` on macOS ARM64; no guard was weakened.
- Warning-free release verification passes: `target/release/quicfuscate --help` exits 0, the binary is `10,017,344` bytes, SHA-256 `58846405405c870c076b0001fcef6a1a4bb0271d3e83fac3a8b1d36c3fb5b7a9`, and current target usage is `7,120,912 KiB`, below the 12-GiB cleanup threshold.
- Current seam evidence is `scripts/out/audits/workspace-seams-20260809T103120/workspace-seams.json`: 25 workspace packages, 261 Rust files, 204,279 source lines, 134 module edges, 55 Cargo workspace dependency edges, and the unchanged 11-module product SCC. qf-transport-path depends only on `qf-common` and `qf-transport-cc`; `quicfuscate -> qf-transport-path`; `protected_changes=[]`.
- Runtime guardrail evidence is `scripts/out/audits/runtime-guardrails-20260809T103120/audit-runtime-guardrails.log` with Critical 0 and Warnings 0. Frontend/Tauri paths remain untouched.

## Transport NAT Workspace Leaf (2026-08-09, TODO-562)

- `crates/qf-transport-nat/` now owns the former NAT traversal implementation from `src/transport/nat.rs`, the engine-facing `NatTraversalSection`, and the `NatTraversalMode`, `NatDiscoveryReason`, and `NatTraversalConfig` contracts formerly embedded in `src/transport/config.rs`: STUN/TURN message codecs, ICE candidate selection, bounded NAT path discovery, serialized section validation/conversion, and the baseline NAT tests. Root `engine::NatTraversalSection`, `transport::config`, and `transport::nat` remain compatibility projections, preserving existing public paths.
- The child depends only on `qf-common` for the protocol clock, `qf-error` for address-parse errors, and direct `rand`, `serde`, `tokio`, and `log` runtime dependencies. No frontend, Tauri, or visible API field projection is required.
- The earlier NAT runtime slice passed qf-transport-nat `35/35` tests, workspace all-target checking with `rust-tests`, workspace strict `rust-tests` Clippy, the all-feature library/binary Clippy lane, and the complete workspace all-target `rust-tests` suite; the suite recorded qf-transport-cc `92/92`, qf-transport-nat `35/35`, qf-transport-path `27/27`, and root `1,989/1,989` with no failures. Full all-feature/all-target Clippy remained intentionally platform-blocked at `scripts/tests/rust/rt-io-hotpath-kernel-integration.rs:4` on macOS ARM64; no guard was weakened.
- Warning-free release verification passes: `target/release/quicfuscate --help` exits 0, the binary is `10,017,488` bytes, SHA-256 is `f4b85b8f51e1079e1bc03176249a22ce421a9a7aa51dada6952ad6dd64094f73`, and the target ends at `6,477,840 KiB` with `4,959,620 KiB` free. The mandatory initial `cargo clean` and the second cleanup after isolated NAT Clippy crossed the local 2-GiB free-space floor were completed before this final verification; the target stayed below the 12-GiB cleanup threshold.
- Current seam evidence is `scripts/out/audits/workspace-seams-20260809T105633/workspace-seams.json`: 26 workspace packages, 264 Rust files, 204,296 source lines, 134 module edges, 58 Cargo workspace dependency edges, the unchanged 11-module product SCC (`brain`, `core`, `engine`, `fec`, `implementations`, `interface`, `optimize`, `qftls`, `simd`, `stealth`, `transport`), and `protected_changes=[]`. qf-transport-nat depends only on `qf-common` and `qf-error`; `quicfuscate -> qf-transport-nat` is the only product-to-leaf edge.
- The configuration-owner follow-up passes qf-transport-nat `38/38` tests, root all-target `rust-tests` checking, strict root `rust-tests` Clippy, and EngineConfig tests `39/39`; no frontend or Tauri path changed.
- Follow-up seam evidence is `scripts/out/audits/workspace-seams-20260809T-nat-config/workspace-seams.json`: 35 workspace packages, 307 Rust files, 205,394 source lines, 128 module edges, 94 Cargo workspace dependency edges, the unchanged 9-module product SCC (`brain`, `core`, `engine`, `fec`, `implementations`, `interface`, `qftls`, `stealth`, `transport`), and `protected_changes=[]`.
- Runtime guardrail evidence is `scripts/out/audits/runtime-guardrails-20260809T105633/audit-runtime-guardrails.log` with Critical 0 and Warnings 0. No frontend or Tauri path changed, and no frontend field/API projection is required. TODO-562 remains blocked for the coordinated cyclic product-core split and external Linux/native/CI acceptance gates.

## Transport Packet-Number Workspace Leaf (2026-08-09, TODO-562)

- `crates/qf-transport-pn/` now owns the packet-number implementation formerly embedded in `src/transport/pn.rs`: packet-number spaces and ACK scheduling, connection-ID sets, adaptive ACK ranges, stream reassembly buffers, and transport random helpers. The root `transport::pn` module remains a compatibility projection. The SIMD-dispatched varint codec stays root-owned because it depends on the root SIMD transport backend; no scalar fallback or duplicate codec was introduced.
- The child depends only on `qf-common` for protocol time and secure entropy and `qf-transport-types` for `ConnectionId`. Isolated all-target/all-feature checking, `23/23` tests, and strict all-target/all-feature Clippy pass. Root library compatibility checking passes after exposing only the cross-crate `PktNumSpace::MAX_PACKET_NUMBER` contract required by existing connection code. The complete workspace all-target `rust-tests` suite exits 0 with qf-transport-pn `23/23`, root `1,966/1,966`, and no failure markers across the remaining registered targets. The post-test target reached `9,522,700 KiB`; the mandatory cleanup then removed `10.2 GiB` before the next gate.
- The all-feature/all-target Clippy lane reaches the repository-owned Linux-only guard `scripts/tests/rust/rt-io-hotpath-kernel-integration.rs:4` and exits 101 on this macOS ARM64 host; no guard was weakened. The all-feature library/binary Clippy lane remains green.
- Warning-free release verification passes: `target/release/quicfuscate --help` exits 0, the binary is `10,034,256` bytes, SHA-256 is `bb3c71e359016dd592d5b1bc3dbdf9257767fe6a204c44a3a83207c6dd6c18ae`, and current target usage is `1,486,848 KiB`, below the `12,582,912 KiB` cleanup threshold. The final release build was run after the post-suite `cargo clean` removed `10.2 GiB`.
- Fresh seam evidence is `scripts/out/audits/workspace-seams-20260809T111927/workspace-seams.json`: 27 workspace packages, 265 Rust files, 204,304 source lines, 134 module edges, 61 Cargo workspace dependency edges, and the unchanged 11-module product SCC (`brain`, `core`, `engine`, `fec`, `implementations`, `interface`, `optimize`, `qftls`, `simd`, `stealth`, `transport`). qf-transport-pn depends only on `qf-common` and `qf-transport-types`; `quicfuscate -> qf-transport-pn`; `protected_changes=[]`.
- The refreshed runtime guardrail audit is fully green with `Critical: 0` and `Warnings: 0` at `scripts/out/audits/runtime-guardrails-20260809T111927/audit-runtime-guardrails.log`. The final protected-path check is empty, so no frontend or Tauri path changed and no frontend field/API projection is required.

## Transport Recovery Workspace Leaf (2026-08-09, TODO-562)

- `crates/qf-transport-recovery/` now owns the RFC 9002 recovery implementation formerly embedded in `src/transport/recovery.rs`: packet-number-space tracking, bounded sent-packet retention, ACK/loss accounting, PTO and time-threshold timers, persistent-congestion evidence, path-migration recovery, and the migration policy contract formerly defined in `src/transport/config.rs`. The root `transport::recovery` and `transport::config` modules remain compatibility projections.
- The child depends only on `qf-common`, `qf-error`, `qf-memory-pool`, `qf-telemetry`, and `qf-transport-cc`. The existing `with_memory_pool` constructor remains source-compatible while the recovery state no longer stores an unused pool reference, eliminating the reverse dependency on the root `optimize` namespace. No FEC, crypto, SIMD, or frontend behavior was changed.
- Isolated qf-transport-recovery all-target/all-feature tests pass `37/37`; strict all-target/all-feature Clippy passes. Root library compatibility checking, workspace all-target checking with `rust-tests`, workspace strict `rust-tests` Clippy, and the complete workspace all-target `rust-tests` suite pass; the suite records qf-transport-recovery `37/37`, root `1,929/1,929`, and zero registered-target failures. The all-feature library/binary Clippy lane is green.
- The complete all-feature/all-target Clippy lane remains intentionally platform-blocked at the repository-owned Linux-only guard `scripts/tests/rust/rt-transport-uring.rs:8` on macOS ARM64 (exit 101); no guard was weakened.
- Warning-free release verification passes: `target/release/quicfuscate --help` exits 0, the binary is `10,034,560` bytes, SHA-256 is `b93263bce10fe3f2576fd054597f01ecba7a60f49f6bd5da37f8e8e31c887b53`, and current target usage is `7,494,480 KiB`, below the `12,582,912 KiB` cleanup threshold. Free space is `15,848,064 KiB`.
- Fresh seam evidence is `scripts/out/audits/workspace-seams-20260809T113613/workspace-seams.json`: 28 workspace packages, 266 Rust files, 204,303 source lines, 133 module edges, 67 Cargo workspace dependency edges, and the unchanged 11-module product SCC (`brain`, `core`, `engine`, `fec`, `implementations`, `interface`, `optimize`, `qftls`, `simd`, `stealth`, `transport`). qf-transport-recovery depends only on the five backend leaves above; `quicfuscate -> qf-transport-recovery`; `protected_changes=[]`.
- Runtime guardrail evidence is fully green with `Critical: 0` and `Warnings: 0` at `scripts/out/audits/runtime-guardrails-20260809T113613/audit-runtime-guardrails.log`. The final protected-path check is empty, so no frontend or Tauri path changed and no frontend field/API projection is required.

## CPU Transport Acceleration Workspace Leaf (2026-08-09, TODO-562)

- `crates/qf-cpu/src/transport.rs` now owns the transport acceleration implementation formerly embedded in `src/optimize/transport.rs`: congestion sample aggregation with VNNI/AVX2/NEON dispatch, bounded bitmap range updates, ECN mark counting, and packet-number reconstruction with scalar parity fallbacks. The root `src/optimize/transport.rs` is a compatibility projection, so `ConnectionStats` and packet parsing retain their established paths.
- The qf-cpu boundary now includes the dependency-free `qf-transport-types` statistics contract. No root transport, FEC, stealth, crypto, or frontend/Tauri implementation is imported by the child; the runtime guardrail audit was updated to follow the canonical child owner path.
- Isolated qf-cpu all-target/all-feature checking, `83/83` tests, and strict all-target/all-feature Clippy pass. Root library compatibility checking, workspace all-target checking with `rust-tests`, and workspace strict `rust-tests` Clippy pass. The complete workspace all-target `rust-tests` matrix exits 0 with `3,005` passed, `0` failed, and `6` ignored; qf-cpu contributes `83/83` and root contributes `1,912/1,912`.
- The all-feature library/binary Clippy lane passes with `RUSTFLAGS=-Dwarnings`. The complete all-feature/all-target Clippy lane remains intentionally platform-blocked at the repository-owned Linux-only guard `scripts/tests/rust/rt-io-hotpath-kernel-integration.rs:4` on macOS ARM64 (exit `101`); no guard was weakened.
- Warning-free release verification passes: `target/release/quicfuscate --help` exits 0, the binary is `10,034,544` bytes, SHA-256 is `8347155450a28671ceda3c8162fbac5a8d6b9eec8df2961e876fbdebe7698e7a`, and the final observed target usage is `10,988,636 KiB` with `13,233,988 KiB` free. The target remains below the `12,582,912 KiB` cleanup threshold.
- Fresh seam evidence is `scripts/out/audits/workspace-seams-20260809T1148/workspace-seams.json`: 28 workspace packages, 267 Rust files, 204,311 source lines, 133 module edges, 68 Cargo workspace dependency edges, and the unchanged 11-module product SCC (`brain`, `core`, `engine`, `fec`, `implementations`, `interface`, `optimize`, `qftls`, `simd`, `stealth`, `transport`). qf-cpu depends on `qf-common`, `qf-telemetry`, and `qf-transport-types`; `quicfuscate -> qf-cpu`; `protected_changes=[]`.
- Runtime guardrail evidence is fully green with `Critical: 0` and `Warnings: 0` at `scripts/out/audits/runtime-guardrails-20260809T1215/audit-runtime-guardrails.log`. No frontend or Tauri path changed, and no frontend field/API projection is required.

## Transport Frames Workspace Leaf (2026-08-09, TODO-562)

- `crates/qf-transport-frames/` now owns the former `src/transport/frames.rs` codec: checked frame wire-length admission, serialization, zero-copy parsing, ACK-range canonicalization, packet-space legality, RFC 9221 DATAGRAM forms, no-LEN STREAM boundaries, and the 21 moved unit tests. `crates/qf-transport-types::PacketType` supplies the leaf's packet-space contract.
- The root `transport::frames` module is a compatibility adapter. It injects the existing root SIMD varint codec, x86 ACK canonicalizer, and ARM STREAM-header parser through narrow traits, so the optimized runtime path is preserved without duplicate frame logic. Its `Arc<MemoryPool>` batch parameter remains source-compatible and allocation-free.
- Isolated qf-transport-frames checking, `21/21` tests, and strict all-target/all-feature Clippy pass. Root library checking, workspace all-target checking with `rust-tests`, workspace strict `rust-tests` Clippy, and the complete workspace all-target test command exit 0. The all-feature library/binary Clippy lane passes with `RUSTFLAGS=-Dwarnings`.
- The complete all-feature/all-target Clippy lane remains intentionally platform-blocked at the repository-owned Linux-only guard `scripts/tests/rust/rt-transport-uring.rs:8` on macOS ARM64; no guard was weakened. The target guard triggered `cargo clean` at `13,200,356 KiB` and removed `14.2 GiB`; the post-clean target was `941,468 KiB`.
- Fresh seam evidence is `scripts/out/audits/workspace-seams-20260809T101237Z/workspace-seams.json`: 29 workspace packages, 268 Rust files, 204,593 source lines, 133 module edges, 71 Cargo workspace dependency edges, and the unchanged 11-module product SCC (`brain`, `core`, `engine`, `fec`, `implementations`, `interface`, `optimize`, `qftls`, `simd`, `stealth`, `transport`). `protected_changes=[]`.
- No frontend or Tauri path changed, and no frontend field/API projection is required. TODO-562 remains blocked for the coordinated cyclic product-core split and external Linux/native/CI acceptance gates.

## TLS Cover Cipher Contract Workspace Ownership (2026-08-10, TODO-562)

- `crates/qf-stealth/src/tls_cover.rs` now canonically owns `TlsCoverCipherSuite` and its stable `as_str` and TLS-ID mapping. `src/stealth/parts/tls_cover_provider.rs` retains only environment preference parsing and provider/runtime crypto installation, while `src/stealth/mod.rs` reexports the child type for historical root callers and tests.
- The child remains root-independent and does not own rustls, transport packet crypto, FEC, engine, implementation, frontend, or Tauri behavior. The move changes no wire behavior and requires no frontend field or API projection.
- qf-stealth tests pass `99/99` with strict all-target/all-feature Clippy; root Stealth tests pass `190/190`. The workspace all-target `rust-tests`, strict workspace Clippy, all-feature workspace check, formatting, and diff hygiene pass. The complete all-feature/all-target lanes remain bounded by the unchanged Linux-only guards on macOS ARM64.
- TODO-562 remains open for the measured 9-module product SCC and external Linux/native/CI acceptance gates. Frontend and Tauri paths remain untouched.

## Audit Test Isolation Cleanup (2026-08-10, TODO-562)

- qf-audit tests now use unique `audit_test_path` names plus `remove_audit_set` cleanup for active files, checkpoints, and rotated segments. Fixed process-ID paths were the source of stale-checkpoint `ENOENT` failures under interrupted or parallel test runs.
- qf-audit tests pass `42/42` with strict all-target/all-feature Clippy, and the full workspace all-target `rust-tests` command exits `0` after the fix. No production audit behavior changed and no frontend or Tauri path is involved.

## Individual Memory-Lock Status Contract Workspace Ownership (2026-08-10, TODO-562)

- `crates/qf-memory-lock/src/lib.rs` now canonically owns `MemoryLockAllocationStatus`, including stable diagnostics for disabled, individually locked, process-covered, and unavailable sensitive allocations. `src/qftls.rs` retains `qftls::TlsKeyLockStatus` as a compatibility alias, so preloaded TLS identity callers and status matching remain source-compatible.
- The move changes no lock/unlock behavior, TLS identity publication, zeroization, or startup policy. The child remains root-independent and has no frontend or Tauri surface.
- qf-memory-lock tests pass `12/12` with strict all-target/all-feature Clippy; root qftls tests pass `24/24` and strict root library Clippy. No frontend field or API projection is required.
- Fresh seam evidence is `scripts/out/audits/workspace-seams-20260810T-memory-status-final/workspace-seams.json`: revision `d69e10446c92be684346387d613c28340f18b25f`, `36` workspace packages, `322` Rust files, `206,306` source lines, `125` module edges, `106` workspace dependency edges, the unchanged 9-module product SCC, and `protected_changes=[]`. Runtime guardrails are `Critical: 0` and `Warnings: 0` at `scripts/out/audits/runtime-guardrails-20260810T-memory-status-final/audit-runtime-guardrails.log`.
- The full workspace all-target `rust-tests`, workspace rust-tests Clippy, all-feature workspace check, all-feature library/binary Clippy, formatting, documentation truth, and diff hygiene pass. All-feature/all-target check and Clippy stop only at the unchanged macOS-inapplicable Linux guards `scripts/tests/rust/rt-io-hotpath-kernel-integration.rs:4` and `scripts/tests/rust/rt-transport-uring.rs:8`. Post-gate target usage is `12,068,088 KiB`, below the `12,582,912 KiB` cleanup threshold; the 2-GiB free-space floor remained respected.
- `bash scripts/tests/audits/verify-audit-completeness.sh` fails closed on the current non-ignored untracked Claude extraction paths, including qf-* manifests/sources and root compatibility files. Those paths were preserved exactly as found and were not staged, reverted, deleted, or hidden; this is a worktree-governance blocker, not a frontend or runtime failure.

## Backend Continuation Verification (2026-08-10)

- The locally actionable backend continuation is complete. `qf-engine-types` now contains the remaining root-independent engine configuration, QKey, lifecycle/statistics, and structured command/result contracts; root engine modules retain concrete orchestration, aggregate configuration, QKey encoding/registry, and compatibility paths. `qf-crypto` rejects counters above QUIC's 62-bit limit at every AEAD trait boundary, and the property suite uses the valid QUIC range.
- Focused proof is green: qf-engine-types `15/15`, qf-crypto `137/137`, AEAD property suite `12/12`, root engine/config/QKey `77/77`; full workspace all-target `rust-tests` is `119` result blocks with `3,093` passed, `0` failed, and `6` ignored. Workspace strict check/Clippy, all-feature lib/bin check/Clippy/tests, formatting, shell syntax, and diff hygiene pass.
- The all-feature/all-target check and Clippy lanes remain blocked only by the unchanged macOS-inapplicable Linux guards at `scripts/tests/rust/rt-io-hotpath-kernel-integration.rs:4` and `scripts/tests/rust/rt-transport-uring.rs:8`. Runtime guardrails are green with `Critical: 0` and `Warnings: 0` at `scripts/out/audits/runtime-guardrails-20260810T-backend-final-docs/audit-runtime-guardrails.log`.
- Seam evidence at `scripts/out/audits/workspace-seams-20260810T-backend-final/workspace-seams.json` records revision `d69e10446c92be684346387d613c28340f18b25f`, `36` packages, `322` Rust files, `206,258` source lines, `125` module edges, `106` workspace dependency edges, the unchanged 9-module product SCC, and `protected_changes=[]`. Release `--help` passes with binary size `9,991,520` bytes and SHA-256 `72a4a685da7ec419e63392574ab7e8e803a376d1c6db66a8065617a58ba881e8`.
- The target guard was applied to every Cargo build/test/check/Clippy command, triggered `cargo clean` after `12,638,284 KiB` target usage, removed `14.2 GiB`, and ended below the `12,582,912 KiB` threshold. Frontend/Tauri paths remained untouched. The measured 9-module SCC and external Linux/native/CI acceptance gates remain the explicit blockers; this slice does not claim to dissolve them.

## Stealth Profile Slot Parser Workspace Ownership (2026-08-10, TODO-562)

- `crates/qf-stealth/src/profiles.rs` now canonically owns the pure `browser[@os]` fingerprint rotation-slot grammar, including trimming, default-OS inheritance, legacy-separator rejection, and bounded error reporting. The parser symbol in `src/stealth/parts/browser_profiles.rs` is only a root compatibility re-export; the TLS-Cover-coupled `FingerprintProfile` remains root-local and no duplicate parser remains.
- qf-stealth all-target/all-feature checking, strict all-target/all-feature Clippy, and tests pass `89/89`. Root all-target `rust-tests` checking, strict root `rust-tests` Clippy, and the root Stealth filter pass `190/190`. The complete workspace all-target `rust-tests` run has `118` result blocks with `3,051` passed, `0` failed, and `6` ignored.
- Fresh seam evidence is `scripts/out/audits/workspace-seams-20260810T-stealth-profile-slot-final-2/workspace-seams.json`: source revision `d69e10446c92be684346387d613c28340f18b25f`, `35` workspace packages, `311` Rust files, `205,552` source lines, `128` module edges, `94` Cargo workspace dependency edges, the unchanged 9-module product SCC (`brain`, `core`, `engine`, `fec`, `implementations`, `interface`, `qftls`, `stealth`, `transport`), and `protected_changes=[]`.
- Runtime guardrails are green with `Critical: 0` and `Warnings: 0` at `scripts/out/audits/runtime-guardrails-20260810T-stealth-profile-slot-final-2/audit-runtime-guardrails.log`. The all-feature/all-target check remains explicitly platform-blocked on macOS ARM64 by `scripts/tests/rust/rt-io-hotpath-kernel-integration.rs:4` and `scripts/tests/rust/rt-transport-uring.rs:8`, both Linux-only guards; no platform guard was weakened.
- Warning-free release verification passes with `RUSTFLAGS=-Dwarnings cargo build --release --bin quicfuscate --locked --offline`; `target/release/quicfuscate --help` exits `0`, the binary is `9,973,232` bytes with SHA-256 `ad050ac8e92f6e05c10d992ffc94ac7697834c081538465a407b6d16421f3c25`, and the post-build target guard records `8,567,272 KiB`, below the `12,582,912 KiB` cleanup threshold and above the `2,097,152 KiB` build floor.
- No frontend or Tauri path changed, and no frontend field/API projection is required. TODO-562 remains blocked for the coordinated cyclic product-core split and external Linux/native/CI acceptance gates.

## Stealth Traffic-State Workspace Ownership (2026-08-10, TODO-562)

- `crates/qf-stealth/src/traffic.rs` now canonically owns the root-independent `RateChoker`, `ServerPushState`, and `ServerPushTriggerReason` contracts. The root stealth module re-exports those types for historical paths, while `StealthManager` uses the child constructors and bounded burst-state methods; no duplicate traffic-state implementation remains.
- The child depends only on `qf-common::time_source::ProtocolClock` and the standard library. Token-bucket pacing, saturating cover-byte accounting, and the bounded one-minute burst window stay independent of transport, FEC, Reality, engine, implementation, frontend, and Tauri code.
- qf-stealth all-target/all-feature tests pass `91/91` with strict all-target/all-feature Clippy. Root library checking with `rust-tests`, the focused root Stealth filter, and strict root library Clippy pass; the focused root Stealth filter passes `190/190`. The complete workspace all-target `rust-tests` run exits `0` with `118` result blocks, `3,053` passed, `0` failed, and `6` ignored.
- Fresh seam evidence is `scripts/out/audits/workspace-seams-20260810T-stealth-traffic-final/workspace-seams.json`: source revision `d69e10446c92be684346387d613c28340f18b25f`, `35` workspace packages, `312` Rust files, `205,631` source lines, `128` module edges, `94` Cargo workspace dependency edges, the unchanged 9-module product SCC, and `protected_changes=[]`. Runtime guardrails are green with `Critical: 0` and `Warnings: 0` at `scripts/out/audits/runtime-guardrails-20260810T-stealth-traffic-final/audit-runtime-guardrails.log`.
- The all-feature/all-target check remains platform-blocked on macOS ARM64 by the repository-owned Linux-only guards at `scripts/tests/rust/rt-io-hotpath-kernel-integration.rs:4` and `scripts/tests/rust/rt-transport-uring.rs:8`; no guard was weakened. Warning-free release verification passes with `--help` exit `0`, a `9,973,504`-byte `quicfuscate` binary, SHA-256 `c0591efd975cd9d8f02a9fc32569e5acc133d927ddf2dc3d0fa3b9b175f13d1d`, and target usage `5,675,964 KiB` with `19,291,572 KiB` free, below the `12,582,912 KiB` cleanup threshold. No frontend or Tauri path changed, and no frontend field/API projection is required.

## Stealth TLS Profile Workspace Ownership (2026-08-10, TODO-562)

- `crates/qf-stealth/src/tls_profile.rs` canonically owns the browser-shaped `TlsProfile` contract and all six supported persona constructors, including AES-GCM cipher policy, ALPN ordering, extension ordering, ECH/GREASE defaults, cosmetic jitter, and derived Edge/Opera/Brave profiles. The root `qftls` module re-exports the child type, so existing `crate::qftls::TlsProfile` callers retain their paths without a duplicate implementation.
- The child profile module uses only the existing qf-stealth `rand` and standard-library surface. `profile_from_fingerprint` remains root-local because `FingerprintProfile` is coupled to TLS Cover and transport metadata; the rustls provider, `QuicTlsProvider`, `Level`, CryptoContext, and all transport-facing lifecycle remain root-owned.
- qf-stealth all-target/all-feature tests pass `95/95` with strict all-target/all-feature Clippy. The root qftls filter passes `24/24`, the focused root Stealth filter passes `190/190`, root library checking and strict root library Clippy pass, and the complete workspace all-target `rust-tests` run exits `0` with `118` result blocks, `3,057` passed, `0` failed, and `6` ignored.
- Fresh seam evidence is `scripts/out/audits/workspace-seams-20260810T-stealth-tls-profile-final/workspace-seams.json`: source revision `d69e10446c92be684346387d613c28340f18b25f`, `35` workspace packages, `313` Rust files, `205,603` source lines, `128` module edges, `94` workspace dependency edges, unchanged 9-module product SCC, and `protected_changes=[]`. Runtime guardrails are green with `Critical: 0` and `Warnings: 0` at `scripts/out/audits/runtime-guardrails-20260810T-stealth-tls-profile-final/audit-runtime-guardrails.log`.
- The all-feature/all-target check remains platform-blocked on macOS ARM64 by the repository-owned Linux-only guard `scripts/tests/rust/rt-transport-uring.rs:8`; no guard was weakened. Warning-free release verification passes with `--help` exit `0`, a `9,973,504`-byte `quicfuscate` binary, SHA-256 `c0591efd975cd9d8f02a9fc32569e5acc133d927ddf2dc3d0fa3b9b175f13d1d`, and target usage `6,379,064 KiB` with `18,449,696 KiB` free, below the `12,582,912 KiB` cleanup threshold. No frontend or Tauri path changed, and no frontend field/API projection is required.

## Stealth TLS Cover Builder Workspace Ownership (2026-08-10, TODO-562)


- `crates/qf-stealth/src/tls_cover.rs` now canonically owns the root-independent synthetic TLS record and ClientHello extension builder, including its moved regression tests. `src/stealth/tls_cover.rs` is the compatibility projection for the historical root path; the root `TlsCoverProvider` retains only crypto-context installation, entropy/key lifecycle, handshake state, and transport-facing provider behavior.
- The child builder depends on existing `BrowserProfile`, `OsProfile`, and `qf-common::env_utils::EnvSnapshot` contracts. The ultra-mode flag is read from the supplied environment snapshot, so no provider or transport dependency crosses into qf-stealth and no frontend or Tauri behavior is involved.
- qf-stealth all-target/all-feature checking, strict all-target/all-feature Clippy, and tests pass `85/85`. Root all-target `rust-tests` checking, strict root `rust-tests` Clippy, and the root Stealth filter pass `190/190`. The complete workspace all-target `rust-tests` command exits `0` with no test failures.
- Fresh seam evidence is `scripts/out/audits/workspace-seams-20260810T-stealth-tls-cover-final-2/workspace-seams.json`: source revision `d69e10446c92be684346387d613c28340f18b25f`, `35` workspace packages, `311` Rust files, `205,524` source lines, `128` module edges, `94` Cargo workspace dependency edges, the unchanged 9-module product SCC (`brain`, `core`, `engine`, `fec`, `implementations`, `interface`, `qftls`, `stealth`, `transport`), and `protected_changes=[]`.
- Runtime guardrails are green with `Critical: 0` and `Warnings: 0` at `scripts/out/audits/runtime-guardrails-20260810T-stealth-tls-cover-final-2/audit-runtime-guardrails.log`. The all-feature/all-target check remains explicitly platform-blocked on macOS ARM64 by `scripts/tests/rust/rt-io-hotpath-kernel-integration.rs:4`, which requires Linux; no platform guard was weakened.
- Warning-free release verification passes with `RUSTFLAGS=-Dwarnings cargo build --release --bin quicfuscate --locked --offline`; `target/release/quicfuscate --help` exits `0`, the binary is `9,973,184` bytes with SHA-256 `7bef1cd1c79e42579849b237a1970c16eb02d3ff69eefae710aa46eccfd1e8fd`, and the post-build target guard records `7,286,392 KiB`, below the `12,582,912 KiB` cleanup threshold and above the `2,097,152 KiB` build floor.
- No frontend or Tauri path changed, and no frontend field/API projection is required. TODO-562 remains blocked for the coordinated cyclic product-core split and external Linux/native/CI acceptance gates.

## Intelligent Level Hints Workspace Ownership (2026-08-10, TODO-562)

- `crates/qf-transport-types/src/runtime.rs` canonically owns the connection-local `IntelligentLevelHints` atomic contract: bounded Brain and probe escalation levels, MASQUE preference, effective-level selection, and test injection. `src/brain.rs` keeps the historical `crate::brain::IntelligentLevelHints` path as a compatibility re-export, while StealthManager and escalation state consume the leaf directly.
- The moved contract uses only `std` atomics inside the existing qf-transport-types serialization boundary. It introduces no product dependency and no frontend or Tauri surface. The direct Stealth-to-Brain source edge is removed; Brain and Stealth retain their existing runtime behavior and connection-local isolation.
- qf-transport-types all-target/all-feature tests pass `15/15` with strict Clippy. Root library checking, root strict Clippy, the focused root Stealth filter `190/190`, workspace strict `rust-tests` Clippy, workspace all-feature library/binary Clippy, and the complete workspace all-target `rust-tests` run pass; the complete run has `118` result blocks, `3,058` passed, `0` failed, and `6` ignored.
- Fresh seam evidence is `scripts/out/audits/workspace-seams-20260810T-level-hints-final/workspace-seams.json`: source revision `d69e10446c92be684346387d613c28340f18b25f`, `35` workspace packages, `313` Rust files, `205,633` source lines, `127` module edges, `94` workspace dependency edges, unchanged 9-module product SCC, and `protected_changes=[]`. Runtime guardrails are green with `Critical: 0` and `Warnings: 0` at `scripts/out/audits/runtime-guardrails-20260810T-level-hints-final/audit-runtime-guardrails.log`.
- The all-feature/all-target check remains platform-blocked on macOS ARM64 by the repository-owned Linux-only guard `scripts/tests/rust/rt-transport-uring.rs:8`; no guard was weakened. Warning-free release verification passes with `--help` exit `0`, a `9,990,016`-byte `quicfuscate` binary, SHA-256 `dccbf7430636acc9cbede75f4d42ad2d11eea67d05f1c41ff25d2a1147388657`, and target usage `8,739,000 KiB` at the guarded build checkpoint, below the `12,582,912 KiB` cleanup threshold. No frontend or Tauri path changed, and no frontend field/API projection is required.

## QUIC Encryption-Level Contract Workspace Ownership (2026-08-10, TODO-562)

- `crates/qf-transport-types/src/lib.rs` now owns the dependency-free `QuicEncryptionLevel` contract for Initial, EarlyData, Handshake, and Application encryption levels. `src/qftls.rs` re-exports it as `crate::qftls::Level`, preserving the historical TLS API without a duplicate enum.
- The moved contract retains `repr(C)` and discriminants `0..=3` and introduces no connection, rustls, crypto, stealth, frontend, or Tauri dependency. TLS provider lifecycle and `crypto::aead::Level` stay with their existing owners.
- qf-transport-types tests pass `16/16` with strict Clippy; root qftls tests pass `24/24`; root checking and strict Clippy pass; the complete workspace all-target `rust-tests` run exits `0` with `118` result blocks, `3,059` passed, `0` failed, and `6` ignored.
- Fresh seam evidence is `scripts/out/audits/workspace-seams-20260810T-level-final/workspace-seams.json`: `35` packages, `313` Rust files, `205,646` source lines, `127` module edges, `94` Cargo workspace dependency edges, unchanged 9-module product SCC, and `protected_changes=[]`. Runtime guardrails are green with `Critical: 0` and `Warnings: 0` at `scripts/out/audits/runtime-guardrails-20260810T-level-final/audit-runtime-guardrails.log`.
- The all-feature/all-target check remains blocked only by the repository-owned Linux-only guard `scripts/tests/rust/rt-transport-uring.rs:8` on macOS ARM64; no guard was weakened. Warning-free release verification passes with `--help` exit `0`, a `9,990,016`-byte binary, SHA-256 `b493c25239022bff8fcdbb74df8b34d7e8563a6213c395af79c2ac0f1f7894a4`, and target usage `9,945,572 KiB`, below the `12,582,912 KiB` cleanup threshold. No frontend or Tauri path changed, and no frontend field/API projection is required.

## Stealth Brain Configuration Workspace Ownership (2026-08-10, TODO-562)

- `crates/qf-transport-types/src/brain.rs` now owns `StealthBrainConfig`, including its twelve fields, exact defaults, environment overrides, bounded clamping, cross-field validation, and captured-environment constructors. `src/brain.rs` retains the historical `crate::brain::StealthBrainConfig` path through a compatibility re-export; sensor fusion, FEC steering, transport mutation, and provider lifecycle remain root-owned.
- The child consumes `qf-common::env_utils::EnvSnapshot` only at the configuration boundary. No connection, FEC, stealth runtime, optimizer, frontend, or Tauri implementation crosses into the child, and existing environment variable names, defaults, validation errors, and fallback behavior remain unchanged.
- qf-transport-types tests pass `19/19` with strict Clippy; the focused root Brain filter passes `62/62`; root and workspace strict `rust-tests` Clippy plus all-feature library/binary Clippy pass. The complete workspace all-target `rust-tests` run exits `0` with `118` result blocks, `3,062` passed, `0` failed, and `6` ignored.
- Seam evidence is `scripts/out/audits/workspace-seams-20260810T-brain-config-final/workspace-seams.json`: source revision `d69e10446c92be684346387d613c28340f18b25f`, `35` workspace packages, `314` Rust files, `205,701` source lines, `127` module edges, `95` workspace dependency edges, unchanged 9-module product SCC, new `qf-transport-types -> qf-common` configuration edge, and `protected_changes=[]`. Runtime guardrails are green with `Critical: 0` and `Warnings: 0` at `scripts/out/audits/runtime-guardrails-20260810T-brain-config-final/audit-runtime-guardrails.log`.
- The all-feature/all-target check remains intentionally blocked by the Linux-only guard `scripts/tests/rust/rt-io-hotpath-kernel-integration.rs:4` on macOS ARM64; no guard was weakened. Release verification passes with `--help` exit `0`, a `9,990,144`-byte binary, SHA-256 `9af45c7eb233c6bdcf0f8f89996102ba945726f84af80ebdbfa82f2f18a688eb`, and target usage `1,694,492 KiB` with `21,017,564 KiB` free, below the `12,582,912 KiB` cleanup threshold. The threshold guard reported `release_threshold_clean=not_needed`; the prior workspace gate automatically cleaned at the threshold. No frontend or Tauri path changed and no frontend field/API projection is required.

## Stealth Network-Fingerprint Normalization Workspace Ownership (2026-08-09, TODO-562)

- `crates/qf-stealth/src/fingerprint.rs` is now the canonical owner of the root-independent TCP/ICMP network-fingerprint implementation: `OsFingerprintProfile`, `PacketNormalizer`, IP-ID behavior, bounded TCP option normalization, incremental IP/TCP checksums, ICMP unreachable policy, and the complete regression suite. `src/stealth/fingerprint.rs` is a compatibility projection for the historical root namespace; the root connection path keeps its existing `required_capacity` call through a doc-hidden compatibility export.
- The child retains its existing `qf-common`, `clap`, `log`, `rand`, and `serde` dependency surface and adds no product dependency edge. The implementation depends only on the child-owned `OsProfile` contract; TLS Cover, adaptive FEC, H3, transport, engine, implementation, frontend, and Tauri behavior remain outside this seam.
- qf-stealth all-target/all-feature tests pass `69/69` with strict all-target/all-feature Clippy. Root all-target checking with `rust-tests`, strict root `rust-tests` Clippy, and the focused root Stealth filter pass `206/206`. The complete workspace all-target `rust-tests` suite exits `0` with no failures, and the all-feature library/binary Clippy lane passes.
- Fresh seam evidence is `scripts/out/audits/workspace-seams-20260809T-stealth-fingerprint-final-2/workspace-seams.json`: `35` workspace packages, `310` Rust files, `205,501` source lines, `128` module edges, `94` Cargo workspace dependency edges, the unchanged 9-module product SCC (`brain`, `core`, `engine`, `fec`, `implementations`, `interface`, `qftls`, `stealth`, `transport`), and `protected_changes=[]`.
- Runtime guardrails are fully green with `Critical: 0` and `Warnings: 0` at `scripts/out/audits/runtime-guardrails-20260809T-stealth-fingerprint-final-2/audit-runtime-guardrails.log`. The complete all-feature/all-target check remains intentionally blocked only by the repository-owned macOS-incompatible Linux guard at `scripts/tests/rust/rt-io-hotpath-kernel-integration.rs:4`; no guard was weakened.
- Warning-free release verification passes with `RUSTFLAGS=-Dwarnings cargo build --release --bin quicfuscate --locked --offline`; `target/release/quicfuscate --help` exits `0`, the binary is `9,972,768` bytes with SHA-256 `90f00497ea181c76104853196f577a4f18145e9439d3436c5aa38b0b7799f314`, and the final target guard records `2,179,060 KiB` with `22,922,776 KiB` free, below the `12,582,912 KiB` cleanup threshold. The workspace test run previously crossed the threshold at `12,862,984 KiB` and was cleaned automatically to `0 KiB`.
- No frontend or Tauri path changed, and no frontend field/API projection is required. TODO-562 remains blocked for the coordinated cyclic product-core split and external Linux/native/CI acceptance gates.

## Transport Crypto Stream Workspace Leaf (2026-08-09, TODO-562)

- `crates/qf-transport-crypto-stream/` now owns the former `CryptoStream` implementation embedded in `src/transport/packet.rs`: reliable CRYPTO-frame buffering, ACK/loss range retention, PTO retransmission queues, bounded handshake-flight memory, and out-of-order receive reassembly. The root packet namespace re-exports the child type, so `CryptoContext` and qftls callers retain their existing paths.
- The child depends only on `qf-error` and `log`; no packet parser, crypto context, FEC, stealth, or frontend/Tauri implementation crosses into the leaf. The moved private range-overflow test remains attached to the canonical implementation and passes `1/1`.
- Isolated qf-transport-crypto-stream checking, `1/1` test, and strict all-target/all-feature Clippy pass. Root library checking with `rust-tests` passes after the re-export and private-owner removal. The complete workspace all-target `rust-tests` run exits 0 with 113 result blocks, `3,007` passed, `0` failed, and `6` ignored; qf-transport-crypto-stream contributes `1/1`, qf-transport-frames contributes `21/21`, and the root library contributes `1,890/1,890`. Workspace all-target checking, strict `rust-tests` Clippy, and all-feature library/binary Clippy pass.
- Fresh seam evidence is `scripts/out/audits/workspace-seams-20260809T102646Z/workspace-seams.json`: 30 workspace packages, 269 Rust files, 204,577 source lines, 133 module edges, 73 Cargo workspace dependency edges, and the unchanged 11-module product SCC (`brain`, `core`, `engine`, `fec`, `implementations`, `interface`, `optimize`, `qftls`, `simd`, `stealth`, `transport`). `protected_changes=[]`. Runtime guardrails are fully green with `Critical: 0` and `Warnings: 0` at `scripts/out/audits/runtime-guardrails-20260809T102646Z/audit-runtime-guardrails.log`.
- The complete all-feature/all-target Clippy lane remains intentionally platform-blocked at the repository-owned Linux-only guard `scripts/tests/rust/rt-io-hotpath-kernel-integration.rs:4` on macOS ARM64; no guard was weakened. Warning-free release verification remains the final gate for this slice.
- Warning-free release verification is green: `RUSTFLAGS=-Dwarnings cargo build --release --bin quicfuscate --locked --offline` succeeded, `target/release/quicfuscate --help` exited 0, the binary is `9,919,696` bytes with SHA-256 `214522993e6026f781beb3ce6093ac57bb5ffeedfd118f4a0933954ee585b8e0`, and the target ended at `7,186,816 KiB`, below the `12,582,912 KiB` cleanup threshold. The mandatory initial and threshold-triggered `cargo clean` cycles remain recorded above.
- No frontend or Tauri path changed, and no frontend field/API projection is required. TODO-562 remains blocked for the coordinated cyclic product-core split and external Linux/native/CI acceptance gates.

## Transport UDP Workspace Leaf (2026-08-09, TODO-562)

- `crates/qf-transport-udp/` now owns the former `src/optimize/udp.rs` and `src/transport/udpfast.rs` implementations: bounded Linux `sendmmsg`/`recvmmsg`, Apple `sendmsg_x` with scalar fallback, UDP GSO/GRO probing, the runtime `UdpFastPath`, checked completion and receive metadata, family-safe sockaddr conversion, and the test-gated NIC RPS helper. The roots `src/optimize/udp.rs` and `src/transport/udpfast.rs` are compatibility adapters.
- The child depends on `qf-cpu` for prefetch/inline policy plus `libc`, `smallvec`, and `sysinfo`; its low-level helper exports are hidden compatibility contracts rather than a new product API. No transport connection, FEC, crypto, stealth, or frontend/Tauri implementation crosses this boundary.
- Isolated qf-transport-udp all-target/all-feature checking, `11/11` tests on ARM64 macOS, and strict all-target/all-feature Clippy pass. Root library checking and strict `rust-tests` Clippy pass after adapter wiring. The complete workspace all-target `rust-tests` suite exits 0 with 114 result blocks, `3,007` passed, `0` failed, and `6` ignored; qf-transport-udp contributes `11/11`, qf-transport-batch `7/7`, qf-transport-crypto-stream `1/1`, qf-transport-frames `21/21`, and the root library `1,872/1,872`.
- Fresh seam evidence is `scripts/out/audits/workspace-seams-20260809T112739Z/workspace-seams.json`: 32 workspace packages, 272 Rust files, 204,685 source lines, 133 module edges, 79 Cargo workspace dependency edges, and the unchanged 11-module product SCC (`brain`, `core`, `engine`, `fec`, `implementations`, `interface`, `optimize`, `qftls`, `simd`, `stealth`, `transport`). qf-transport-batch depends on `qf-transport-udp`, qf-transport-udp depends on `qf-cpu`, and the root edge is `quicfuscate -> qf-transport-udp`; `protected_changes=[]`.
- The final refreshed runtime guardrail audit is fully green with `Critical: 0` and `Warnings: 0` at `scripts/out/audits/runtime-guardrails-20260809T114855Z/audit-runtime-guardrails.log`. Its owner-path assertions follow `crates/qf-transport-udp/src/lib.rs` and `fastpath.rs` while preserving the root compatibility adapter. The complete all-feature/all-target Clippy lane remains intentionally platform-blocked by the repository-owned Linux-only guards at `scripts/tests/rust/rt-io-hotpath-kernel-integration.rs:4` and `scripts/tests/rust/rt-transport-uring.rs:8` on macOS ARM64; no guard was weakened. Fresh release verification ended at `8,518,292 KiB` with `12,312,660 KiB` free, below the `12,582,912 KiB` cleanup threshold.
- Warning-free release verification passes with `RUSTFLAGS=-Dwarnings cargo build --release --bin quicfuscate --locked --offline`; `target/release/quicfuscate --help` exits 0, the binary is `9,919,680` bytes with SHA-256 `429b8a783c3ab1db106d39992bfc2409c9b69b608df37f9dcab3056307871542`, and the target ends at `10,354,308 KiB` with `11,885,328 KiB` free, below the `12,582,912 KiB` cleanup threshold.
- No frontend or Tauri path changed, and no frontend field/API projection is required. TODO-562 remains blocked for the coordinated cyclic product-core split and external Linux/native/CI acceptance gates.

## Transport Batch Workspace Leaf (2026-08-09, TODO-562)

- `crates/qf-transport-batch/src/lib.rs` now owns the former `src/transport/batch.rs` implementation: CPU-derived batch sizing, preallocated receive buffers, Linux `sendmmsg`/`recvmmsg` support, portable scalar fallback behavior, socket acceleration setup, checked datagram lengths, and the existing caller-descriptor/error regressions. The root `src/transport/batch.rs` is a cfg-gated compatibility projection for the historical `transport::batch::BatchProcessor` path.
- The child depends only on `qf-cpu`, `qf-telemetry`, and `qf-transport-udp` workspace contracts plus `libc`, `log`, and `socket2`; it is explicitly rust parity/test-only and has no normal runtime call sites. No transport connection, FEC, crypto, stealth, or frontend/Tauri implementation crosses this boundary.
- Isolated qf-transport-batch all-target/all-feature checking, `7/7` tests on ARM64 macOS, and strict all-target/all-feature Clippy pass. The root compatibility library check and strict `rust-tests` Clippy pass. The complete workspace all-target `rust-tests` suite exits 0 with `114` result blocks, `3,007` passed, `0` failed, and `6` ignored; qf-transport-batch contributes `7/7` and the root library contributes `1,875/1,875`.
- The all-feature library/binary Clippy lane passes with `RUSTFLAGS=-Dwarnings`. The complete all-feature/all-target Clippy lane remains intentionally platform-blocked by the repository-owned Linux-only guards at `scripts/tests/rust/rt-io-hotpath-kernel-integration.rs:4` and `scripts/tests/rust/rt-transport-uring.rs:8` on macOS ARM64; no guard was weakened. The release build initially exposed two normal-feature unused imports in the child; cfg-scoping those imports fixed the production-feature warning, after which warning-free release verification passed.
- Warning-free release verification passes with `RUSTFLAGS=-Dwarnings cargo build --release --bin quicfuscate --locked --offline`; `target/release/quicfuscate --help` exits 0, the binary is `9,919,664` bytes with SHA-256 `afceab64d55857dbcf13bfc7fe1b09946459c7277e16d43963c3f7319deb4896`, and target usage is `2,020,240 KiB`, below the `12,582,912 KiB` cleanup threshold. A proactive `cargo clean` removed `13.4 GiB` before the workspace Clippy rebuild when the target approached the threshold.
- Fresh seam evidence is `scripts/out/audits/workspace-seams-20260809T111032Z/workspace-seams.json`: 32 workspace packages, 271 Rust files, 204,671 source lines, 133 module edges, 78 Cargo workspace dependency edges, and the unchanged 11-module product SCC (`brain`, `core`, `engine`, `fec`, `implementations`, `interface`, `optimize`, `qftls`, `simd`, `stealth`, `transport`). qf-transport-batch depends on `qf-cpu`, `qf-telemetry`, and `qf-transport-udp`; the root edge is `quicfuscate -> qf-transport-batch`; `protected_changes=[]`.
- The final refreshed runtime guardrail audit is fully green with `Critical: 0` and `Warnings: 0` at `scripts/out/audits/runtime-guardrails-20260809T111033Z/audit-runtime-guardrails.log`. No frontend or Tauri path changed, and no frontend field/API projection is required. TODO-562 remains blocked for the coordinated cyclic product-core split and external Linux/native/CI acceptance gates.

## FEC Runtime-Initialization Workspace Ownership (2026-08-09, TODO-562)

- `crates/qf-fec/src/runtime.rs` now owns process-wide FEC initialization: bounded Rayon thread-cap policy, one-time global pool initialization, Galois-table initialization, and the `STREAM_ADJUST_MIN_MS` cadence constant. The root only re-exports the doc-hidden contracts used by `AdaptiveFec`; the root direct `rayon` dependency was removed because no root implementation remains.
- Isolated qf-fec all-target/all-feature checking, strict all-target/all-feature Clippy, and tests pass (`81/81`). Root all-target checking with `rust-tests`, strict `rust-tests` Clippy, focused root FEC tests pass (`211/211`), formatting and `git diff --check` pass; Cargo lock resolution remains green after the dependency ownership change.
- Fresh seam evidence is `scripts/out/audits/workspace-seams-20260809T-fec-runtime/workspace-seams.json`: `35` workspace packages, `298` Rust files, `205,096` source lines, `129` module edges, `93` Cargo workspace dependency edges, and the unchanged 9-module product SCC (`brain`, `core`, `engine`, `fec`, `implementations`, `interface`, `qftls`, `stealth`, `transport`); `protected_changes=[]`.
- Target usage is `9,849,564 KiB` and free space is `6,619,596 KiB`, below the `12,582,912 KiB` cleanup threshold and above the `2,097,152 KiB` build floor. No frontend or Tauri path changed and no frontend field/API projection is required; any UI projection remains deferred to the frontend phase. TODO-562 remains blocked for the coordinated cyclic product-core split and external Linux/native/CI acceptance gates.

## FEC Matrix Workspace Ownership (2026-08-09, TODO-562)

- `crates/qf-fec/src/matrix.rs` now owns the checked GF(2^8) matrix contract and its malformed-shape regression. The root `src/fec/mod.rs` retains only compatibility re-exports for `matrix_multiply_scalar` and `MatrixError`; the old duplicate matrix implementation and unused SSSE3 helper were removed without changing the exercised result path.
- Isolated qf-fec all-target/all-feature checking, strict all-target/all-feature Clippy, and tests pass (`81/81`, including the moved matrix test). Root all-target checking with `rust-tests`, strict `rust-tests` Clippy, focused root FEC tests pass (`211/211`), formatting and `git diff --check` pass.
- Fresh seam evidence is `scripts/out/audits/workspace-seams-20260809T-fec-matrix/workspace-seams.json`: `35` workspace packages, `297` Rust files, `205,080` source lines, `129` module edges, `93` Cargo workspace dependency edges, and the unchanged 9-module product SCC (`brain`, `core`, `engine`, `fec`, `implementations`, `interface`, `qftls`, `stealth`, `transport`); `protected_changes=[]`.
- Target usage is `8,254,980 KiB` and free space is `7,300,432 KiB`, below the `12,582,912 KiB` cleanup threshold and above the `2,097,152 KiB` build floor. No frontend or Tauri path changed and no frontend field/API projection is required; any UI projection remains deferred to the frontend phase. TODO-562 remains blocked for the coordinated cyclic product-core split and external Linux/native/CI acceptance gates.

## FEC Wire-Receiver Workspace Ownership (2026-08-09, TODO-562)

- `crates/qf-fec/src/receiver.rs` now owns the pool-backed `WireFecReceiver`, bounded receive-window lifecycle, profile-epoch admission, systematic/source framing, repair coefficient reconstruction, Fountain symbol deduplication, streaming-gap checks, partial/full recovery draining, and all 24 receiver/wire integration tests. `src/fec/wire.rs` is now a compatibility projection for the historical root namespace and keeps the root-only `FecMode` adapter visible to adaptive control.
- The child depends only on existing qf-fec codecs/decoders, qf-memory-pool, qf-telemetry, and the root-independent wire contract. `WireFecReceiver::set_fountain_seed` remains a doc-hidden compatibility method used by the root connection lifecycle; no frontend or Tauri surface is involved.
- Isolated qf-fec all-target/all-feature checking, strict all-target/all-feature Clippy, and tests pass (`80/80`). Root all-target checking with `rust-tests`, strict `rust-tests` Clippy, focused root FEC tests pass (`211/211`), formatting and `git diff --check` pass.
- Fresh seam evidence is `scripts/out/audits/workspace-seams-20260809T-fec-wire-receiver/workspace-seams.json`: `35` workspace packages, `296` Rust files, `205,152` source lines, `129` module edges, `93` Cargo workspace dependency edges, and the unchanged 9-module product SCC (`brain`, `core`, `engine`, `fec`, `implementations`, `interface`, `qftls`, `stealth`, `transport`); `protected_changes=[]`.
- Target usage is `8,106,792 KiB` and free space is `6,338,748 KiB`, below the `12,582,912 KiB` cleanup threshold and above the `2,097,152 KiB` build floor. No frontend or Tauri path changed and no frontend field/API projection is required; any UI projection remains deferred to the frontend phase. TODO-562 remains blocked for the coordinated cyclic product-core split and external Linux/native/CI acceptance gates.

## FEC Observer Workspace Ownership (2026-08-09, TODO-562)

- `crates/qf-fec/src/observer.rs` now owns the root-independent FEC observer state: ACK-delay EWMA, ECN evidence, platform/profile resolution, immutable runtime-policy snapshots, Brain hint attachment, streaming-interval calculation, and one-shot redundancy-hint admission. `src/fec/parts/codecs_and_observers.rs` retains only `FecTransportObserver`, the root `TransportObserver` adapter, and the live `Connection::set_fec_redundancy_ppm` bridge.
- The child depends only on its existing FEC/common contracts plus `parking_lot`; the transport trait and live Connection remain root-owned, so no reverse dependency or transport API widening was introduced. Existing observer behavior and connection-local semantics remain covered by the root tests.
- Isolated qf-fec all-target/all-feature checking, strict all-target/all-feature Clippy, and tests pass (`56/56`). Root all-target checking with `rust-tests`, strict `rust-tests` Clippy, focused root FEC tests pass (`235/235`), formatting and `git diff --check` pass.
- Fresh seam evidence is `scripts/out/audits/workspace-seams-20260809T-fec-observer/workspace-seams.json`: `35` workspace packages, `295` Rust files, `205,140` source lines, `129` module edges, `93` Cargo workspace dependency edges, and the unchanged 9-module product SCC (`brain`, `core`, `engine`, `fec`, `implementations`, `interface`, `qftls`, `stealth`, `transport`); `protected_changes=[]`.
- Target usage is `7,279,060 KiB` and free space is `10,410,632 KiB`, below the `12,582,912 KiB` cleanup threshold. No frontend or Tauri path changed and no frontend field/API projection is required; any UI projection remains deferred to the frontend phase. TODO-562 remains blocked for the coordinated cyclic product-core split and external Linux/native/CI acceptance gates.

## FEC Loss-Estimator Workspace Ownership (2026-08-09, TODO-562)

- `crates/qf-fec/src/loss.rs` now owns the root-independent `LossEstimator` contract: bounded EMA smoothing, Kalman integration, CUSUM change detection, bounded burst-window classification, clean-link proof, fountain-readiness admission, disturbance reporting, and burst-variance reporting. `src/fec/mod.rs` keeps the compatibility re-export and `src/fec/parts/adaptive_controller.rs` passes validated product policy and ambient Kalman overrides through the explicit child constructor; the root no longer retains a second production estimator owner.
- The child preserves the existing semantics and test contract without adding a frontend or Tauri dependency. Test-only constructors and probes remain feature-gated, while production construction uses validated `FecConfig` values and avoids widening the root public API.
- Isolated qf-fec all-target/all-feature checking, strict all-target/all-feature Clippy, and tests pass (`56/56`). Root all-target checking with `rust-tests`, strict `rust-tests` Clippy, and focused root FEC tests pass (`235/235`); `cargo fmt --all -- --check` and `git diff --check` pass.
- Fresh seam evidence is `scripts/out/audits/workspace-seams-20260809T-loss-estimator/workspace-seams.json`: `35` workspace packages, `294` Rust files, `205,068` source lines, `129` module edges, `93` Cargo workspace dependency edges, and the unchanged 9-module product SCC (`brain`, `core`, `engine`, `fec`, `implementations`, `interface`, `qftls`, `stealth`, `transport`); `protected_changes=[]`.
- The target is `5,697,220 KiB` and free space is `12,349,896 KiB`, below the `12,582,912 KiB` cleanup threshold. The mandatory initial `cargo clean` remains applied, and the next build guard must clean again if `target/` reaches the threshold.
- This is a backend-only ownership boundary. No frontend or Tauri path changed and no frontend field/API projection is required; any later UI projection remains deferred to the frontend phase. TODO-562 remains blocked for the coordinated cyclic product-core split and external Linux/native/CI acceptance gates.

## SIMD Workspace Leaf Reconciliation (2026-08-09, TODO-562)

- `crates/qf-simd/` now owns the former `src/simd/` implementation: runtime dispatch, scalar and architecture-specific SIMD helpers, FEC/Galois/bitstream operations, QPACK Huffman, QUIC varints, header validation, SHA-256/HMAC dispatch, and the moved SIMD parity tests. The root `src/simd.rs` remains a compatibility projection; root H3 QPACK delegates Huffman tables and codec operations to qf-simd, while root frame compatibility consumes only doc-hidden ACK and ARM STREAM helper exports.
- The child depends only on `qf-common`, `qf-cpu`, `qf-crypto`, and `qf-telemetry`, plus non-Windows `sha2-asm`; root feature forwarding covers `rust-tests`, `benches`, `prefetch`, `aggressive_inline`, and `internal_avx10_preview`. No frontend or Tauri implementation crosses this boundary.
- Isolated qf-simd all-target/all-feature checking and strict all-target/all-feature Clippy pass; its all-target/all-feature test suite passes `61/61`. Workspace all-target checking with `rust-tests` passes after the adapter move. Fresh seam evidence is `scripts/out/audits/workspace-seams-20260809T143432-qf-simd-final/workspace-seams.json`: 33 workspace packages, 273 Rust files, 204,747 source lines, 132 module edges, 84 Cargo workspace dependency edges, and a reduced 9-module product SCC (`brain`, `core`, `engine`, `fec`, `implementations`, `interface`, `qftls`, `stealth`, `transport`). qf-simd has only four child dependency edges plus `quicfuscate -> qf-simd`; `protected_changes=[]`.
- Workspace strict `rust-tests` Clippy and the complete workspace all-target `rust-tests` suite pass with `116` result blocks, `3,007` passed, `0` failed, and `6` ignored. The all-feature library/binary Clippy lane passes; the complete all-feature/all-target Clippy lane remains platform-blocked by the repository-owned Linux-only guards at `scripts/tests/rust/rt-io-hotpath-kernel-integration.rs:4` and `scripts/tests/rust/rt-transport-uring.rs:8` on macOS ARM64; no guard is weakened. Warning-free release verification passes with a `9,919,664`-byte binary, SHA-256 `e2fd68fca63f1674ada98063ad0a1eb3aebd9eb5445e044653422f152405132d`, and target usage `5,039,748 KiB` with `14,706,484 KiB` free. Runtime guardrails are fully green with `Critical: 0` and `Warnings: 0` at `scripts/out/audits/runtime-guardrails-20260809T142109-qf-simd-final/audit-runtime-guardrails.log`.
- No frontend or Tauri path changed, and no frontend field/API projection is required by this backend slice.

## FEC Wire Contract Workspace Leaf (2026-08-09, TODO-562)

- `crates/qf-fec/src/wire.rs` now owns the root-independent FEC wire contract: codec selection through `WireMode`, repair-coefficient generation, profile and packet metadata validation, fixed-header serialization/parsing, source-symbol framing, and typed wire reports/errors. The root `src/fec/wire.rs` retains the pool-backed `WireFecReceiver`, decoder windows, admission telemetry, and the compatibility `FecMode -> WireMode` adapter.
- `crates/qf-fec/src/policy.rs` now owns the bounded environment snapshot and FEC runtime-policy parsing; the root exposes only the compatibility alias used by decoders, observers, and adaptive control. The policy leaf depends on `qf-common` and the child wire limits, with no engine or transport dependency. Final post-extraction tests, audits, Clippy, and release gates are green below.
- The extraction keeps the dependency direction one-way: `quicfuscate -> qf-fec`. No frontend or Tauri surface is involved, and no receiver/decoder ownership was widened across the seam.
- Isolated qf-fec all-target/all-feature tests pass `56/56`; strict qf-fec Clippy, workspace checking with `rust-tests`, strict workspace `rust-tests` Clippy, and the complete workspace all-target `rust-tests` matrix pass. The workspace matrix exits 0 with `118` result blocks, `3,016` passed, `0` failed, and `6` ignored.
- Fresh seam evidence is `scripts/out/audits/workspace-seams-20260809T-qf-fec-policy-final/workspace-seams.json`: `35` workspace packages, `281` Rust files, `205,012` source lines, `130` module edges, `92` Cargo workspace dependency edges, and the unchanged 9-module product SCC (`brain`, `core`, `engine`, `fec`, `implementations`, `interface`, `qftls`, `stealth`, `transport`); `protected_changes=[]`.
- Runtime guardrails are fully green with `Critical: 0` and `Warnings: 0` at `scripts/out/audits/runtime-guardrails-20260809T-qf-fec-policy-final/audit-runtime-guardrails.log`; AMX proof and SIMD feature contracts pass. The all-feature library/binary Clippy lane passes with `RUSTFLAGS=-Dwarnings`; the full all-feature/all-target Clippy lane remains platform-blocked by the repository-owned Linux-only guard at `scripts/tests/rust/rt-transport-uring.rs:8` on macOS ARM64; no guard was weakened. Warning-free release verification passes with `RUSTFLAGS=-Dwarnings cargo build --release --bin quicfuscate --locked --offline`; `target/release/quicfuscate --help` exits 0, the binary is `9,937,216` bytes, SHA-256 is `45920bab75912c1602a7b4bb1c105e48bd21f6c20cf028900c2440edc21edac5`, and final target usage is `3,635,400 KiB` with `19,307,396 KiB` free, below the 12-GiB cleanup threshold.
- No frontend or Tauri field/API projection is required by the completed backend slices; any later UI projection remains explicitly deferred to the frontend phase.

## Stealth Utility Workspace Leaf (2026-08-09, TODO-562)

- `crates/qf-stealth/` now owns the root-independent domain-fronting and flow-shaping helpers formerly included from `src/stealth/parts/domain_fronting.rs` and `src/stealth/parts/flow_shaping.rs`: deterministic atomic CDN rotation, explicit random fallback, built-in provider catalogs, bounded packet-history retention, jitter, and handshake-flight pacing. The root stealth module keeps compatibility projections and no frontend/Tauri surface is involved.
- The child depends only on `qf-common` and `rand`; `quicfuscate -> qf-stealth` is one-way. `ProtocolClock` remains owned by qf-common, while `FlowShaper`, `StealthPacketClass`, `CdnProvider`, and `DomainFrontingManager` are child-owned doc-hidden contracts used by the existing manager and test paths.
- Isolated qf-stealth all-target/all-feature checking and strict Clippy pass. Workspace all-target checking with `rust-tests`, strict workspace `rust-tests` Clippy, the complete workspace all-target `rust-tests` matrix, and the all-feature library/binary Clippy lane pass. The workspace matrix exits 0 with `118` result blocks, `3,011` passed, `0` failed, and `6` ignored.
- Fresh seam evidence is `scripts/out/audits/workspace-seams-20260809T-qf-stealth-final/workspace-seams.json`: 35 workspace packages, 279 Rust files, 204,847 source lines, 130 module edges, 91 Cargo workspace dependency edges, and the unchanged 9-module product SCC (`brain`, `core`, `engine`, `fec`, `implementations`, `interface`, `qftls`, `stealth`, `transport`); `protected_changes=[]`.
- Runtime guardrails are fully green with `Critical: 0` and `Warnings: 0` at `scripts/out/audits/runtime-guardrails-20260809T-qf-stealth-final/audit-runtime-guardrails.log`; AMX proof and SIMD feature contracts pass. Full all-feature/all-target Clippy remains blocked by the repository-owned Linux-only guard at `scripts/tests/rust/rt-transport-uring.rs:8` on macOS ARM64; no guard was weakened. Warning-free release verification passes with `target/release/quicfuscate --help` exit 0, a `9,937,120`-byte binary, SHA-256 `1a647be6e7f9ecdd2a8c46a654b71d8942012796772caade4faaf05db2a371a7`, and final target usage `9,732,580 KiB` with `4,883,940 KiB` free.

## Stealth Scheduler Workspace Leaf (2026-08-09, TODO-562)

- `crates/qf-stealth/src/chaff.rs` and `crates/qf-stealth/src/probe_detector.rs` now own the former root `src/stealth/parts/chaff.rs` and `src/stealth/parts/probe_detector.rs`: bounded traffic-analysis lifecycle scheduling, jittered and phase-locked cadence, one-slot chaff admission, QUIC PING/PADDING plaintext generation, active-probe pattern matching, bounded timestamp retention, threshold escalation, and response-mode selection. The root files remain public compatibility projections; H3-coupled `CoverTrafficScheduler` stays root-local.
- qf-stealth depends on `qf-common`, `log`, and `rand`; the product edge remains one-way (`quicfuscate -> qf-stealth`). The child has no transport, H3, engine, or frontend/Tauri dependency. No frontend or Tauri field/API projection is required.
- Isolated qf-stealth all-target/all-feature tests pass `16/16`; strict qf-stealth Clippy, root all-target checking with `rust-tests`, workspace strict `rust-tests` Clippy, the complete workspace all-target `rust-tests` matrix, and the all-feature library/binary Clippy lane pass. The complete matrix exits 0 with `118` result blocks, `3,016` passed, `0` failed, and `6` ignored; `protected_changes=[]`.
- The root `rt-stealth-config-toml` target now serializes its two global-compression-policy tests with a local mutex after a reproducible parallel race; all nine assertions pass without changing production behavior.
- Fresh seam evidence is `scripts/out/audits/workspace-seams-20260809T-qf-stealth-chaff-probe-final/workspace-seams.json`: `35` workspace packages, `283` Rust files, `205,034` source lines, `130` module edges, `92` Cargo workspace dependency edges, and the unchanged 9-module product SCC (`brain`, `core`, `engine`, `fec`, `implementations`, `interface`, `qftls`, `stealth`, `transport`); `protected_changes=[]`.
- Runtime guardrails are fully green with `Critical: 0` and `Warnings: 0` at `scripts/out/audits/runtime-guardrails-20260809T-qf-stealth-chaff-probe-final/audit-runtime-guardrails.log`; AMX proof and SIMD feature contracts pass. The all-feature library/binary Clippy lane passes with `RUSTFLAGS=-Dwarnings`; the complete all-feature/all-target Clippy lane remains platform-blocked by the repository-owned Linux-only guards at `scripts/tests/rust/rt-io-hotpath-kernel-integration.rs:4` and `scripts/tests/rust/rt-transport-uring.rs:8` on macOS ARM64; no guard was weakened.
- Warning-free release verification passes with `RUSTFLAGS=-Dwarnings cargo build --release --bin quicfuscate --locked --offline`; `target/release/quicfuscate --help` exits 0, the binary is `9,937,280` bytes, SHA-256 is `892c6c6ab98f68a940cf0a1b27710b2931bcd5467a5056b2e85440ccab8f813c`, and final target usage is `5,192,604 KiB` with `16,495,540 KiB` free, below the 12-GiB cleanup threshold.

## Stealth Persona Enum Workspace Leaf (2026-08-09, TODO-562)

- `crates/qf-stealth/src/profiles.rs` now owns the root-independent `BrowserProfile` and `OsProfile` persona identifiers, including serde/CLI value derives, case-insensitive parsing, and the documented legacy macOS aliases. The root `src/stealth/parts/browser_profiles.rs` re-exports those types as a compatibility projection; `FingerprintProfile` remains root-local because it still couples TLS Cover, environment snapshots, and the transport-facing metadata contract.
- qf-stealth depends on `qf-common`, `clap`, `log`, `rand`, and `serde`; the product edge remains one-way (`quicfuscate -> qf-stealth`). The child has no engine, transport, H3, implementation, frontend, or Tauri dependency.
- Isolated qf-stealth all-target/all-feature tests pass `19/19`; strict qf-stealth Clippy, root all-target checking with `rust-tests`, root strict `rust-tests` Clippy, and the complete workspace all-target `rust-tests` matrix pass with exit 0. The current matrix has no failures; root contributes `1,748` tests and qf-stealth contributes `19`; the three new profile-contract tests are included in the workspace result.
- Fresh seam evidence is `scripts/out/audits/workspace-seams-20260809T-qf-stealth-profiles-final/workspace-seams.json`: `35` workspace packages, `284` Rust files, `205,067` source lines, `130` module edges, `92` Cargo workspace dependency edges, and the unchanged 9-module product SCC (`brain`, `core`, `engine`, `fec`, `implementations`, `interface`, `qftls`, `stealth`, `transport`); qf-stealth's only workspace edge is `qf-stealth -> qf-common`, with `quicfuscate -> qf-stealth` as the product edge; `protected_changes=[]`.
- Runtime guardrails are fully green with `Critical: 0` and `Warnings: 0` at `scripts/out/audits/runtime-guardrails-20260809T-qf-stealth-profiles-final/audit-runtime-guardrails.log`; AMX proof and SIMD feature contracts pass. The complete all-feature/all-target Clippy lane remains platform-blocked by the repository-owned Linux-only guards at `scripts/tests/rust/rt-io-hotpath-kernel-integration.rs:4` and `scripts/tests/rust/rt-transport-uring.rs:8` on macOS ARM64; no guard was weakened.
- Warning-free release verification passes with `RUSTFLAGS=-Dwarnings cargo build --release --bin quicfuscate --locked --offline`; `target/release/quicfuscate --help` exits 0, the binary is `9,936,976` bytes, SHA-256 is `8630860a0c34109c0f5fa1fc2e487025a6d2213682663fd1ba2b04437fa1ad66`, and final target usage is `3,255,956 KiB` with `18,220,728 KiB` free, below the 12-GiB cleanup threshold.
- No frontend or Tauri path changed, and no frontend field/API projection is required; any later UI projection remains explicitly deferred to the frontend phase.

## FEC Fountain Workspace Leaf (2026-08-09, TODO-562)

- `crates/qf-fec/` now owns the former `src/fec/fountain_codes.rs` and `src/fec/gf_tables.rs` implementations plus connection-local seed, decode-prefetch, GF16 slice, Brain/FEC hint, and Kalman smoothing primitives: deterministic LT source selection, robust-soliton distributions, HMAC-derived fountain seeds, GF(2^8)/GF(2^16) arithmetic and inverse tables, architecture-dispatched GF16 slice kernels, SIMD XOR dispatch, bounded repair admission and eviction, belief-propagation recovery, partial indexed results, and the moved FEC regression tests. Root `fec::fountain_codes`, `fec::gf_tables`, and the Brain/FEC contracts preserve historical compatibility paths through doc-hidden projections.
- The child depends on `qf-crypto`, `qf-cpu`, `qf-memory-pool`, and `qf-telemetry` plus `log`. It owns `BrainFecHints` and `KalmanFilter` alongside the default fountain seed, source-symbol bound, connection-local seed derivation, decode prefetch, Galois tables, GF16 scalar byte helpers, and architecture-dispatched u16 slice multiplication reused by root Brain, wire, and codec paths. No frontend or Tauri field/API projection is required.
- Isolated qf-fec all-target/all-feature tests pass `51/51`, strict all-target/all-feature Clippy passes, the complete workspace all-target `rust-tests` matrix exits 0 with `117` result blocks, `3,007` passed, `0` failed, and `6` ignored, workspace checking and strict `rust-tests` Clippy pass, and the all-feature library/binary Clippy lane passes.
- Fresh seam evidence is `scripts/out/audits/workspace-seams-20260809T-brainfec-final/workspace-seams.json`: 34 workspace packages, 278 Rust files, 204,853 source lines, 130 module edges, 89 Cargo workspace dependency edges, and the unchanged 9-module product SCC (`brain`, `core`, `engine`, `fec`, `implementations`, `interface`, `qftls`, `stealth`, `transport`); `protected_changes=[]`.
- Runtime guardrails are fully green with `Critical: 0` and `Warnings: 0` at `scripts/out/audits/runtime-guardrails-20260809T-brainfec-final/audit-runtime-guardrails.log`; AMX proof and SIMD feature contracts pass. Full all-feature/all-target Clippy remains blocked by the repository-owned Linux-only guard at `scripts/tests/rust/rt-transport-uring.rs:8` on macOS ARM64; no guard was weakened. Warning-free release verification passes with `target/release/quicfuscate --help` exit 0, a `9,920,400`-byte binary, SHA-256 `ecf5255b1137fbbaf5a8fed80f3011c40b7bac9b0b8699ab835124df3874e915`, and final target usage `6,676,268 KiB` with `9,258,820 KiB` free.

## FEC Packet, Decoder, and Zero-Path Ownership (2026-08-09, TODO-562)

- `crates/qf-fec/src/codecs.rs` now canonically owns pooled FEC packet storage, FEC mode/control contracts, GF(2^4)/GF(2^8)/GF(2^16) encoder ownership, and the child-local SIMD/telemetry adapters. The root `src/fec/parts/codecs_and_observers.rs` retains only the transport observer, loss estimator, and compatibility wrapper.
- `crates/qf-fec/src/decoders.rs` now canonically owns GF(2^4), GF(2^8), and GF(2^16) decoder state, dimension validation, Wiedemann scratch/solver helpers, and recovery result APIs. `src/fec/parts/decoders.rs` is a compatibility projection, while the root adaptive/config layer no longer extends child-private decoder state.
- `crates/qf-fec/src/zero.rs` owns the bounded zero-overhead encoder/decoder path. Root FEC keeps doc-hidden re-exports for historical `internal` and test callers. No duplicate production owner or frontend/Tauri projection was introduced.
- Isolated qf-fec all-target/all-feature checking, strict all-target/all-feature Clippy, and the complete qf-fec all-target/all-feature test suite pass (`56/56`). Root FEC compatibility checking and strict `rust-tests` Clippy pass; the focused root FEC suite passes (`235/235`).
- Fresh seam evidence is `scripts/out/audits/workspace-seams-20260809T-continuation-fec-zero/workspace-seams.json`: 35 workspace packages, 287 Rust files, 205,105 source lines, 129 module edges, 93 Cargo workspace dependency edges, the unchanged 9-module product SCC (`brain`, `core`, `engine`, `fec`, `implementations`, `interface`, `qftls`, `stealth`, `transport`), and `protected_changes=[]`.
- The mandatory target guard remains active: current target usage is 6.62 GiB with 12.01 GiB free, below the 12-GiB cleanup threshold and above the 2-GiB free-space floor. The coordinated FEC dispatch/adaptive boundary and the remaining product-core SCC are still open; TODO-562 remains blocked for those boundaries and external Linux/native/CI acceptance gates.

## FEC Mode-Manager Workspace Leaf (2026-08-09, TODO-562)

- `crates/qf-fec/src/manager.rs` now owns the root-independent `ModeManager`: bounded loss history, target ranking, hysteresis and dwell-time switching, explicit mode/window parameter resolution, finite-redundancy admission, and protocol-time access through `qf-common`. The root `src/fec/internal.rs` no longer contains a second production mode-manager implementation.
- The root FEC module keeps a doc-hidden `ModeManager` compatibility re-export for adaptive-controller, engine, and test callers. The remaining root integration still owns encoder/decoder dispatch, lazy decoding, interleaving, packet orchestration, and product telemetry.
- This slice adds no frontend or Tauri surface. The qf-fec dependency remains one-way from `quicfuscate`, and any future frontend projection is deferred to the frontend phase.
- Isolated qf-fec all-target/all-feature checking, strict all-target/all-feature Clippy, and the complete qf-fec all-target/all-feature test suite pass (`56/56`). Root all-target `rust-tests` checking, root strict `rust-tests` Clippy, and the focused root FEC suite pass (`235/235`).
- Fresh seam evidence is `scripts/out/audits/workspace-seams-20260809T-manager/workspace-seams.json`: 35 workspace packages, 289 Rust files, 205,121 source lines, 129 module edges, 93 Cargo workspace dependency edges, the unchanged 9-module product SCC (`brain`, `core`, `engine`, `fec`, `implementations`, `interface`, `qftls`, `stealth`, `transport`), and `protected_changes=[]`.
- The post-gate target guard records `4,583,968 KiB` target usage and `13,599,680 KiB` free, below the `12,582,912 KiB` cleanup threshold and above the `2,097,152 KiB` free-space floor. The remaining FEC dispatch/adaptive boundary, product-core SCC, and external Linux/native/CI acceptance gates stay open.

## FEC Variant-Dispatch Workspace Leaf (2026-08-09, TODO-562)

- `crates/qf-fec/src/variants.rs` now contains the mode-to-codec `EncoderVariant` and `DecoderVariant` dispatch, fountain repair identity allocation, pooled repair/result conversion, and FEC decoder progress telemetry. The child depends only on existing FEC, memory-pool, wire, policy, and telemetry contracts.
- `src/fec/internal.rs` retains `LazyDecoder`, `InterleavedEncoder`, and `InterleavedDecoder`; it re-exports the child variants so historical `quicfuscate::fec::internal` test and wire callers remain source-compatible.
- This slice is backend-only and introduces no frontend or Tauri surface. Isolated qf-fec all-target/all-feature checking, strict all-target/all-feature Clippy, and the complete qf-fec all-target/all-feature test suite pass (`56/56`). Root all-target `rust-tests` checking, root strict `rust-tests` Clippy, the focused root FEC suite, and `git diff --check` pass; the focused root FEC suite passes (`235/235`).
- Fresh seam evidence is `scripts/out/audits/workspace-seams-20260809T-variants/workspace-seams.json`: 35 workspace packages, 290 Rust files, 205,128 source lines, 129 module edges, 93 Cargo workspace dependency edges, the unchanged 9-module product SCC (`brain`, `core`, `engine`, `fec`, `implementations`, `interface`, `qftls`, `stealth`, `transport`), and `protected_changes=[]`.
- The post-gate target guard records `4,802,520 KiB` target usage and `12,309,232 KiB` free, below the `12,582,912 KiB` cleanup threshold and above the `2,097,152 KiB` free-space floor. The remaining root FEC orchestration, product-core SCC, and external Linux/native/CI acceptance gates stay open; any later frontend projection remains deferred to the frontend phase.

## FEC Interleaved-Encoder Workspace Leaf (2026-08-09, TODO-562)

- `crates/qf-fec/src/interleaved.rs` now owns `InterleavedEncoder`, lane-aware repair identity allocation, represented `(k, n)` shape reporting, and the bounded repair-ordinal constants. The root `src/fec/internal.rs` keeps the historical `InterleavedEncoder` path as a compatibility re-export; `LazyDecoder` and `InterleavedDecoder` remain root-owned for the next coupled extraction.
- This slice is backend-only and introduces no frontend or Tauri surface. Isolated qf-fec all-target/all-feature checking, strict all-target/all-feature Clippy, and tests pass (`56/56`); root all-target `rust-tests` checking, strict `rust-tests` Clippy, formatting, diff hygiene, and focused root FEC tests pass (`235/235`).
- Fresh seam evidence is `scripts/out/audits/workspace-seams-20260809T-interleaved-encoder/workspace-seams.json`: 35 workspace packages, 291 Rust files, 205,116 source lines, 129 module edges, 93 Cargo workspace dependency edges, the unchanged 9-module product SCC, and `protected_changes=[]`. The post-gate target guard records `5,018,204 KiB` target usage and `13,094,456 KiB` free, below the cleanup threshold and above the free-space floor.

## FEC Lazy-Decoder Workspace Leaf (2026-08-09, TODO-562)

- `crates/qf-fec/src/lazy.rs` now owns `LazyDecoder`: bounded pending source/repair state, gap and tail-loss detection, streaming repair admission, lazy versus full recovery decisions, partial-result draining, fountain-seed propagation, and lazy-skip telemetry. The root `src/fec/internal.rs` re-exports it; `InterleavedDecoder` remains root-owned as the next adapter boundary.
- This slice is backend-only and introduces no frontend or Tauri surface. Isolated qf-fec all-target/all-feature checking, strict all-target/all-feature Clippy, and tests pass (`56/56`); root all-target `rust-tests` checking, strict `rust-tests` Clippy, formatting, diff hygiene, and focused root FEC tests pass (`235/235`).
- Fresh seam evidence is `scripts/out/audits/workspace-seams-20260809T-lazy-decoder/workspace-seams.json`: 35 workspace packages, 292 Rust files, 205,082 source lines, 129 module edges, 93 Cargo workspace dependency edges, the unchanged 9-module product SCC, and `protected_changes=[]`. The post-gate target guard records `5,258,896 KiB` target usage and `12,844,788 KiB` free, below the cleanup threshold and above the free-space floor.

## FEC Interleaved-Decoder Workspace Leaf (2026-08-09, TODO-562)

- `crates/qf-fec/src/interleaved_decoder.rs` now owns `InterleavedDecoder`: validated wire-profile construction, lane routing for source and repair identities, full and partial result aggregation, recovery-state queries, seed propagation, and bounded test introspection. The root `src/fec/internal.rs` re-exports it for wire receive, adaptive controller, and historical test callers.
- This slice is backend-only and introduces no frontend or Tauri surface. Isolated qf-fec all-target/all-feature checking, strict all-target/all-feature Clippy, and tests pass (`56/56`); root all-target `rust-tests` checking, strict `rust-tests` Clippy, formatting, diff hygiene, and focused root FEC tests pass (`235/235`).
- Fresh seam evidence is `scripts/out/audits/workspace-seams-20260809T-interleaved-decoder/workspace-seams.json`: 35 workspace packages, 293 Rust files, 205,084 source lines, 129 module edges, 93 Cargo workspace dependency edges, the unchanged 9-module product SCC, and `protected_changes=[]`. The post-gate target guard records `5,510,796 KiB` target usage and `12,330,412 KiB` free, below the cleanup threshold and above the free-space floor.

## FEC Configuration Workspace Ownership (2026-08-09, TODO-562)

- `crates/qf-fec/src/config.rs` now canonically owns `FecConfig`, including adaptive-FEC TOML parsing, defaults, product defaults, validation, mode/window policy, and the public configuration fields. The root `src/fec` namespace re-exports the child type, so existing engine, implementation, CLI, and test callers retain `quicfuscate::fec::FecConfig` without a duplicate owner.
- Engine coupling is limited to the root compatibility adapter: `EngineFecMode` and `EngineFecSection` are child-owned contracts, implemented for the root engine enums/section in `src/fec/mod.rs`. qf-fec does not import engine, transport, implementations, frontend, or Tauri code. The child adds only direct `serde` derive and `toml` parsing dependencies.
- qf-fec all-target/all-feature checking, strict all-target/all-feature Clippy, and tests pass (`81/81`). Root all-target checking with `rust-tests`, strict `rust-tests` Clippy, focused FEC tests (`211/211`), formatting, and `git diff --check` pass. Fresh seam evidence is `scripts/out/audits/workspace-seams-20260809T-fec-config/workspace-seams.json`: `35` workspace packages, `299` Rust files, `205,184` source lines, `129` module edges, `93` workspace dependency edges, the unchanged 9-module product SCC, and `protected_changes=[]`.
- The post-gate target guard records `11,212,088 KiB` target usage and `2,997,980 KiB` free, below the `12,582,912 KiB` cleanup threshold and above the `2,097,152 KiB` build floor. No frontend or Tauri path changed; any UI projection remains deferred to the frontend phase.

## FEC Runtime-Plan Workspace Ownership (2026-08-09, TODO-562)

- `crates/qf-fec/src/runtime_plan.rs` now owns the root-independent FEC runtime-plan contract: host compute-profile snapshots, explicit memory-pool and environment inputs, policy-aware mode/target resolution, CPU-dependent stream cadence, wire-safe `(k, n, depth)` normalization, partial-recovery admission, and loss-estimator construction. The root adaptive controller keeps only the product lifecycle adapter and injects the existing global pool.
- The child depends on qf-cpu, qf-common, qf-memory-pool, and its existing FEC policy/target/loss contracts without importing engine, transport, implementations, frontend, or Tauri code. Existing root callers retain `FecAmbientInputs`, `FecComputeProfile`, `FecRuntimePlan`, and `wire_safe_encoder_params` through compatibility re-exports; the explicit compute-profile test remains unchanged.
- qf-fec all-target/all-feature checking, strict all-target/all-feature Clippy, and tests pass (`81/81`). Root all-target checking with `rust-tests`, strict `rust-tests` Clippy, and focused FEC tests pass (`211/211`); formatting and `git diff --check` pass.
- Fresh seam evidence is `scripts/out/audits/workspace-seams-20260809T-runtime-plan/workspace-seams.json`: `35` workspace packages, `300` Rust files, `205,214` source lines, `129` module edges, `93` Cargo workspace dependency edges, the unchanged 9-module product SCC (`brain`, `core`, `engine`, `fec`, `implementations`, `interface`, `qftls`, `stealth`, `transport`), and `protected_changes=[]`.
- The post-gate target guard records `12,345,244 KiB` target usage and `3,139,948 KiB` free, below the `12,582,912 KiB` cleanup threshold and above the `2,097,152 KiB` build floor. No frontend or Tauri path changed; any UI projection remains deferred to the frontend phase.

## FEC State-Contract Workspace Ownership (2026-08-09, TODO-562)

- `crates/qf-fec/src/state.rs` now owns the root-independent `FecTelemetrySnapshot` and `FecPolicyChange` value contracts. The root adaptive controller retains the live counters and policy transition logic, while the root namespace re-exports both types unchanged for engine, connection, implementation, and test callers.
- The child state contract depends only on `FecControlPolicy` and `FecMode`; it imports no transport, engine, implementation, frontend, or Tauri code. Existing field names and wire/telemetry semantics are unchanged.
- qf-fec all-target/all-feature checking, strict Clippy, and tests pass (`81/81`); root all-target `rust-tests` checking, strict `rust-tests` Clippy, and focused FEC tests pass (`211/211`). No frontend or Tauri path changed; UI projection remains deferred.

## Transport Traffic-Policy Workspace Ownership (2026-08-09, TODO-562)

- `crates/qf-transport-types/src/traffic.rs` now canonically owns `TrafficAnalysisDefense` and `TrafficAnalysisPolicy`, including parsing, serde aliases, defaults, validation, ceiling intersection, and wire-cost estimation. `src/transport/config.rs` re-exports the child contracts, preserving every historical `transport::config` caller.
- The child imports no connection, FEC, stealth, engine, implementation, frontend, or Tauri code. It depends on serde for the existing configuration serialization contract and keeps the transport policy as a backend value boundary.
- qf-transport-types all-target/all-feature checking, strict Clippy, and tests pass (`10/10`); root transport configuration regressions pass (`50/50`), root all-target `rust-tests` checking, and strict `rust-tests` Clippy pass. Fresh seam evidence is `scripts/out/audits/workspace-seams-20260809T-transport-traffic-policy/workspace-seams.json`: `35` workspace packages, `302` Rust files, `205,269` source lines, `129` module edges, `93` Cargo workspace dependency edges, unchanged 9-module product SCC, and `protected_changes=[]`.
- The target was cleaned after the free-space guard reached `1,555,740 KiB`; the post-clean target guard was `0 KiB` with `15,491,160 KiB` free. No frontend or Tauri path changed; any UI projection remains deferred to the frontend phase.

## Transport Runtime-Control Contracts (2026-08-09, TODO-562)

- `crates/qf-transport-types/src/runtime.rs` now owns the root-independent `FecControlDelta` and `BrainRuntimePermissions` contracts. `src/transport.rs` re-exports them, so Connection, Brain, StealthManager, engine, and test callers retain their existing paths while the root loses two duplicate contract owners.
- The child runtime contracts have no product, connection, FEC implementation, frontend, or Tauri dependency. Their defaults remain inert for FEC deltas and fully permissive for Brain actuators, matching the previous behavior.
- qf-transport-types all-target/all-feature checking, strict Clippy, and tests pass (`12/12`); the complete root transport test filter passes (`312/312`), root all-target `rust-tests` checking, strict `rust-tests` Clippy, formatting, and `git diff --check` pass. Fresh seam evidence is `scripts/out/audits/workspace-seams-20260809T-transport-runtime-contracts/workspace-seams.json`: `35` workspace packages, `303` Rust files, `205,300` source lines, `129` module edges, `93` Cargo workspace dependency edges, unchanged 9-module product SCC, and `protected_changes=[]`.
- The post-gate target guard records `4,497,072 KiB` target usage and `14,902,544 KiB` free, below the `12,582,912 KiB` cleanup threshold and above the `2,097,152 KiB` build floor. No frontend or Tauri path changed; any UI projection remains deferred to the frontend phase.

## Transport PMTU-Policy Workspace Ownership (2026-08-09, TODO-562)

- `crates/qf-transport-recovery/src/lib.rs` now canonically owns the root-independent `PmtuPolicy` contract, including defaults and DPLPMTUD bound/timer validation. `src/transport/config.rs` re-exports the child type, preserving the historical `transport::config::PmtuPolicy` path without a duplicate definition.
- The child recovery contract keeps the existing `ConnectionError` validation semantics and introduces no frontend, Tauri, engine, implementation, or connection dependency. The root transport connection continues to consume the same policy values and validation behavior through the compatibility re-export.
- qf-transport-recovery all-target/all-feature tests pass (`38/38`). Root all-target checking with `rust-tests`, strict `rust-tests` Clippy, and the complete root transport test filter pass (`312/312`); formatting and `git diff --check` pass.
- Fresh seam evidence is `scripts/out/audits/workspace-seams-20260809T-pmtu-policy/workspace-seams.json`: `35` workspace packages, `303` Rust files, `205,313` source lines, `129` module edges, `93` Cargo workspace dependency edges, the unchanged 9-module product SCC (`brain`, `core`, `engine`, `fec`, `implementations`, `interface`, `qftls`, `stealth`, `transport`), and `protected_changes=[]`.
- The post-gate target guard records `4,854,520 KiB` target usage and `13,195,432 KiB` free, below the `12,582,912 KiB` cleanup threshold and above the `2,097,152 KiB` build floor. No frontend or Tauri path changed; any UI projection remains deferred to the frontend phase.

## Transport Stealth-Contract Workspace Ownership (2026-08-09, TODO-562)

- `crates/qf-transport-types/src/stealth.rs` now canonically owns the root-independent `BrowserProfile`, `StealthRuntimePolicy`, and `StealthRuntimeDelta` value contracts. `src/transport.rs` and `qf-transport-recovery` retain compatibility re-exports, and `qf-transport-cc` preserves its historical `cc::stealth_shaper::BrowserProfile` path.
- The congestion-control child consumes the shared profile through a one-way `qf-transport-cc -> qf-transport-types` edge. No connection, FEC, engine, implementation, frontend, or Tauri behavior moved into the value crate; the old root structs were removed rather than duplicated.
- qf-transport-types all-target/all-feature tests pass (`14/14`), qf-transport-cc tests pass (`92/92`), qf-transport-recovery tests pass (`38/38`), and the complete root transport test filter passes (`312/312`). Root all-target `rust-tests` checking, strict `rust-tests` Clippy, isolated checks/Clippy, formatting, and `git diff --check` pass.
- Fresh seam evidence is `scripts/out/audits/workspace-seams-20260809T-transport-stealth-contracts/workspace-seams.json`: `35` workspace packages, `304` Rust files, `205,336` source lines, `129` module edges, `94` Cargo workspace dependency edges, the unchanged 9-module product SCC (`brain`, `core`, `engine`, `fec`, `implementations`, `interface`, `qftls`, `stealth`, `transport`), and `protected_changes=[]`.
- The post-gate target guard records `6,179,472 KiB` target usage and `12,314,976 KiB` free, below the `12,582,912 KiB` cleanup threshold and above the `2,097,152 KiB` build floor. No frontend or Tauri path changed; any UI projection remains deferred to the frontend phase.

## Transport PMTU-State Workspace Ownership (2026-08-09, TODO-562)

- `crates/qf-transport-recovery/src/pmtu.rs` now owns the DPLPMTUD `PmtuState` machine: validated construction, probe scheduling, congestion-bypass timing, binary-search confirmation/loss, black-hole reset, and above-floor ACK evidence. The root connection module imports the child state and retains only its independent prefetch helpers.
- The recovery child exposes the safe `min_mtu()` query required by packetization and a test-/`rust-tests`-gated confirmed-MTU setter for the historical root compatibility harness; PMTU state fields remain private. No connection, FEC, engine, implementation, frontend, or Tauri implementation moved into the child.
- qf-transport-recovery all-target/all-feature checking, strict Clippy, and tests pass (`41/41`). Root all-target `rust-tests` checking, strict `rust-tests` Clippy, and the complete root transport test filter pass (`309/309`); formatting and `git diff --check` pass.
- Fresh seam evidence is `scripts/out/audits/workspace-seams-20260809T-transport-pmtu-state/workspace-seams.json`: `35` workspace packages, `305` Rust files, `205,349` source lines, `129` module edges, `94` Cargo workspace dependency edges, the unchanged 9-module product SCC (`brain`, `core`, `engine`, `fec`, `implementations`, `interface`, `qftls`, `stealth`, `transport`), and `protected_changes=[]`.
- The post-gate target guard records `7,276,704 KiB` target usage and `11,557,068 KiB` free, below the `12,582,912 KiB` cleanup threshold and above the `2,097,152 KiB` build floor. No frontend or Tauri path changed; any UI projection remains deferred to the frontend phase.

## Stealth Persona Enum Contracts (2026-08-09, TODO-562)

- `crates/qf-stealth/src/config.rs` now canonically owns the root-independent `PaddingStrategy`, `StealthMode`, and `RotationMode` enums, including serde aliases and their existing configuration spellings. `src/stealth/parts/config.rs` removes the duplicate definitions and `src/stealth/mod.rs` re-exports the child types through the historical root namespace.
- The child config leaf has no transport, connection, FEC, engine, implementation, frontend, or Tauri dependency. `FecMode` remains root-local because its adaptive FEC behavior is still coupled to the root FEC controller; no unrelated contract was moved.
- qf-stealth all-target/all-feature checking, strict Clippy, and tests pass (`22/22`). Root all-target `rust-tests` checking, strict `rust-tests` Clippy, and the complete root Stealth test filter pass (`230/230`). Fresh seam evidence is `scripts/out/audits/workspace-seams-20260809T-stealth-enums/workspace-seams.json`: `35` workspace packages, `306` Rust files, `205,385` source lines, `129` module edges, `94` Cargo workspace dependency edges, the unchanged 9-module product SCC (`brain`, `core`, `engine`, `fec`, `implementations`, `interface`, `qftls`, `stealth`, `transport`), and `protected_changes=[]`.
- The post-gate target guard records `7,538,108 KiB` target usage and `11,221,844 KiB` free, below the `12,582,912 KiB` cleanup threshold and above the `2,097,152 KiB` build floor. No frontend or Tauri path changed; any UI projection remains deferred to the frontend phase.

## Stealth TLS ClientHello Catalog Workspace Ownership (2026-08-09, TODO-562)

- `crates/qf-stealth/src/tls_client_hello.rs` now canonically owns `TlsClientHelloProfileCatalog` and its exact 13 browser/OS metadata combinations. `src/stealth/parts/tls_client_hello.rs` is a compatibility projection for the historical root path; rustls remains the active wire ClientHello owner and no transport override setter is introduced.
- The child depends only on its existing root-independent profile contracts and `qf-common`; no connection, transport, FEC, engine, implementation, frontend, or Tauri behavior crosses this boundary. The catalog test locks the curated matrix and rejects unsupported Safari/Linux metadata.
- qf-stealth all-target/all-feature tests pass `23/23` with strict all-target/all-feature Clippy. Root all-target `rust-tests` checking, strict root `rust-tests` Clippy, and the root Stealth test filter pass `252/252`; formatting, diff hygiene, and the refreshed runtime guardrail audit pass with `Critical: 0` and `Warnings: 0` at `scripts/out/audits/runtime-guardrails-20260809T-stealth-tls-final-2/audit-runtime-guardrails.log`.
- Fresh seam evidence is `scripts/out/audits/workspace-seams-20260809T-stealth-tls-catalog-final-2/workspace-seams.json`: `35` workspace packages, `309` Rust files, `205,489` source lines, `128` module edges, `94` Cargo workspace dependency edges, the unchanged 9-module product SCC (`brain`, `core`, `engine`, `fec`, `implementations`, `interface`, `qftls`, `stealth`, `transport`), and `protected_changes=[]`.
- Warning-free release verification passes with `RUSTFLAGS=-Dwarnings cargo build --release --bin quicfuscate --locked --offline`; `target/release/quicfuscate --help` exits `0`, the binary is `9,972,752` bytes with SHA-256 `bf108ec19b2e3758ac3be307339b5acde66a649465867bd1c465fe1f83426ad9`, and the target guard records `11,894,856 KiB` with `5,010,352 KiB` free, below the `12,582,912 KiB` cleanup threshold and above the `2,097,152 KiB` build floor.
- No frontend or Tauri path changed, and no frontend field/API projection is required. TODO-562 remains blocked for the coordinated cyclic product-core split and external Linux/native/CI acceptance gates.

## Anti-Replay Configuration Workspace Ownership (2026-08-09, TODO-562)

- `crates/qf-transport-anti-replay/src/config.rs` now owns the operator-facing `AntiReplaySection` contract: strict serde/TOML decoding, defaults, and enabled-mode limit validation. `src/engine/config.rs` removes the duplicate definition, re-exports the child type through `quicfuscate::engine`, and maps the child validation message into the existing `ConfigError::Validation` surface.
- The child now depends on `qf-common`, `parking_lot`, `serde`, and `sha2`; it has no connection, FEC, stealth, engine, implementation, frontend, or Tauri dependency. The runtime `AntiReplayConfig` strike-register contract remains in the same child and is unchanged.
- qf-transport-anti-replay all-target/all-feature checking, strict Clippy, and tests pass (`14/14`). Root all-target `rust-tests` checking, strict `rust-tests` Clippy, EngineConfig tests (`39/39`), and AppConfig projection tests (`2/2`) pass. Fresh seam evidence is `scripts/out/audits/workspace-seams-20260809T-anti-replay-config/workspace-seams.json`: `35` workspace packages, `307` Rust files, `205,427` source lines, `129` module edges, `94` Cargo workspace dependency edges, the unchanged 9-module product SCC (`brain`, `core`, `engine`, `fec`, `implementations`, `interface`, `qftls`, `stealth`, `transport`), and `protected_changes=[]`.
- The post-gate target guard records `8,739,040 KiB` target usage and `9,848,312 KiB` free, below the `12,582,912 KiB` cleanup threshold and above the `2,097,152 KiB` build floor. No frontend or Tauri path changed; any UI projection remains deferred to the frontend phase.

## QUIC Version Configuration Contract (2026-08-09, TODO-562)

- `crates/qf-transport-version/src/lib.rs` now also owns the root-independent `QuicVersion` configuration enum and its `V1`/`V2` wire mapping. `src/engine/config.rs` removes the duplicate definition and re-exports the child type through the historical `quicfuscate::engine::QuicVersion` path; client and engine callers continue to use the same method and serde spellings.
- The child adds only serde to its existing `qf-common` and `qf-error` boundary. Negotiation state, long-header mapping, and the low-frequency scalar Version Information codec remain unchanged; no packet, connection, FEC, stealth, implementation, frontend, or Tauri behavior crossed the boundary.
- qf-transport-version all-target/all-feature checking, strict Clippy, and tests pass (`7/7`). Root all-target `rust-tests` checking, strict `rust-tests` Clippy, and EngineConfig tests (`39/39`) pass. Fresh seam evidence is `scripts/out/audits/workspace-seams-20260809T-quic-version-config/workspace-seams.json`: `35` workspace packages, `307` Rust files, `205,449` source lines, `129` module edges, `94` Cargo workspace dependency edges, the unchanged 9-module product SCC (`brain`, `core`, `engine`, `fec`, `implementations`, `interface`, `qftls`, `stealth`, `transport`), and `protected_changes=[]`.
- The post-gate target guard records `9,924,628 KiB` target usage and `7,133,388 KiB` free, below the `12,582,912 KiB` cleanup threshold and above the `2,097,152 KiB` build floor. No frontend or Tauri path changed; any UI projection remains deferred to the frontend phase.

## Adaptive FEC Controller Workspace Ownership (2026-08-09, active)

- The former root `src/fec/parts/adaptive_controller.rs` and `gf16_and_config.rs` implementations are now the canonical `qf-fec` adaptive controller sources. `qf-fec::AdaptiveFec` owns mode transitions, pooled interleaved encode/decode, loss feedback, wire-profile publication, telemetry, SIMD selection, and runtime control deltas; the root retains only type and contract re-exports.
- Product construction injects the existing `optimize::global_pool()` through `AdaptiveFec::new_with_snapshot_and_pool`; standalone qf-fec construction uses an explicit child-owned pool. This keeps the child independent of the product crate without changing the product pool lifecycle.
- The child exposes only `#[doc(hidden)]` compatibility fields and methods required by the existing root FEC regression modules. No frontend, Tauri, or visible UI/API projection is part of this backend move.
- Verification is complete for this slice: qf-fec all-target/all-feature tests pass `81/81`; the focused root FEC suite passes `249/249`; qf-fec all-target/all-feature Clippy, root all-target `rust-tests` checking, strict workspace `rust-tests` Clippy, formatting, and `git diff --check` pass. The complete workspace all-target `rust-tests` test command exits `0`.
- `cargo check --workspace --all-features` passes, and the all-feature library/binary Clippy lane passes with `-D warnings`. The complete all-feature/all-target check remains platform-blocked by the repository-owned Linux-only guard at `scripts/tests/rust/rt-transport-uring.rs:8` on macOS ARM64; no guard was weakened.
- Runtime guardrails are green with `Critical: 0` and `Warnings: 0` at `scripts/out/audits/runtime-guardrails-20260809T-adaptive-fec-final-2/audit-runtime-guardrails.log`. Warning-free release verification passes with `RUSTFLAGS=-Dwarnings cargo build --release --bin quicfuscate --locked --offline`; `target/release/quicfuscate --help` exits `0`, the binary is `9,972,752` bytes, and SHA-256 is `a43b1782cd94e3afd07bced762954c0b1a9116470048e6aa56dd5610319f6b07`.
- Fresh seam evidence is `scripts/out/audits/workspace-seams-20260809T-adaptive-fec-final-2/workspace-seams.json`: `35` workspace packages, `308` Rust files, `205,472` source lines, `128` module edges, `94` Cargo workspace dependency edges, the unchanged 9-module product SCC (`brain`, `core`, `engine`, `fec`, `implementations`, `interface`, `qftls`, `stealth`, `transport`), and `protected_changes=[]`. The final target guard records `11,575,580 KiB` with `5,447,036 KiB` free, below the `12,582,912 KiB` cleanup threshold and above the `2,097,152 KiB` build floor.
- TODO-562 remains blocked for the measured product SCC and external Linux/native/CI acceptance gates. No frontend or Tauri field/API projection is required by this backend slice; any UI projection remains explicitly deferred to the frontend phase.

## Deviations

None.

## Linux TUN Provisioning Runtime Isolation (2026-08-10, TODO-562)

- Hosted CI run `31434401022` proved that `tun-provisioning-negative-netns.sh` isolated the network namespace but shared the host mount namespace. Its intentional `qf-missing` race correctly retained `/run/quicfuscate/routing/firewall-owner.json` after unverifiable rollback, then deleted the network namespace; the following IPv4 PTB server correctly refused that cross-TUN durable identity.
- The harness now enters the network namespace before creating a private mount namespace, bind-mounts one evidence-backed directory over `/run`, and only then drops to `nobody` for the permission case. Ordinary rollback cases remain shared within the harness, while the adversarial owner record is required to exist inside the isolated runtime artifact and cannot contaminate subsequent CI jobs.
- Product cleanup semantics are unchanged: no cross-TUN owner check, generation check, or fail-closed rollback contract was weakened. Native execution remains a hosted Linux gate; local Bash syntax, ShellCheck, documentation truth, runtime guardrails, and diff hygiene own the non-privileged verification slice.

## ChaCha20 x16 Hosted Clippy Closure (2026-08-11, TODO-562)

- Hosted Clippy Matrix job `93609917539` failed only because the AVX-512 parity regression indexed its fixed returned block array with a range-loop counter. The test now enumerates the returned blocks directly and retains the same scalar wrapping-counter comparison for all sixteen lanes.
- The exact workspace all-target `rust-tests` Clippy command passes with `-D warnings`. No lint suppression, runtime implementation, frontend, or Tauri surface changed.

## io_uring Hosted Clippy Closure (2026-08-11, TODO-562)

- Hosted Clippy Matrix job `93609917519` exposed seven profile-only diagnostics: two indexed disposition loops, one over-wide inbound receive signature, one redundant cfg wrapper, one single-pattern match, one needless return, and the public `BatchSendResult::len` method without `is_empty`.
- Client and server batch paths now enumerate exact mutable disposition slots. `UringInboundRuntime` owns the receive ring and event bridge as one lifecycle unit, reducing the inbound function boundary without hiding parameters. Server batching and teardown use direct control flow, and `BatchSendResult` exposes and tests `is_empty` alongside `len`.
- The exact workspace all-target `io_uring` Clippy profile passes with warnings denied on the local host. The installed `x86_64-unknown-linux-gnu` cross target stops in `sha2-asm` before product code because `x86_64-linux-gnu-gcc` is unavailable; hosted Linux CI remains the authoritative native confirmation. No frontend or Tauri surface changed.

## Windows Engine Reload Fixture Portability (2026-08-11, TODO-562)

- Hosted Windows core job `93605231541` failed the reload regression before exercising product behavior because Rust's `::`-delimited test thread name was embedded directly in the temporary fixture filename, which Windows rejected with error 123.
- The singular reload regression now uses a stable PID-scoped `file-reload` suffix. Its valid replacement, invalid candidate, missing candidate, and state-preservation assertions are unchanged.
- The focused reload test passes `1/1` locally. Hosted Windows remains the authoritative platform confirmation; no frontend or Tauri surface changed.

## x86 ACK-Merge Parity Inference Closure (2026-08-11, TODO-562)

- Hosted Clippy Matrix job `93613464511` failed the `tun-tests,rust-tests` profile only because the x86-only parity target left two generic constructors ambiguous: the scalar merge output vector and the AVX-512 random generator.
- The target now declares the existing `(u64, u64)` output element type and constructs the already imported `StdRng` directly. Range generation, fixed seed, SIMD feature guards, scalar oracle, loop coverage, and parity assertions are unchanged.
- The exact workspace all-target `tun-tests,rust-tests` Clippy profile passes with warnings denied locally. Because the x86 body is cfg-excluded on ARM64 and the installed Linux cross target lacks `x86_64-linux-gnu-gcc`, hosted x86 remains the authoritative compilation confirmation. No frontend or Tauri surface changed.

## Windows Resolver Object Identity Closure (2026-08-11, TODO-562)

- Hosted Windows core job `93605231541` exposed four resolver restore failures because the non-Unix fallback identified files by mutable length and last-modified time. A legitimate in-place managed write therefore looked like an object replacement before ownership publication or restore.
- Windows now queries `GetFileInformationByHandle` and stores the stable volume serial plus complete 64-bit file index in the existing object-identity fields. Symlink path capture opens the reparse point while target capture follows it, preserving the same path-versus-target contract as Unix `dev+ino`; content ownership and backup digest checks remain mandatory and unchanged.
- The portable resolver suite passes `15/15`. A standalone Windows-MSVC metadata compile proves the exact Win32 signatures and types; the complete root cross-check still stops in `ring` before product code because the host lacks the Windows C sysroot. Hosted Windows remains the authoritative runtime confirmation. No frontend or Tauri surface changed.

## ChaCha20 x4 Integration Namespace Closure (2026-08-11, TODO-562)

- Hosted Clippy Matrix job `93614789702` reached the x86-only ChaCha20 x4 parity target and found two stale references to the compatibility `optimize::crypto` namespace, which intentionally re-exports only the public x4 block function and not the test-only dispatch override.
- Override setup, RAII cleanup, and block generation now use the canonical `optimize::simd::crypto` owner. Seeds, backends, runtime feature guards, wrapping counter oracle, telemetry assertions, and production exports are unchanged.
- Exact local workspace all-target `rust-tests` Clippy remains the deterministic host gate; the body is cfg-excluded on ARM64, so hosted x86 remains the authoritative compilation and execution confirmation. No frontend or Tauri surface changed.

## io_uring Worker Completion Preservation (2026-08-11, TODO-562)

- Hosted Linux fastpath job `93605231576` passed direct connected and unconnected io_uring sends but failed the runtime-owned worker after it reported a successful kernel completion set as `0/2` and quarantined the ring.
- The controlled poll path used `CompletionQueue::count`, which consumes the iterator and advances the shared completion head on drop. It therefore acknowledged every ready CQE before the disposition reaper could inspect it. The worker now uses the non-consuming `ExactSizeIterator::len` observation and leaves all CQEs owned by the existing strict reaper.
- Direct sender submission, deadlines, shutdown checks, quarantine behavior, exact per-packet disposition accounting, and the Linux kernel regression are unchanged. The exact local workspace all-target `io_uring` Clippy profile, formatting, documentation truth, runtime guardrails, diff hygiene, and protected-path check pass; hosted Linux owns kernel-backed confirmation. No frontend or Tauri surface changed.

## x86 Benches TLS-Padding Import Ownership (2026-08-11, TODO-562)

- Hosted Clippy Matrix job `93617134272` failed only because `add_tls_padding_avx2` compiled for the benches feature while its `_mm256_set1_epi8`, `_mm256_storeu_si256`, and `__m256i` imports were scoped to tests and rust-tests.
- The AVX2 helper now owns its x86 intrinsic import locally, matching the helper's exact architecture and feature boundary without widening the module-level import or runtime API.
- The exact local workspace all-target benches Clippy profile passes with warnings denied. The body is cfg-excluded on ARM64, so hosted x86 remains the authoritative compilation confirmation. Dispatch, target-feature proof, byte output, fallback behavior, frontend, and Tauri surfaces are unchanged.

## nftables Owned Set-Rule Verification (2026-08-11, TODO-562)

- Hosted native PTB artifact `server-ipv4-ptb-native` from CI run `31438761246` proved that server startup reached nftables ownership verification and then rejected its own fanout rule. The required fragment ended at `accept`, while the real owned rule correctly continued with `comment "quicfuscate-owner-..."`.
- Set-rule matching still requires an exact normalized prefix, exact order-independent set membership, and a token-bounded required suffix. It now permits legitimate trailing nftables metadata after that suffix instead of requiring the complete listed line to end there.
- Focused qf-firewall matcher regressions pass `3/3`, including canonicalized set order, differing-member rejection, and the owned-comment case. Strict qf-firewall all-target/all-feature Clippy passes. No rule generation, ownership marker, cleanup policy, frontend, or Tauri surface changed; hosted Linux owns the native PTB rerun.

## Native TUN Evidence Upload Ownership (2026-08-11, TODO-562)

- CI run `31438761246` retained the isolated TUN provisioning artifact as designed, but `actions/upload-artifact` could not traverse the root-created routing directory and reported `EACCES` while scanning `/tmp/quicfuscate-tun-provisioning.*`.
- The Linux traffic job now discovers only the fixed `mktemp` evidence prefix with Bash `nullglob` and recursively transfers those retained trees to the unprivileged runner immediately after the privileged harness. The normalization step runs under `always()`, so both successful and failing proof artifacts remain uploadable.
- Harness runtime modes, isolated `/run`, fail-closed owner retention, cleanup semantics, and product code are unchanged. The Bash block parses, workflow YAML parses, documentation truth and diff hygiene pass; hosted Linux owns end-to-end artifact confirmation. No frontend or Tauri surface changed.

## MASQUE Downlink Queue Workspace Ownership (2026-08-10, TODO-562)

- `crates/qf-transport-types/src/masque.rs` now owns the bounded `MasqueDownlinkQueue` FIFO and `MasqueDownlinkQueueReject` admission contract, including packet-count and byte-budget enforcement, FIFO order, byte accounting, and discard accounting.
- `src/core_parts/connection.rs` preserves the historical root paths through a compatibility re-export. Server DNS interception, live-auth routing, metrics, and inline backend tests consume the child type directly, removing queue-specific implementation-to-core references without changing concrete connection ownership.
- The queue child is root-independent and has no frontend or Tauri surface. The 9-module product SCC remains expected because `implementations` still depends on concrete `QuicFuscateConnection` behavior in `core`; this slice does not claim the larger cycle is resolved.
- Validation is complete: qf-transport-types `22/22` tests and strict Clippy, qf-fec `81/81` tests, qf-transport-cc `94/94` tests and strict Clippy, and root core `33/33` pass. The workspace all-target `rust-tests` suite reports `118` result blocks, `3,067` passed, `0` failed, and `6` ignored; workspace strict Clippy, `cargo check --workspace --all-features`, and all-feature library/binary Clippy pass.
- All-target check and Clippy remain explicitly platform-blocked only at `scripts/tests/rust/rt-io-hotpath-kernel-integration.rs:4` and `scripts/tests/rust/rt-transport-uring.rs:8`, both Linux-only guards on macOS ARM64. Runtime guardrails are green with `Critical: 0` and `Warnings: 0` at `scripts/out/audits/runtime-guardrails-20260810T-connection-stats-final/audit-runtime-guardrails.log`.
- Seam evidence is `scripts/out/audits/workspace-seams-20260810T-connection-stats-final/workspace-seams.json`: `35` packages, `317` Rust files, `205,846` source lines, `127` module edges, `96` Cargo workspace dependency edges, unchanged 9-module product SCC, `implementations -> core` at three references, and `protected_changes=[]`.
- Release verification passes with `--help` exit `0`, binary size `9,989,968` bytes, SHA-256 `32cb41223f784570f8be1f09e9dd251957f1f51e563764ae1672e2b232be2caa`, and target/free-space `11,448,400 / 9,508,052 KiB`, below the `12,582,912 KiB` cleanup threshold and above the `2,097,152 KiB` floor. Frontend and Tauri paths remain untouched.

## FEC, MASQUE Handler, and Connection-Statistics Contract Ownership (2026-08-10, TODO-562)

- `qf-fec` now owns `ActiveFecPolicyChange`; `qf-transport-types` owns the MASQUE callback aliases; and `qf-transport-cc` owns `ConnectionStats` and its bounded congestion projection. Root compatibility re-exports preserve historical paths while removing three root-only contract implementations.
- The qf-transport-cc dependency remains one-way through qf-cpu and qf-transport-types. The larger product SCC remains open because concrete connection ownership still crosses `implementations -> core`; no frontend projection is part of this backend work.

## TUN and Fastpath Contract Workspace Ownership (2026-08-10, TODO-562)

- `crates/qf-transport-types/src/tun.rs` now owns the root-independent TUN constants, `TunError`, `TunConfig`, address/MTU validation, `TunCapabilities`, `TunReadContract`, `TunDevice`, and `TunFactory`. `crates/qf-transport-types/src/fastpath.rs` owns `FastpathMode` and captured-environment parsing.
- `src/interface.rs` preserves every historical root contract path through compatibility re-exports while retaining `TunPacket`, `TunInterface`, pooled I/O, platform backends, factory registration, capability probes, telemetry, and shutdown lifecycle. No frontend or Tauri field/API projection is required.
- The child imports no root runtime, connection, engine, FEC, or stealth implementation. The contract move preserves public signatures and wire behavior.
- Validation is green: qf-transport-types `27/27` all-target/all-feature tests and strict Clippy; the complete workspace all-target `rust-tests` suite reports `118` result blocks, `3,072` passed, `0` failed, and `6` ignored; strict workspace `rust-tests` Clippy, `cargo check --workspace --all-features`, all-feature library/binary Clippy, and the root library `rust-tests` filter `1,659/1,659` pass with `RUSTFLAGS=-Dwarnings` where applicable.
- The complete all-target check exits only at `scripts/tests/rust/rt-io-hotpath-kernel-integration.rs:4`; the all-target Clippy lane is bounded by the repository-owned Linux-only guards at `scripts/tests/rust/rt-io-hotpath-kernel-integration.rs:4` and `scripts/tests/rust/rt-transport-uring.rs:8` on macOS ARM64. No guard was weakened.
- Runtime guardrails are green with `Critical: 0` and `Warnings: 0` at `scripts/out/audits/runtime-guardrails-20260810T-tun-contract-final/audit-runtime-guardrails.log`. Seam evidence is `scripts/out/audits/workspace-seams-20260810T-tun-contract-final/workspace-seams.json`: `35` packages, `319` Rust files, `205,949` source lines, `126` module edges, `96` Cargo workspace dependency edges, unchanged 9-module product SCC, and `protected_changes=[]`.
- Release verification passes with `RUSTFLAGS=-Dwarnings cargo build --release --bin quicfuscate --locked --offline`; `target/release/quicfuscate --help` exits `0`, the binary is `9,990,080` bytes, and SHA-256 is `d1c8fdfb35a5936296ba4b3f20efa75440d8477152458a30de79953766ba99a2`. The final target guard records `3,106,692 KiB` with `20,537,516 KiB` free, below the `12,582,912 KiB` cleanup threshold and above the `2,097,152 KiB` build floor. Frontend and Tauri paths remain untouched and no frontend field/API projection is required by this backend slice.

## Transport Packet Protocol Contract Ownership (2026-08-10, TODO-562)

- `crates/qf-transport-types/src/protocol.rs` now owns the root-independent `Epoch`, `PacketType`, decoded transport `Header`, and `TransportError` contracts. The root `transport` namespace preserves the historical `Epoch`, `PacketType`, `Header`, and `Error` paths through compatibility re-exports.
- The child has no connection, packet I/O, crypto, FEC, stealth, engine, implementation, frontend, or Tauri dependency. The separate `transport::packet::Header` parser adapter and all wire behavior remain unchanged.
- `src/transport/packet/headers.rs` now physically owns the packet namespace's public `Header` model, packet-number encoding, and header parse/format functions; `src/transport/packet.rs` remains the compatibility aggregate and retains packet protection, connection setup, AEAD, Retry/VN generation, and CRYPTO-stream integration. The root exports and wire behavior remain unchanged.

## qf-crypto AEGIS Adapter Split (2026-08-13, TODO-562)

- `crates/qf-crypto/src/aegis.rs` retains the AEGIS state machines, wrappers, and algorithm core; private `crates/qf-crypto/src/aegis/batch.rs` owns the `AeadSeal`/`AeadOpen` single-packet and batch adapters. Public paths and private-policy selection remain unchanged, and normal rustls packet protection is still the live owner.
- qf-crypto all-feature tests pass `144/144`, strict all-target Clippy passes, formatting/diff hygiene pass, and the module structure gate remains free of oversized files and textual assembly. This is an internal ownership cut only; it does not activate AEGIS/MORUS.
- This backend-only move requires no frontend field/API projection. qf-transport-types all-target/all-feature tests pass `30/30` with strict Clippy, the root library `rust-tests` filter passes `1,659/1,659`, and the complete workspace all-target `rust-tests` suite reports `118` result blocks, `3,072` passed, `0` failed, and `6` ignored; strict workspace `rust-tests` Clippy, `cargo check --workspace --all-features`, and all-feature library/binary Clippy pass as well. The complete all-target check/Clippy lanes remain blocked only by the Linux-only guards at `scripts/tests/rust/rt-io-hotpath-kernel-integration.rs:4` and `scripts/tests/rust/rt-transport-uring.rs:8` on macOS ARM64.
- Runtime guardrails remain green with `Critical: 0` and `Warnings: 0` at `scripts/out/audits/runtime-guardrails-20260810T-protocol-final/audit-runtime-guardrails.log`. The fresh seam audit at `scripts/out/audits/workspace-seams-20260810T-protocol-final/workspace-seams.json` records `35` packages, `320` Rust files, `205,975` source lines, `126` module edges, `96` Cargo workspace dependency edges, the unchanged 9-module product SCC, and `protected_changes=[]`.

## qf-crypto MORUS State-Operations Split (2026-08-13, TODO-562)

- `crates/qf-crypto/src/morus.rs` retains MORUS state-transition kernels and runtime backend dispatch. Private `crates/qf-crypto/src/morus/state_ops.rs` owns state initialization, keystream/finalization, associated-data processing, block encrypt/decrypt, and partial-block I/O helpers. Existing public APIs, algorithms, SIMD selection, and activation boundaries are unchanged.
- Verification passes qf-crypto all-feature library tests `144/144`, strict qf-crypto all-target/all-feature Clippy, workspace `cargo check --all-features`, formatting, `git diff --check`, and the Rust module structure audit (`383` files, no oversized file, no include assembly). No frontend or Tauri path was changed.
- Release verification passes with `RUSTFLAGS=-Dwarnings CARGO_BUILD_JOBS=2 cargo build --release --bin quicfuscate --locked --offline`; `target/release/quicfuscate --help` exits `0`, the binary is `9,990,080` bytes, and SHA-256 is `7e7e9cb010ecf8ad973d0d0a99cb55d068a5fe8f2d90d7881cff2f67909b38df`. The final target guard records `8,485,768 KiB` with `13,148,664 KiB` free, below the `12,582,912 KiB` cleanup threshold and above the `2,097,152 KiB` build floor. Frontend and Tauri paths remain untouched and no frontend field/API projection is required.

## Engine Configuration Contract Workspace Ownership (2026-08-10, TODO-562)

- `crates/qf-engine-types/src/lib.rs` now owns the root-independent engine lifecycle/error/event/statistics contracts plus `EngineSection`, `SecurityConfig`, `OptimizationConfig`, `TransportConfig`, `ConnectionConfig`, `InterfaceConfig`, the canonical client-tunnel address model, and structured engine command/result contracts, including validation and runtime projections. `crates/qf-engine-types/src/qkey.rs` physically owns `QKeyConfig`, zeroizing `QKeyToken`, checksum validation, and the bounded QKey codec boundary; `lib.rs` remains the aggregate/re-export surface. `src/engine/config.rs` retains the aggregate `EngineConfig`, `StealthSection`, concrete runtime adapters, builder, and root `ConfigError`; `src/engine/engine.rs` and `src/engine/qkey.rs` retain orchestration and QKey encoding/registry behavior while reexporting child contracts.
- The child has one-way backend dependencies on `qf-common`, `qf-cpu`, `qf-firewall`, `qf-fec`, `qf-memory-lock`, `qf-transport-cc`, `qf-transport-recovery`, `qf-transport-types`, and `qf-transport-version`, plus `log`, `serde`, `sha2`, and `sysinfo`. No frontend or Tauri dependency crosses the seam, and no concrete connection/transport implementation was moved into the child.
- After the QKey ownership cut, qf-engine-types all-feature library tests pass `71/71`; strict child Clippy, workspace all-feature checking, formatting, diff hygiene, and the Rust module-structure gate pass (`379` files, `0` oversized, `0` include-assembly). Root compatibility paths and QKey checksum/serialization behavior remain unchanged. All frontend/Tauri paths remain unchanged.
- The complete `--all-features --all-targets` check and Clippy lanes remain intentionally unavailable on macOS ARM64 and stop at `scripts/tests/rust/rt-io-hotpath-kernel-integration.rs:4`, whose compile error explicitly requires Linux. The all-feature library/binary lanes pass; no platform guard was weakened.
- The runtime guardrail audit is fully green with `Critical: 0` and `Warnings: 0` at `scripts/out/audits/runtime-guardrails-20260810T-engine-config-final-final/audit-runtime-guardrails.log`. Its interface and memory-lock owner probes were updated to the new child locations after verification; no runtime guard was weakened.
- Fresh seam evidence is `scripts/out/audits/workspace-seams-20260810T-engine-config-final/workspace-seams.json`: source revision `d69e10446c92be684346387d613c28340f18b25f`, `36` packages, `322` Rust files, `206,215` source lines, `125` module edges, `106` workspace dependency edges, unchanged 9-module product SCC, and `protected_changes=[]`.
- Release verification passes with `RUSTFLAGS=-Dwarnings CARGO_BUILD_JOBS=2 cargo build --release --bin quicfuscate --locked --offline`; `target/release/quicfuscate --help` exits `0`, the binary is `9,991,520` bytes, and SHA-256 is `063730bf0d56250b09b3c9098c36517c0064a38e85b58e52e397d6016672e604`. The initial cargo-clean requirement was fulfilled before this slice. Target usage remained below `12,582,912 KiB` throughout the final gates, so no threshold-triggered clean was required; the final guard recorded `11,021,556 KiB` with `10,553,436 KiB` free.
- TODO-562 remains blocked for the coordinated 9-module product-core split and external Linux/native/CI acceptance gates. This contract slice does not claim to dissolve the cyclic runtime implementation. No frontend or Tauri field/API projection is required; protected frontend paths remain untouched.

## Engine Contract Workspace Ownership (2026-08-10, TODO-562)

- `crates/qf-engine-types/src/lib.rs` now owns the engine `EngineMode`/`StealthMode` mode enums, serialized `EngineSection` configuration and validation, the engine-facing `FecMode` adapter, the engine-facing `FecSection` runtime projection and `FecSectionError`, lifecycle state, typed data-plane faults/errors, events/callbacks, atomic runtime statistics, immutable snapshots, disconnect reasons, and the root-independent `ConfigError` contract plus its `EngineError` conversion. `src/engine/engine.rs` and `src/engine/config.rs` re-export the child contracts and retain concrete orchestration and configuration-owned command types.
- The child depends on the root-independent `qf-fec` contract, `serde`, and the standard library and has no client/server runtime, TUN, transport implementation, stealth, frontend, or Tauri dependency. No frontend field/API projection is required.
- qf-engine-types all-target/all-feature tests pass `16/16` with strict Clippy; the root `engine::config` filter passes `38/38`; the complete workspace all-target `rust-tests` suite exits `0`; workspace strict Clippy and all-feature library/binary Clippy pass.
- The all-target check and Clippy lanes remain macOS ARM64 platform-blocked at the unchanged repository-owned Linux guards `scripts/tests/rust/rt-io-hotpath-kernel-integration.rs:4` and `scripts/tests/rust/rt-transport-uring.rs:8`. Runtime guardrails report `Critical: 0` and `Warnings: 0` at `scripts/out/audits/runtime-guardrails-20260810T-fec-section-final/audit-runtime-guardrails.log`.
- Fresh seam evidence is `scripts/out/audits/workspace-seams-20260810T-fec-section-final/workspace-seams.json`: `36` packages, `321` Rust files, `206,097` source lines, `126` module edges, `98` workspace dependency edges, unchanged 9-module product SCC, and `protected_changes=[]`.
- Release verification passes with `RUSTFLAGS=-Dwarnings CARGO_BUILD_JOBS=2 cargo build --release --bin quicfuscate --locked --offline`; `target/release/quicfuscate --help` exits `0`, size is `9,990,688` bytes, SHA-256 is `3ede1c864adbbe8d8e06d8f46c9ab034f9ba5ddd758b159d83d67ccba59f08b3`, and the target guard records `2,827,664 KiB` with `20,052,236 KiB` free. No frontend or Tauri field/API projection is required.

## Stealth Configuration Workspace Ownership (2026-08-10, TODO-562)

- `crates/qf-stealth/src/stealth_config.rs` now owns `StealthConfig`, its mode constructors, rotation projections, TOML/file parsing, validation, immutable environment-snapshot projection, transport overrides, compression-policy propagation, MASQUE overrides, and the legacy `FecMode` value contract. `src/stealth/mod.rs` preserves the historical root paths as direct re-exports and no longer includes a root implementation.
- The child consumes `EnvSnapshot` from `qf-common` and `CompressionPolicy` from `qf-compress` through one-way workspace edges. Root-only transport and manager behavior remains outside the child; compatibility methods are public but documentation-hidden so the root adapter can call the canonical owner without duplicating policy.
- Verification passes qf-stealth `116/116`, root `cargo check --all-features`, the complete root all-feature library suite `1,697/1,697`, workspace library/binary/example strict Clippy, qf-stealth all-target strict Clippy, formatting, and diff hygiene. The unrestricted workspace all-target Clippy lane stops only at the unchanged Linux-only `compile_error!` in `scripts/tests/rust/rt-io-hotpath-kernel-integration.rs:4` on macOS ARM64; no platform guard was weakened.
- The final disk guard records `9,083,324 KiB` target usage and `13,785,588 KiB` free, below the `12,582,912 KiB` cleanup threshold and above the `2,097,152 KiB` build floor. Protected frontend/Tauri paths remain untouched and no frontend field/API projection is required.
- Post-push seam evidence is `scripts/out/audits/workspace-seams-20260810T-stealth-config-postpush/workspace-seams.json` at source revision `da1160d215e885deffe0b0f1edfcef063722727c`: `36` workspace packages, `332` Rust files, `206,915` source lines, `124` module edges, `114` workspace dependency edges, the unchanged 9-module product SCC, and `protected_changes=[]`.

## TLS Cover Material Workspace Ownership (2026-08-10, TODO-562)

- `crates/qf-stealth/src/tls_cover.rs` now owns fresh OS-entropy acquisition and domain-separated HKDF derivation for TLS Cover key/IV material. The profile and client/server role remain explicit derivation domains; intermediate entropy, PRK, and expanded material are zeroized.
- `src/stealth/parts/tls_cover_provider.rs` retains only the `ConnectionError` compatibility mapping, root `CryptoContext` installation, sequence lifecycle, frame construction, and encryption. The root `stealth` module no longer imports root HKDF functions.
- Verification passes qf-stealth `118/118`, the complete root all-feature library suite `1,697/1,697`, strict workspace library/binary/example Clippy, qf-stealth all-target strict Clippy, formatting, and diff hygiene. The final disk guard records `10,040,504 KiB` target usage and `12,687,208 KiB` free, below the cleanup threshold and above the build floor.
- Protected frontend/Tauri paths remain untouched and no frontend field/API projection is required.
- Post-push seam evidence is `scripts/out/audits/workspace-seams-20260810T-tls-cover-material-postpush/workspace-seams.json` at source revision `7ce31037e65d4c2f4006c4fba2c2d875ee986b43`: `36` workspace packages, `332` Rust files, `206,956` source lines, `123` module edges, `115` workspace dependency edges, the unchanged 9-module product SCC, and `protected_changes=[]`.

## TLS Cover Record-Plan Workspace Ownership (2026-08-10, TODO-562)

- `crates/qf-stealth/src/tls_cover.rs` now owns bounded synthetic record planning: role/performance payload sizing, optional padding, profile tagging, TLS header length encoding, random plaintext, and immutable-snapshot jitter selection. `TlsCoverRecordPlan` transfers the header, plaintext, and optional delay without importing root crypto state.
- `src/stealth/parts/tls_cover_provider.rs` now consumes the plan, performs root `CryptoContext` encryption/sequence mutation, assembles ciphertext, and applies the dedicated synchronous delay. The move also fixes a latent large-cap bug: padding headroom is bounded by the TLS `u16` ciphertext limit rather than only caller capacity, so a large `max_len` plus padding override cannot truncate the record length field.
- Verification passes qf-stealth `121/121`, the complete root all-feature library suite `1,697/1,697`, strict workspace library/binary/example Clippy, qf-stealth all-target strict Clippy, formatting, and diff hygiene. The final disk guard records `10,640,032 KiB` target usage and `12,040,172 KiB` free, below the cleanup threshold and above the build floor.
- Protected frontend/Tauri paths remain untouched and no frontend field/API projection is required.
- Post-push seam evidence is `scripts/out/audits/workspace-seams-20260810T-tls-cover-record-plan-postpush/workspace-seams.json` at source revision `cd56daf80daa5a3c71ead888c46daa4534ddb557`: `36` workspace packages, `332` Rust files, `207,009` source lines, `123` module edges, `115` workspace dependency edges, the unchanged 9-module product SCC, and `protected_changes=[]`.

## Intelligent Stealth Policy Workspace Ownership (2026-08-10, TODO-562)

- `crates/qf-stealth/src/intelligent_policy.rs` owns `IntelligentStealthInputs` and the root-independent Intelligent-mode signal-to-transport-policy mapping. The root manager keeps only environment capture and its historical associated-function adapter.
- Verification passes qf-stealth `124/124`, root all-feature checking and library tests `1,697/1,697`, strict workspace library/binary/example Clippy, qf-stealth all-target strict Clippy, formatting, and diff hygiene.
- Protected frontend/Tauri paths remain untouched and no frontend field/API projection is required. TODO-562 remains blocked for the coordinated 9-module product SCC and external Linux/native/CI gates.
- Post-push seam evidence is `scripts/out/audits/workspace-seams-20260810T-intelligent-stealth-policy-postpush/workspace-seams.json` at source revision `cfa3ee80529fd9c716b23e075a8436d3567021d0`: `36` packages, `333` Rust files, `207,092` source lines, `123` module edges, `115` workspace dependency edges, unchanged 9-module product SCC, and `protected_changes=[]`.

## Runtime Policy Generation Workspace Ownership (2026-08-10, TODO-562)

- `crates/qf-engine-types/src/lib.rs` now owns the shared runtime-policy publication generation and its saturating, clone-shared read/write gate. The stealth runtime keeps its historical crate-private path through a compatibility re-export, while server code consumes the engine contract directly.
- Verification passes qf-engine-types `19/19`, root all-feature checking and library tests `1,697/1,697`, strict child/workspace Clippy, formatting, and diff hygiene.
- Protected frontend/Tauri paths remain untouched and no frontend field/API projection is required. TODO-562 remains blocked for the coordinated 9-module product SCC and external Linux/native/CI gates.
- Post-push seam evidence is `scripts/out/audits/workspace-seams-20260810T-runtime-policy-generation-postpush/workspace-seams.json` at source revision `ce245cd6841af63d87a658e4ad0640fc35a92ec3`: `36` packages, `333` Rust files, `207,120` source lines, `123` module edges, `115` workspace dependency edges, unchanged 9-module product SCC, and `protected_changes=[]`.

## AppConfig Engine Ownership (2026-08-10, TODO-562)

- `src/engine/app_config.rs` now owns the validated runtime projection that was incorrectly nested under the TUN interface module. `interface::app_config` and the root `app_config` export remain compatible, while internal engine/server callers use the canonical engine path.
- Verification passes the focused AppConfig tests `2/2`, root all-feature checking and library tests `1,697/1,697`, strict workspace Clippy, formatting, and diff hygiene.
- Protected frontend/Tauri paths remain untouched and no frontend field/API projection is required. TODO-562 remains blocked for the measured product SCC and external Linux/native/CI gates.
- Public `rt-interface` compatibility passes `4/4`; strict root Rustdoc passes. Post-push seam evidence is `scripts/out/audits/workspace-seams-20260810T-app-config-engine-postpush/workspace-seams.json` at source revision `31ade03c9af03d0d1a4bff711efda6b4a685ea39`: `36` packages, `334` Rust files, `207,119` lines, `120` module edges, `115` dependency edges, unchanged 9-module SCC, and `protected_changes=[]`. The move removes `interface -> fec` and `interface -> stealth`; only the compatibility re-export retains `interface -> engine`.

## QFTLS Stealth Contract Dependency (2026-08-10, TODO-562)

- `src/qftls.rs` now imports the already extracted profile/persona contracts directly from `qf-stealth`; the public conversion function and compatibility paths are unchanged.
- The root `TlsCoverProvider` references remain intentional because the adapter owns root crypto installation and implements the root TLS trait. No concrete runtime owner crosses into the child.
- Focused all-feature library conversion tests pass `4/4`; the first unbounded test command stopped only at the unchanged Linux-only integration guard at `scripts/tests/rust/rt-io-hotpath-kernel-integration.rs:4` on macOS ARM64. No guard or test was weakened.
- Root all-feature checking and library tests `1,697/1,697`, strict workspace library/binary/example Clippy, formatting, documentation truth, and diff hygiene pass. The final pre-commit guard records `12,574,980 KiB` target usage and `10,011,368 KiB` free, still below the `12,582,912 KiB` cleanup trigger and above the build floor.
- Protected frontend/Tauri paths remain untouched and no frontend field/API projection is required.
- Post-push seam evidence is `scripts/out/audits/workspace-seams-20260810T-qftls-stealth-contract-postpush/workspace-seams.json` at source revision `3af4eec3fc1653f5a77d600dd32aaf20bd582301`: `36` packages, `334` Rust files, `207,104` source lines, `120` module edges, `115` dependency edges, unchanged 9-module SCC, and `protected_changes=[]`. The measured `qftls -> stealth` edge is reduced from `34` references to the `2` concrete `TlsCoverProvider` adapter references.

## TLS Profile Conversion Workspace Ownership (2026-08-10, TODO-562)

- The root-independent fingerprint-to-TLS conversion and its owner tests move from `src/qftls.rs` to `crates/qf-stealth/src/tls_profile.rs`; qftls keeps direct public re-exports.
- Root stealth manager conversion calls now target qf-stealth directly, and TLS Cover level signatures use qf-transport-types directly. `rg` finds no remaining `crate::qftls` or `qftls::` reference under `src/stealth/`.
- Verification passes qf-stealth `127/127`, root all-feature check and library `1,693/1,693`, public qftls profiles `4/4`, strict child/workspace Clippy, strict child Rustdoc, formatting, and diff hygiene. The required threshold clean removed `77,696` files and `15.1 GiB` after `target/` reached `12,718,120 KiB`; final target/free space is `2,537,148 / 20,029,300 KiB`.
- Protected frontend/Tauri paths remain untouched and no frontend field/API projection is required.
- Post-push seam evidence is `scripts/out/audits/workspace-seams-20260810T-tls-profile-conversion-postpush/workspace-seams.json` at source revision `be2553865781c272e2f6bb3eb901464f6d556ddc`: `36` packages, `334` Rust files, `207,079` source lines, `119` module edges, `115` dependency edges, unchanged 9-module SCC, and `protected_changes=[]`. `stealth -> qftls` is absent; `qftls -> stealth` remains with exactly `2` concrete cover-adapter references.

## Brain Intelligent-Policy Dependency (2026-08-10, TODO-562)

- `src/brain.rs` now consumes the child-owned intelligent-policy function and input contract directly instead of calling the root `StealthManager` adapter. Environment capture and the full signal projection remain behaviorally identical.
- Manager compatibility helpers and the root input re-export are test-only. Focused Brain tests pass `65/65`; root all-feature checking, strict workspace Clippy, formatting, and diff hygiene pass. Final target/free space is `3,931,084 / 18,602,192 KiB`.
- No frontend or Tauri path or field projection is involved.
- Post-push seam evidence is `scripts/out/audits/workspace-seams-20260810T-brain-intelligent-policy-postpush/workspace-seams.json` at source revision `570223f68b3ef369ded291779355bc689b2fa765`: `36` packages, `334` Rust files, `207,083` source lines, `118` module edges, `115` dependency edges, unchanged 9-module SCC, and `protected_changes=[]`. The direct `brain -> stealth` edge is gone; `brain -> transport` remains concrete at `4` references.

## Transport Stealth-Contract Dependency (2026-08-10, TODO-562)

- The five transport references to child-owned fingerprint and traffic-analysis contracts now use qf-stealth directly. No concrete stealth runtime ownership moves.
- Focused transport-policy tests pass `25/25`; root all-feature checking, strict workspace Clippy, formatting, and diff hygiene pass. Final target/free space is `4,170,736 / 18,332,576 KiB`.
- No frontend or Tauri path or field projection is involved.
- Post-push seam evidence is `scripts/out/audits/workspace-seams-20260810T-transport-stealth-contract-postpush/workspace-seams.json` at source revision `1e0b186c254c4161f9b388b933eafc34c8df3bd0`: `36` packages, `334` Rust files, `207,083` source lines, `117` module edges, `115` dependency edges, unchanged 9-module SCC, and `protected_changes=[]`. The direct `transport -> stealth` edge is gone; the reverse concrete runtime edge remains at `15` references.

## Core Engine-Contract Dependency (2026-08-10, TODO-562)

- Root core now consumes canonical `qf-engine-types::{QKeyToken, FecMode}` contracts directly instead of routing five token paths and one fixture path through the concrete root engine namespace.
- Focused core tests pass `33/33`; root all-feature checking, strict workspace Clippy, formatting, and diff hygiene pass. Final target/free space is `4,171,964 / 18,309,144 KiB`.
- Token ownership, zeroization, constructor signatures, runtime engine orchestration, frontend, Tauri, and wire behavior remain unchanged.
- Post-push seam evidence is `scripts/out/audits/workspace-seams-20260810T-core-engine-contract-postpush/workspace-seams.json` at source revision `d68afdd41b75a9cc456a2ef32f6dc2ba2ab85a35`: `36` packages, `334` Rust files, `207,083` source lines, `116` module edges, `115` dependency edges, unchanged 9-module product SCC, and `protected_changes=[]`. The direct `core -> engine` edge is gone.

## Engine FEC-Contract Dependency (2026-08-10, TODO-562)

- Engine configuration aggregation, runtime projection, server profile construction, and live FEC policy translation now use canonical `qf-fec::{FecConfig, FecControlPolicy}` contracts directly. All nine `engine -> fec` compatibility references are removed.
- Focused engine tests pass `79/79`; root all-feature checking, strict workspace Clippy, formatting, and diff hygiene pass. Final target/free space is `4,172,140 / 18,257,012 KiB`.
- Runtime policy, configuration serialization, public compatibility paths, frontend, and Tauri behavior remain unchanged.
- Post-push seam evidence is `scripts/out/audits/workspace-seams-20260810T-engine-fec-contract-postpush/workspace-seams.json` at source revision `588f51d2fddd207f17f96c049f93e34c5a97a7d1`: `36` packages, `334` Rust files, `207,083` source lines, `115` module edges, `115` dependency edges, unchanged 9-module product SCC, and `protected_changes=[]`. The direct `engine -> fec` edge is gone.

## Engine Stealth-Contract Dependency (2026-08-10, TODO-562)

- Engine aggregation, validation, runtime projection, profile construction, and active-mode observation now use canonical `qf-stealth` contracts directly. All 39 `engine -> stealth` compatibility references are removed.
- Focused engine tests pass `79/79`; root all-feature checking, strict workspace Clippy, formatting, and diff hygiene pass. Final target/free space is `4,211,928 / 18,216,776 KiB`.
- Concrete manager ownership, runtime behavior, public compatibility paths, frontend, and Tauri behavior remain unchanged.
- Post-push seam evidence is `scripts/out/audits/workspace-seams-20260810T-engine-stealth-contract-postpush/workspace-seams.json` at source revision `65df4fe97c431d68b211de7b24dd8b203bdf564c`: `36` packages, `334` Rust files, `207,079` source lines, `114` module edges, `115` dependency edges, unchanged 9-module product SCC, and `protected_changes=[]`. The direct `engine -> stealth` edge is gone.

## FEC Engine-Mode Test Dependency (2026-08-10, TODO-562)

- Root FEC policy tests now consume canonical `qf-engine-types::FecMode` directly. Both `fec -> engine` test-only compatibility references are removed.
- The focused engine-mode policy test passes `1/1`; root all-feature checking, strict workspace Clippy, formatting, and diff hygiene pass. Final target/free space is `4,251,332 / 18,089,340 KiB`.
- Production FEC control, engine behavior, public compatibility paths, frontend, and Tauri behavior remain unchanged.
- Post-push seam evidence is `scripts/out/audits/workspace-seams-20260810T-fec-engine-mode-test-postpush/workspace-seams.json` at source revision `3f02bf7b1238358f09918ceafc7169f154d496ca`: `36` packages, `334` Rust files, `207,079` source lines, `113` module edges, `115` dependency edges, unchanged 9-module product SCC, and `protected_changes=[]`. The direct `fec -> engine` edge is gone.

## Transport Fastpath-Mode Test Dependency (2026-08-10, TODO-562)

- The root transport UDP-fastpath compatibility shim now consumes canonical `qf-transport-types::FastpathMode` directly. All three `transport -> interface` test-only references are removed.
- Focused UDP-fastpath compatibility tests pass `6/6`; root all-feature checking, strict workspace Clippy, formatting, and diff hygiene pass. Final target/free space is `4,248,376 / 18,006,276 KiB`.
- Production transport, interface, frontend, and Tauri behavior remain unchanged.
- Post-push seam evidence is `scripts/out/audits/workspace-seams-20260810T-transport-fastpath-test-postpush/workspace-seams.json` at source revision `4ec1fb9f072c68d2f85c626baa266eba66480d0a`: `36` packages, `334` Rust files, `207,079` source lines, `112` module edges, `115` dependency edges, and `protected_changes=[]`. `transport -> interface` is gone, the former 9-module product SCC is split into `brain/fec/qftls/stealth/transport` and `engine/implementations/interface`, and `core` is outside both SCCs.

## FEC Brain-Hint Test Dependency (2026-08-10, TODO-562)

- Root FEC observer tests now construct canonical `qf-fec::BrainFecHints` directly. The six lexical `fec -> brain` references were test-fixture indirections through `StealthBrain`; production behavior and public compatibility paths are unchanged.
- Focused runtime-hint tests pass `2/2`; root all-feature library tests pass `1,693/1,693`; all-feature checking, strict workspace Clippy, formatting, and diff hygiene pass. Final target/free space is `4,246,792 / 17,953,880 KiB`.
- No frontend or Tauri path or field projection is involved.
- Post-push seam evidence is `scripts/out/audits/workspace-seams-20260810T-fec-brain-hint-test-postpush/workspace-seams.json` at source revision `91cfe266f6f1ba2d1862d13bfcea749772dcb2ae`: `36` packages, `334` Rust files, `207,076` source lines, `111` module edges, `115` dependency edges, and `protected_changes=[]`. `fec -> brain` is gone, Brain is outside every product SCC, and the transport product SCC is reduced to `fec/qftls/stealth/transport`.

## Transport FEC-Observer Contract Dependency (2026-08-10, TODO-562)

- `qf-fec::FecObserver` now implements `qf-transport-types::TransportObserver` directly. Standalone transport constructors consume the child observer, while the root product adapter remains connection-owned for Brain delta application.
- The H3 environment-isolation test consumes the shared environment lock from the root environment compatibility path. No frontend or Tauri projection is involved.
- Verification passes qf-fec `82/82`, focused H3 isolation `1/1`, root all-feature library tests `1,693/1,693`, all-feature checking, strict child/workspace Clippy, formatting, and diff hygiene. Final target/free space is `5,894,708 / 16,234,368 KiB`.
- Post-push seam evidence is `scripts/out/audits/workspace-seams-20260810T-transport-fec-observer-postpush/workspace-seams.json` at source revision `4493a0de24ed9ffc8a147e2fc76f1400b9fc2936`: `36` packages, `334` Rust files, `207,115` source lines, `110` module edges, `116` dependency edges, and `protected_changes=[]`. `transport -> fec` is gone, FEC is outside every product SCC, and the remaining transport product SCC is `qftls/stealth/transport`; the new `qf-fec -> qf-transport-types` Cargo edge owns the observer callback contract explicitly.

## QFTLS Transport-Version Contract Dependency (2026-08-10, TODO-562)

- QFTLS now consumes version information, v1/v2 wire values, and the QUIC fixed bit from the canonical transport leaves. Root packet compatibility preserves `FIXED_BIT` as an exact alias.
- Only root `CryptoContext` ownership remains in the direct `qftls -> transport` edge. No frontend or Tauri projection is involved.
- Verification passes qf-transport-types `39/39`, focused QFTLS tests `20/20`, root all-feature library tests `1,693/1,693`, all-feature checking, strict child/workspace Clippy, formatting, and diff hygiene. Final target/free space is `6,432,156 / 15,625,616 KiB`.
- Post-push seam evidence is `scripts/out/audits/workspace-seams-20260810T-qftls-version-contract-postpush/workspace-seams.json` at source revision `85f0e3a8cc68a3b305d8ff4d952b4d367efe9c1f`: `36` packages, `334` Rust files, `207,102` source lines, `110` module edges, `116` dependency edges, and `protected_changes=[]`. `qftls -> transport` is reduced from `17` references to one concrete root `CryptoContext` import; SCC membership is otherwise unchanged.

## TLS Cover QFTLS Adapter Ownership (2026-08-10, TODO-562)

- The concrete TLS Cover root adapter now lives at `src/qftls/tls_cover_provider.rs` with its QFTLS/rustls composition owner. QFTLS consumes qf-stealth policy/material contracts directly and no longer imports the root stealth module.
- Existing stealth invariants use a test-only crate-internal compatibility projection. No frontend or Tauri projection is involved.
- Focused TLS Cover tests pass `25/25`; root all-feature library tests pass `1,693/1,693`; all-feature checking, strict workspace Clippy, formatting, and diff hygiene pass. Final target/free space is `6,660,308 / 15,375,788 KiB`.
- Post-push seam evidence is `scripts/out/audits/workspace-seams-20260810T-tls-cover-qftls-adapter-postpush/workspace-seams.json` at source revision `ddf9a582d1fcf41dc535c2a52ff8983e985051f5`: `36` packages, `334` Rust files, `207,104` source lines, `110` module edges, `116` dependency edges, and `protected_changes=[]`. `qftls -> stealth` is gone, Stealth is outside every product SCC, and the remaining TLS/transport SCC is `qftls/transport`.

## Transport TLS Contract Dependency (2026-08-10, TODO-562)

- Transport now consumes canonical `qf-stealth::TlsProfile` and `qf-transport-types::QuicEncryptionLevel` directly. Only the concrete QFTLS provider trait/factory ownership remains.
- No frontend or Tauri projection is involved.
- Focused TLS lifecycle tests pass `2/2`; root all-feature library tests pass `1,693/1,693`; all-feature checking, strict workspace Clippy, formatting, and diff hygiene pass. Final target/free space is `6,660,828 / 15,331,636 KiB`.
- Post-push seam evidence is `scripts/out/audits/workspace-seams-20260810T-transport-tls-contract-postpush/workspace-seams.json` at source revision `fc636f661a74eeaf4155864d2a775de4eaa049a9`: `36` packages, `334` Rust files, `207,120` source lines, `110` module edges, `116` dependency edges, and `protected_changes=[]`. `transport -> qftls` is reduced to `2` references; the remaining product SCCs are `qftls/transport` and `engine/implementations/interface`.

## TUN Factory Registry Workspace Ownership (2026-08-10, TODO-562)

- `qf-transport-types` now owns the process-wide external TUN factory registry and target capability projection. Root interface APIs remain compatibility wrappers, while engine control commands consume the canonical child contract directly.
- The production `engine -> interface` dependency is removed without changing TUN opening, factory invocation, frontend, or Tauri behavior.
- Verification passes qf-transport-types `40/40`, the root interface target `4/4`, root all-feature library tests `1,693/1,693`, all-feature checking, strict workspace Clippy, formatting, and diff hygiene. Final target/free space is `7,636,240 / 15,383,564 KiB`.
- Post-push seam evidence is `scripts/out/audits/workspace-seams-20260810T-tun-factory-registry-postpush/workspace-seams.json` at source revision `f5a1b25a537b54d38d941e8c3cab1b866de16a9c`: `36` packages, `334` Rust files, `207,156` source lines, `110` module edges, `116` dependency edges, and `protected_changes=[]`. The direct `engine -> interface` edge is absent; the three-module SCC remains through `engine -> implementations -> interface -> engine`.

## Interface Compatibility Dependency Isolation (2026-08-10, TODO-562)

- `interface::app_config` now projects through the root compatibility owner instead of importing engine internals directly. Windows-only native WFP tests consume a crate-root test bridge instead of introducing an interface-to-implementations product edge.
- Public compatibility, native WFP test semantics, frontend, and Tauri behavior remain unchanged.
- Verification passes the root interface target `4/4`, root all-feature library tests `1,693/1,693`, all-feature checking, strict workspace Clippy, formatting, and diff hygiene. Final target/free space is `7,834,776 / 13,686,852 KiB`.
- Post-push seam evidence is `scripts/out/audits/workspace-seams-20260810T-interface-compatibility-isolation-postpush/workspace-seams.json` at source revision `9d4bb39fa5f80c4f1bb1977e04331a4dfcf1a92e`: `36` packages, `334` Rust files, `207,161` source lines, `109` module edges, `116` dependency edges, and `protected_changes=[]`. Interface is outside every SCC; remaining product SCCs are `engine/implementations` and `qftls/transport`.

## Engine Configuration Workspace Ownership (2026-08-10, TODO-562)

- The aggregate `EngineConfig`, `StealthSection`, builder, TOML loading, normalization, validation, owner tests, and QKey projection move with Git history from `src/engine/config.rs` to `crates/qf-engine-types/src/config.rs`. The root file is reduced to compatibility exports plus the concrete-runtime `DataAeadConfig` adapter.
- Client and server implementations import `qf-engine-types::EngineConfig` directly. All six remaining `implementations -> engine` references are removed; only the concrete one-way `engine -> implementations` runtime edge remains.
- Verification passes qf-engine-types `58/58`, workspace all-feature checking, strict workspace Clippy, the exact admin-auth retry `1/1`, and the complete serial root all-feature library suite `1,655/1,655`. A parallel full run exposed the pre-existing global admin-auth test-state flake at `1,654/1,655`; no test or production behavior was weakened.
- Pre-commit seam evidence is `scripts/out/audits/workspace-seams-20260810T-engine-config-owner-dirty/workspace-seams.json` at source revision `7e74718cc89277eb1acd45423a406b859931f3c9`: `36` packages, `335` Rust files, `207,177` source lines, `107` module edges, `123` dependency edges, sole SCC `qftls/transport`, and `protected_changes=[]`. Final target/free space is `9,954,336 / 7,448,376 KiB`.
- Post-push seam evidence is `scripts/out/audits/workspace-seams-20260810T-engine-config-owner-postpush/workspace-seams.json` at source revision `b8c539c8b9458a974fe994b08e33c58aba0e4b19`; counts and edge topology are identical to the pre-commit report and protected changes remain empty.
- Serialized configuration, public compatibility paths, runtime behavior, frontend, and Tauri remain unchanged. No frontend field/API projection is required.

## QFTLS Packet-Key Installation Port (2026-08-10)

- QFTLS now owns outgoing Initial, Handshake, and application CRYPTO send streams and exposes their ACK, loss, and PTO operations through the provider contract. Transport retains packet scheduling, recovery decisions, and incoming CRYPTO reassembly.
- `QuicTlsKeyInstaller` replaces QFTLS knowledge of root `CryptoContext`. Rustls key changes are converted into complete Handshake and 1-RTT AEAD/header-protection bundles and installed synchronously by the transport-owned `RwLock<CryptoContext>` implementation. Transcript rebuilds clear the complete Handshake and 1-RTT packet-key bundles before fresh material is installed; a new rustls 1-RTT bundle also clears stale secret-derived generations and previous-read keys.
- TLS Cover cipher implementation, sequence advancement, telemetry, and material-retirement protection now have one canonical owner in `qf-crypto::TlsCoverCipherState`; the QFTLS cover adapter and root compatibility API delegate to it.
- `CryptoStream::reset()` now clears unsent, unacknowledged, retransmission, and receive state. A QFTLS transcript rebuild therefore cannot replay CRYPTO bytes retained by the replaced client connection.
- Focused gates pass qf-crypto `140/140`, qf-transport-crypto-stream `2/2`, QFTLS `21/21`, transport connection `134/134`, the direct complete-key-bundle installer regression `1/1`, and the TLS Cover integration target `3/3`.
- Full gates pass workspace all-feature checking, strict workspace library/binary/example Clippy, the complete serial root all-feature library suite `1,657/1,657`, formatting, documentation truth, and diff hygiene. The final pre-commit guard records `11,334,752 KiB` target usage and `4,207,260 KiB` free.
- Final pre-commit seam evidence is `scripts/out/audits/workspace-seams-20260810T-qftls-key-install-port-final-precommit/workspace-seams.json` at source revision `22f7a7274cf9578135eb9b46b331b0cb51a7bbab`: `36` packages, `336` Rust files, `207,678` source lines, `106` module edges, `123` dependency edges, no strongly connected components, and `protected_changes=[]`. The former `qftls/transport` SCC is closed; the remaining direct ownership is one-way `transport -> qftls` with six references.
- Post-push seam evidence is `scripts/out/audits/workspace-seams-20260810T-qftls-key-install-port-postpush/workspace-seams.json` at published revision `c379057f7e9b63a5254c3a40b72c87a56664e0b0`; counts and topology match the final pre-commit report exactly, with no strongly connected components and `protected_changes=[]`.
- Frontend and Tauri remain untouched. No frontend field or API projection is required.

## Stable AMX Detection Repair (2026-08-10, TODO-562 acceptance audit)

- Implementation commit `b4ef8557fc5c416d94c4ef0b404a189a08de94ee` is published on `origin/main`.
- CI run `31406352563` exposed one shared stable-Rust failure across Linux fastpath, Linux traffic analysis, Linux installer, Linux SIMD, Windows core, and the AMX proof lane: `is_x86_feature_detected!("amx-tile"|"amx-int8"|"amx-bf16")` is still an unstable library surface on stable x86_64 Rust 1.97.1.
- `qf-cpu` now reads CPUID leaf `07H/00H` through stable `__cpuid_count`, after validating the maximum basic leaf, and decodes AMX-BF16, AMX-TILE, and AMX-INT8 from EDX bits 22, 24, and 25. SGX remains explicitly fail-closed. CPU evidence, Linux tile-state permission, compiler target features, verified backend state, and product eligibility remain separate; no AMX product backend was enabled.
- Focused AMX tests pass `3/3`, qf-cpu all-feature tests pass `86/86`, strict qf-cpu test Clippy passes, the default stable Linux x86_64 cross-check passes, and the AMX contract plus documentation-truth audits pass. Formatting, diff hygiene, and the protected-path diff are clean.
- Before this repair, the exact acceptance test `cargo test --workspace --all-targets --features rust-tests --locked` completed successfully across every registered workspace target. The real touched qf-crypto incremental check completed in `1.18s`, and all 35 leaf crates passed independent locked checks.
- The Linux x86_64 qf-cpu all-feature cross-check now reaches a separate pre-existing acceptance gap: `internal_avx10_preview` still uses unsupported `avx10.1-256` and `avx10.1-512` detection macros. That feature contract and the remaining CI infrastructure failures are separate next repair slices. Frontend and Tauri remain untouched.
- Post-push seam evidence is `scripts/out/audits/workspace-seams-20260810T-stable-amx-detection-postpush/workspace-seams.json` at published revision `b4ef855f4d20fd78bc0cb72fb820e2d821b44ba3`: 36 packages, 336 Rust files, 207,713 source lines, 106 module edges, 123 workspace dependency edges, no strongly connected components, and `protected_changes=[]`.

## Stable AVX10.1 Detection Repair (2026-08-10, TODO-562 acceptance audit)

- The unsupported stable-Rust `is_x86_feature_detected!("avx10.1-256"|"avx10.1-512")` calls are removed. `qf-cpu` now verifies the current Intel versioned enumeration through stable CPUID and XGETBV intrinsics: `CPUID.07H.01H:EDX[19]`, a defined leaf `24H` subleaf range, version >= 1 in `CPUID.24H.00H:EBX[7:0]`, and the complete SSE/AVX/opmask/ZMM XCR0 state. SGX and every incomplete contract fail closed.
- Intel no longer enumerates separate AVX10 vector lengths. The historical 256-bit and 512-bit fields remain compatibility projections and are set together for verified AVX10.1. The synthetic `X86_P4a` profile remains available for compatibility, while runtime detection selects `X86_P4b`.
- The pure decoder regression covers each independent failure boundary. Stable Linux x86_64 qf-cpu all-feature checking passes, qf-cpu all-feature tests pass `87/87`, strict qf-cpu test Clippy passes, workspace all-feature library/binary/example Clippy passes, and the serial root all-feature library passes. Formatting and diff hygiene are clean.
- This is backend-only. Frontend and Tauri paths remain untouched, and no frontend field/API projection is required.
- Post-push seam evidence is `scripts/out/audits/workspace-seams-20260810T-stable-avx10-detection-postpush/workspace-seams.json` at published revision `1fad839e8ebe84767fbea2389eba40edf6fcf476`: 36 packages, 336 Rust files, 207,784 source lines, 106 module edges, 123 workspace dependency edges, no strongly connected components, and `protected_changes=[]`.
- The AMX proof contract audit now uses ripgrep when present and a recursive fixed-string grep fallback otherwise. Normal and deliberately ripgrep-free executions are byte-identical and pass, closing the Ubuntu runner's undeclared `rg` dependency without weakening any assertion.

## Fuzz Toolchain Isolation Repair (2026-08-10, TODO-562 acceptance audit)

- CI run `31408910756` showed that job-wide `RUSTFLAGS=-Zsanitizer=address` contaminated `cargo-fuzz` installation while `rust-toolchain.toml` forced stable Rust 1.97.1. The job stopped before target discovery with stable Rust's rejection of `-Zsanitizer`.
- Main and scheduled fuzz workflows now install `cargo-fuzz 0.13.2` through explicit Nightly and scope the sanitizer flag only to the shared fuzz runner. Fuzz metadata and target discovery also select Nightly explicitly, preventing the stable repository pin from replacing the required toolchain.
- The fuzz contract audit passes locally with all six declared targets, curated seeds, workflow lanes, sanitizer scope, and artifacts aligned. Hosted target execution remains pending a post-fix CI run and is not claimed green.
- Frontend and Tauri paths remain untouched.

## Proof Harness and Guardrail Leaf Ownership (2026-08-10, TODO-562 acceptance audit)

- CI artifact `privilege-memory-tls-proof` from run `31406352563` stopped in `scripts/tests/lib/lib-common.sh` because Bash `set -u` rejected expansion of an empty `flags` array. `run_cargo` now invokes Cargo directly when there are no environment assignments and retains the validated `env` path when assignments exist. The argument-safety regression proves the exact argv for both cases.
- The privilege and memory-lock negative contracts were still filtered through their former root module paths. The proof suite now selects `qf-privilege` and `qf-memory-lock` explicitly, uses their canonical test names, and gives qf-privilege the empty `rust-tests` harness feature required by the shared runner.
- The complete deterministic suite passes qf-privilege `23/23`, qf-memory-lock `12/12`, QFTLS `15/15`, and privilege integration `1/1`. Both JSON artifacts validate; the proof status is `PASS`, failures are zero, and the Linux-root-only native lane is correctly `UNAVAILABLE` on macOS.
- The first refreshed runtime audit exposed three additional stale root-path assumptions for interface configuration, TLS Cover cipher ownership, and the Stealth string caller. Those assertions now follow qf-engine-types, qf-crypto, qf-stealth, and qf-cpu. The final audit passes all `106` items with `Critical: 0` and `Warnings: 0` at `scripts/out/audits/runtime-guardrails-20260810T-leaf-owner-portability-final/audit-runtime-guardrails.log`.
- Strict workspace all-feature library/binary/example Clippy passes, and the serial root all-feature library suite passes `1,657/1,657`. Script syntax, documentation truth, diff hygiene, and protected-path checks pass. Final target/free space is `6,868,956 / 8,023,472 KiB`; no cleanup was required.
- Hosted CI proof remains pending publication of this slice. Frontend and Tauri paths remain untouched.

## Fuzz Dependency Ownership and Locked Graph (2026-08-10, TODO-562 acceptance audit)

- CI run `31409402706` reached the Nightly AddressSanitizer build after the toolchain isolation repair, then failed in `crates/qf-crypto/src/gcm.rs` because qf-crypto called `log::warn!` without declaring `log` directly. Root builds had hidden the invalid leaf contract through the root package's logging dependency.
- `crates/qf-crypto/Cargo.toml` now declares `log = "0.4"`, and both the root and fuzz lockfiles record that owner. The fuzz lockfile also records all 35 extracted backend leaves instead of the stale pre-extraction root-only graph.
- Fuzz metadata validation no longer uses `--no-deps`; both the audit and runner require the complete locked graph to resolve. The audit additionally checks the direct qf-crypto logging declaration.
- The qf-crypto no-default-feature check, qf-crypto tests `140/140`, strict qf-crypto and workspace Clippy, locked Nightly fuzz all-target check, fuzz contract, script syntax, and serial root all-feature library `1,657/1,657` pass. The exact AddressSanitizer `connection_handling` target builds through the complete backend graph and completes two runs with exit 0 and no crash. Final target/free space is `9,467,028 / 4,357,824 KiB`, below the cleanup threshold and above the build floor.
- That Nightly run also exposed five separate `Atomic*::fetch_update` deprecation warnings in qf-telemetry, qf-memory-pool, qf-instrumentation, and two root stealth callers. They were not part of the qf-crypto dependency repair and are closed in the dedicated continuation below.
- Frontend and Tauri paths remain untouched. No frontend field or API projection is required.

## Linux Firewall Owner Verification Port (2026-08-10, TODO-562 acceptance audit)

- CI run `31411284563` failed default all-target Clippy because `qf-firewall::verify_nft_table_owner` was crate-private and therefore unused inside the leaf. The Linux routing implementation already called the root compatibility path, but that wrapper did not exist; macOS excluded both sides through target configuration and hid the cross-crate contract gap.
- The leaf owner verifier is now public with a documented contract, while `quicfuscate::firewall` provides the crate-private Linux wrapper consumed by server routing. The dependency direction remains one-way from the product root to qf-firewall.
- qf-firewall tests pass `29/29`, Linux x86_64 qf-firewall all-target Clippy passes with warnings denied, and formatting passes. The root Linux cross-check remains unavailable after `sha2-asm` requires the absent `x86_64-linux-gnu-gcc`; it stops before compiling the root package and does not invalidate the leaf proof.
- The exact default workspace all-target Clippy command now passes qf-firewall and reaches a separate qf-compress/qf-fec test-feature failure: `MemoryPool::accounting_snapshot` is unavailable without `rust-tests`. That next feature-ownership repair is separate from the firewall API port.
- Strict workspace all-feature library/binary/example Clippy and documentation truth pass. Final target/free space is `9,942,756 / 3,785,472 KiB`, below the cleanup threshold and above the build floor. Frontend and Tauri paths remain untouched; no frontend field or API projection is required.

## Pool accounting test feature ownership continuation (2026-08-10)

- qf-compress and qf-fec now enable `qf-memory-pool/rust-tests` only through package-local dev-dependencies. Their normal dependency edges remain feature-free, while their external unit-test targets can compile the hidden accounting snapshot used to prove pool-return invariants.
- qf-compress tests pass `17/17`, qf-fec tests pass `82/82`, focused default all-target Clippy passes with warnings denied, and strict workspace all-feature library/binary/example Clippy passes.
- The exact default workspace all-target Clippy lane no longer reports either accounting error. It now reaches a separate root test-target feature-forwarding defect with `81` errors across qf-fec, transport, stealth, and recovery test helpers; that is the next atomic repair.
- The broad failed lane dropped free space below 2 GiB. `cargo clean` removed `52,002` generated files and `11.7 GiB`; the successful fresh all-feature gate leaves `577,064 KiB` of target data and `12,466,456 KiB` free.
- Frontend and Tauri paths remain untouched. No frontend field or API projection is required.

## Root default test feature ownership continuation (2026-08-10)

- The root package now enables exactly the six leaf `rust-tests` surfaces used by root unit tests through dev-dependencies: qf-fec, qf-stealth, qf-transport-batch, qf-transport-recovery, qf-transport-types, and qf-transport-udp. Normal dependency edges stay feature-free.
- The root Stealth acceleration import of `FeatureDetector` is limited to x86_64 production or test, rust-tests, and benchmark builds, removing the sole remaining default non-x86 library warning.
- The exact default workspace all-target Clippy lane now passes with warnings denied. The default root library passes `1,615/1,615`, strict workspace all-feature library/binary/example Clippy passes, and formatting passes.
- Final target/free space is `3,348,156 / 9,435,932 KiB`. Frontend and Tauri paths remain untouched; no frontend field or API projection is required.

## Cargo feature taxonomy leaf-forwarding continuation (2026-08-10)

- The Cargo feature taxonomy mirror was stale for eight groups after backend leaf extraction: server, dev-certs, aggressive_inline, prefetch, std, internal_avx10_preview, rust-tests, and benches. Their exact root-to-leaf forwarding lists now match `Cargo.toml` and metadata without changing any feature implementation.
- `verify-cargo-feature-taxonomy.sh` passes with `26` declared features, `29` effective selectors, `862` Rust source references, no unknown source or target selectors, five green positive profiles, seven retired groups rejected with exit `101`, and zero failures.
- Bash syntax and diff hygiene pass. Final target/free space is `4,006,764 / 8,770,776 KiB`. Frontend and Tauri paths remain untouched; no frontend field or API projection is required.

## qf-cpu x86 Clippy portability continuation (2026-08-10)

- Hosted Clippy Matrix run `31414899592`, job `93541482724`, identified eight qf-cpu warnings in the x86_64 `unsafe_rust` lane. The AVX10 support decoder now matches its unit-test and `internal_avx10_preview` caller contract; the remaining SIMD literal, reduction, pattern, and bitmap diagnostics use behavior-equivalent idiomatic forms.
- Native x86_64-Linux qf-cpu all-target Clippy passes with warnings denied, qf-cpu all-feature tests pass `87/87`, and strict workspace all-feature library/binary/example Clippy passes.
- The exact workspace `unsafe_rust` all-target command clears qf-cpu and then exposed the separate root `CompressionStrategy::BtOpt` dead-code diagnostic closed below.
- Final target/free space is `4,531,144 / 6,997,628 KiB`. Frontend and Tauri paths remain untouched; no frontend field or API projection is required.

## Zstd BtOpt feature ownership continuation (2026-08-10)

- `CompressionStrategy::BtOpt` and its native mapping now compile only with `compression_zstd_ffi`, matching the only path that constructs the variant through the `btopt` environment override. The safe fallback keeps its existing four length-selected strategies without a dormant variant.
- The exact workspace `unsafe_rust` all-target Clippy lane passes. Serial root fallback tests pass `1,646/1,646`, serial root all-feature tests pass `1,657/1,657`, and strict workspace all-feature library/binary/example Clippy passes.
- Final target/free space is `6,621,332 / 4,546,296 KiB`. Frontend and Tauri paths remain untouched; no frontend field or API projection is required.

## qf-transport-udp Linux dependency ownership continuation (2026-08-10)

- qf-transport-udp now declares the Linux-only `log` and `socket2` dependencies used by its GSO/GRO implementation. The lockfile adds only those two existing dependency edges to the leaf package entry, with no version movement.
- Locked x86_64-Linux qf-transport-udp all-target Clippy passes with warnings denied, qf-transport-udp all-feature tests pass `11/11`, the feature-taxonomy audit passes, and strict workspace all-feature library/binary/example Clippy passes.
- Final target/free space is `7,207,740 / 3,832,516 KiB`. Frontend and Tauri paths remain untouched; no frontend field or API projection is required.

## qf-memory-pool Windows Winsock boundary continuation (2026-08-10)

- qf-memory-pool now wraps `WSAGetLastError` in one documented safe adapter, enables the exact windows-sys `Win32_System_IO` binding feature, shares checked total-length accumulation across Unix and Windows, and passes the native Windows `socklen_t` without a redundant conversion. The unused private Windows NUMA affinity mutation is removed; active topology behavior is unchanged.
- Locked Windows-MSVC all-target/all-feature qf-memory-pool Clippy passes with warnings denied, ARM64 qf-memory-pool tests pass `23/23`, and strict workspace all-feature library/binary/example Clippy passes. Native Windows runtime execution remains unclaimed.
- Final target/free space is `7,492,296 / 3,036,000 KiB`. Frontend and Tauri paths remain untouched; no frontend field or API projection is required.

## qf-crypto x86 Clippy portability continuation (2026-08-10)

- Hosted Clippy Matrix job `93544957297` exposed ten qf-crypto x86_64 diagnostics. Fixed four-lane arrays now carry AEGIS VAES and Poly1305 multiply inputs; AES round keys and CTR keystream lanes use bounded iterators; MORUS drops one same-type cast and one needless return. No lint suppression or algorithm change is introduced.
- Locked x86_64-Linux qf-crypto all-target Clippy passes with `rust-tests` and warnings denied. qf-crypto all-feature tests pass `140/140`; exact default and `unsafe_rust,rust-tests` workspace all-target Clippy pass; strict workspace all-feature library/binary/example Clippy passes.
- The free-space floor triggered `cargo clean` before the cold Linux gate. Final target/free space is `1,887,580 / 7,169,396 KiB`. Frontend and Tauri paths remain untouched; no frontend field or API projection is required.

## qf-transport-batch timeout result typing continuation (2026-08-10)

- Clippy Matrix run `31419729146`, job `93557289482`, exposed an ambiguous error parameter in the Linux `recvmmsg` timeout conversion under `zero_copy_dgram`. The closure now returns `std::io::Result<libc::timespec>` explicitly, preserving the checked seconds conversion and syscall behavior.
- The exact `zero_copy_dgram` and default workspace all-target Clippy lanes pass with warnings denied. The Cargo feature-taxonomy audit passes all five positive profiles with zero failures, qf-transport-batch tests pass `7/7`, and strict workspace all-feature library/binary/example Clippy passes.
- Final target/free space is `3,269,876 / 6,667,016 KiB`, below the cleanup threshold and above the build floor. Frontend and Tauri paths remain untouched; no frontend field or API projection is required.

## qf-simd x86 warning and ownership continuation (2026-08-10)

- CI run `31419729080`, Linux fastpath job `93557289942`, exposed ten qf-simd dead-code warnings. Eight private x86 implementations had no repository caller and duplicated active histogram/pattern, dot-product, varint, or extended-SIMD owners; they are removed. The retained SSE4.2 short-pattern safety regression is test-only, and the scalar SVE2 fallback now compiles only for AArch64 without compile-time SVE2.
- The active x86 paths use platform-correct SHA-256 cfg ownership and idiomatic bounded iteration. Reed-Solomon matrix elimination preserves disjoint mutable row ownership without copying. AVX-512 ACK canonicalization now counts qualifying low lanes with `trailing_ones`, restoring vector progress while preserving the scalar-equivalent output contract.
- Locked Linux-x86 and Windows-x86 qf-simd all-target/all-feature Clippy pass with warnings denied. Executable ARM64 qf-simd tests pass `61/61`; the exact default and `unsafe_rust,rust-tests` workspace all-target Clippy lanes, strict workspace all-feature library/binary/example Clippy, and formatting pass. The Linux cross-check uses the installed Clang assembler for `sha2-asm`; native x86 runtime parity remains hosted and is not inferred locally.
- Final target/free space is `4,007,492 / 5,894,636 KiB`, below the cleanup threshold and above the build floor. Frontend and Tauri paths remain untouched; no frontend field or API projection is required.

## qf-transport-batch Linux timeout conversion continuation (2026-08-10)

- Clippy Matrix run `31420421999`, job `93559544354`, rejected the Linux `libc::c_long::try_from(d.subsec_nanos())` conversion as unnecessarily fallible. The nanosecond field is bounded below one billion and now uses a proven infallible cast to signed Linux `c_long`, including 32-bit targets; seconds remain checked against `time_t`, and syscall behavior is unchanged.
- Locked Linux-x86 qf-transport-batch all-target/all-feature Clippy passes with warnings denied. qf-transport-batch tests pass `7/7`; the exact default and `unsafe_rust,rust-tests` workspace all-target lanes, strict workspace all-feature library/binary/example Clippy, and formatting pass locally. The complete Linux workspace cross-check stops before product code because the macOS host has no Linux C sysroot for ring/zstd; hosted Ubuntu remains authoritative for that lane.
- Final target/free space is `4,181,944 / 5,752,860 KiB`, below the cleanup threshold and above the build floor. Frontend and Tauri paths remain untouched; no frontend field or API projection is required.

## Nightly atomic retry API compatibility continuation (2026-08-10)

- Nightly 1.99 deprecates `Atomic*::fetch_update` in favor of `try_update`, whose identical signature is stable since Rust 1.95. All five repository call sites now use the current API: qf-telemetry counter subtraction, qf-instrumentation active-client decrement, qf-memory-pool ownership-ledger decrement, Stealth profile rotation, and Stealth worker release. Ordering, closures, transitions, and result handling remain unchanged.
- Nightly checks with `RUSTFLAGS="-D warnings"` pass for the three affected leaves and the all-feature root library. Stable leaf tests pass `44/44`; the serial root all-feature library passes `1,657/1,657`; exact default and `unsafe_rust,rust-tests` workspace all-target Clippy, strict workspace all-feature library/binary/example Clippy, and formatting pass.
- The pre-Nightly `cargo clean` removed `13,355` generated files and `4.1 GiB`. Final target/free space is `4,059,408 / 5,742,176 KiB`, below the cleanup threshold and above the build floor. Frontend and Tauri paths remain untouched; no frontend field or API projection is required.

## qf-fec AVX2 cast cleanup continuation (2026-08-10)

- Clippy Matrix run `31422244709`, throughput job `93565534889`, exposed two warning-denied redundant casts in qf-fec's AVX2 GF(2^8) nibble-table setup. The shifted table index now retains its existing `u8` type directly, and the shuffle mask uses an explicitly typed `i8` literal. Values, intrinsic selection, arithmetic, and dispatch are unchanged.
- Locked Linux-x86 qf-fec all-target/all-feature Clippy passes with Clang providing the sha2-asm assembler. qf-fec tests pass `82/82`; the feature-taxonomy audit passes all five positive profiles and rejects all seven retired groups; exact default and `unsafe_rust,rust-tests` workspace all-target Clippy, strict workspace all-feature library/binary/example Clippy, and formatting pass.
- Final target/free space is `5,960,644 / 3,771,248 KiB`, below the cleanup threshold and above the build floor. Frontend and Tauri paths remain untouched; no frontend field or API projection is required.

## qf-transport-batch test-surface cfg continuation (2026-08-10)

- CI run `31422243835` exposed four warning-denied Linux installer diagnostics because the parity/test-only leaf compiled three metric statics and their `Ordering` import without compiling `BatchProcessor`. Those owners now share its test/rust-tests boundary plus Linux. The acceleration alias is restricted to Linux/macOS/iOS test surfaces, so Windows no longer compiles an unused alias.
- The exact locked Linux-x86 no-default-feature check passes with `RUSTFLAGS="-D warnings"`; Windows-MSVC all-target/all-feature qf-transport-batch Clippy passes with warnings denied. Leaf tests pass `7/7`; exact default and `unsafe_rust,rust-tests` workspace all-target Clippy, strict workspace all-feature library/binary/example Clippy, and formatting pass.
- Final target/free space is `6,239,120 / 3,482,908 KiB`, below the cleanup threshold and above the build floor. Runtime transport behavior is unchanged because this leaf remains parity/test-only. Frontend and Tauri paths remain untouched; no frontend field or API projection is required.

## Root x86 target-feature inlining continuation (2026-08-10)

- CI run `31422243835` exposed four stable compiler errors across Windows core job `93565626522`, Linux traffic job `93565626630`, and Linux fastpath job `93565626670`: Rust forbids combining `#[inline(always)]` with `#[target_feature]`.
- The AES-NI, VAES, AVX2/FMA, and AVX-512/FMA helpers retain their target-feature attributes, runtime dispatch, signatures, intrinsic bodies, and scalar fallbacks while dropping only forced inlining. A repository-wide source check finds no remaining adjacent forbidden attribute pair.
- The serial root all-feature library passes `1,657/1,657`; exact default and `unsafe_rust,rust-tests` workspace all-target Clippy, strict all-feature workspace library/binary/example Clippy, and formatting pass.
- The complete local Windows cross-check stops in native C dependencies before product code because this macOS host has no Windows C sysroot. Final target/free space is `6,694,056 / 2,888,184 KiB`, below the cleanup threshold and above the build floor. Frontend and Tauri paths remain untouched; no frontend field or API projection is required.

## Root pattern-injection test cfg continuation (2026-08-10)

- CI run `31422243835`, Linux traffic job `93565626630`, exposed four `complete_pattern_end` lookup errors. The shared helper was correctly limited to test/rust-tests, but its private x86 AVX2 and SSE2 consumers still compiled in normal product builds despite having no production caller.
- Both x86 injectors now share the same test/rust-tests boundary as the public test helper and the AArch64 NEON/SVE2 implementations. SIMD selection, complete-window validation, copies, and malformed-position behavior remain unchanged where the test surface is enabled.
- The serial root all-feature library passes `1,657/1,657`; exact default and `unsafe_rust,rust-tests` workspace all-target Clippy, strict all-feature workspace library/binary/example Clippy, formatting, and runtime guardrails pass.
- The complete local Linux cross-check stops in native C dependencies before product code because this macOS host has no Linux C sysroot. Final target/free space is `6,684,724 / 2,846,996 KiB`, below the cleanup threshold and above the build floor. Frontend and Tauri paths remain untouched; no frontend field or API projection is required.

## Client io_uring batch-reference cfg continuation (2026-08-10)

- CI run `31422243835` exposed `batch_refs` as unused in Linux profiles without `io_uring`. The SmallVec of borrowed payload slices is consumed only by `UringBatchWorker::send_batch_with_disposition`; sendmmsg independently builds the subset that io_uring did not accept.
- The vector now shares its only consumer's `io_uring` feature boundary. Batch ownership, per-slot result accounting, sendmmsg subset construction, direct socket fallback, and telemetry remain unchanged.
- The serial root all-feature library passes `1,657/1,657`; exact default and `unsafe_rust,rust-tests` workspace all-target Clippy, strict all-feature workspace library/binary/example Clippy, and formatting pass.
- The complete local Linux cross-check stops in native C dependencies before product code because this macOS host has no Linux C sysroot. Final target/free space is `6,752,040 / 2,629,932 KiB`, below the cleanup threshold and above the build floor. Frontend and Tauri paths remain untouched; no frontend field or API projection is required.

## Wintun startup-owner transfer continuation (2026-08-10)

- CI run `31422243835`, Windows core job `93565626522`, exposed `E0509` in `WintunStartupOwner::into_parts`, `E0507` in the locked cleanup-state test snapshot, and one unnecessary mutable device binding.
- `into_parts` now takes all four optional native owners without moving fields directly from the Drop type. Complete transfer leaves its Drop ledger empty; an incomplete tuple is restored into the same owner before ordered rollback, so library, adapter, session, event, diagnostics, and bounded retry ownership cannot disappear. The snapshot clones under lock and the runtime device binding is immutable.
- The serial root all-feature library passes `1,657/1,657`; exact default and `unsafe_rust,rust-tests` workspace all-target Clippy, strict all-feature workspace library/binary/example Clippy, and formatting pass. The complete local Windows cross-check remains blocked before product code by the missing Windows C sysroot.
- The post-gate free-space floor triggered `cargo clean`, which removed `35,889` generated files and `7.8 GiB`. Final target/free space is `0 / 9,152,212 KiB`. Frontend and Tauri paths remain untouched; no frontend field or API projection is required.

## Windows conditional-compilation ownership continuation (2026-08-10)

- CI run `31422243835`, Windows core job `93565626522`, exposed three warning-denied declarations outside their consumer surfaces: `partial_transition_error`, the root `gf_mul_table` compatibility export, and the root TUN `IpAddr` import.
- The privilege formatter now compiles for Unix production plus unit tests, preserving its platform-independent regression. The GF(2^8) compatibility export is test-only because both root consumers are tests. The TUN address import is Linux/macOS-only because Windows, iOS, and fallback backends never reference it.
- qf-privilege tests pass `23/23`; the serial root all-feature library passes `1,657/1,657`; exact default and `unsafe_rust,rust-tests` workspace all-target Clippy, strict all-feature workspace library/binary/example Clippy, and formatting pass. The complete local Windows root cross-check remains blocked before product code by the missing Windows C sysroot.
- Final target/free space is `3,535,364 / 5,692,884 KiB`, below the cleanup threshold and above the build floor. Frontend and Tauri paths remain untouched; no frontend field or API projection is required.

## Fuzz workspace lock synchronization continuation (2026-08-10)

- CI run `31422243835`, fuzz job `93565626558`, stopped in `verify-fuzz-contract.sh` because locked Nightly metadata needed to update `scripts/tests/fuzz/Cargo.lock`. The stale package entry was qf-transport-udp after its Linux dependency ownership changed.
- Cargo updated the existing lock with exactly two dependency entries, `log` and `socket2 0.5.10`, under qf-transport-udp. No resolved version, checksum, manifest, fuzz target, corpus, workflow, or sanitizer flag changed.
- Locked Nightly metadata passes, the separate fuzz workspace checks all targets on Nightly, and the fuzz contract audit passes its six-target inventory, curated seed, CI lane, sanitizer, artifact, and crypto coverage assertions. Actual six-target AddressSanitizer execution remains owned by hosted Ubuntu.
- Final target/free space is `3,893,020 / 5,335,128 KiB`, below the cleanup threshold and above the build floor. Frontend and Tauri paths remain untouched; no frontend field or API projection is required.

## unsafe_rust optimize ownership continuation (2026-08-10)

- Clippy Matrix run `31427636274`, `unsafe_rust` job `93583017456`, found an unused root x86 intrinsic import and a Linux test reference to the removed `optimize::numa` module.
- The import now compiles only for x86 test/rust-tests builds, exactly matching the unqualified AVX2 injector. The test-only UnsafeMemoryPool drops its obsolete NUMA call; qf-memory-pool is the sole current NUMA owner and its Linux adapter intentionally performs no move, so allocation, ownership, alignment, and release semantics are unchanged.
- Exact local `cargo clippy --workspace --locked --all-targets --features unsafe_rust -- -D warnings` passes, the serial root all-feature library passes `1,657/1,657`, and formatting passes. Hosted Linux x86 confirmation remains pending publication.
- Final target/free space is `4,325,868 / 3,758,204 KiB`, below the cleanup threshold and above the build floor. Frontend and Tauri paths remain untouched; no frontend field or API projection is required.

## Root VBMI2 test compatibility ownership continuation (2026-08-10)

- Clippy Matrix run `31427636274`, `unsafe_rust` job `93583017456`, exposed the private root `gf16_mul_slice_vbmi2` wrapper as dead in x86 product builds. Its repository consumers are exactly two x86 root unit tests.
- The wrapper now requires both `test` and `x86_64`. qf-fec still owns the production VBMI2 implementation and calls it from the live runtime dispatcher, so kernel selection, safety bounds, telemetry, and behavior are unchanged.
- Focused root GF16 tests pass `6/6`, exact local `cargo clippy --workspace --locked --all-targets --features unsafe_rust -- -D warnings` passes, and formatting passes. Native VBMI2 execution and hosted Linux x86 confirmation remain external.
- Final target/free space is `4,534,900 / 3,551,268 KiB`, below the cleanup threshold and above the build floor. Frontend and Tauri paths remain untouched; no frontend field or API projection is required.

## unsafe_rust firewall and routing ownership continuation (2026-08-10)

- Clippy Matrix run `31427636274`, `unsafe_rust` job `93583017456`, exposed the unused root `verify_nft_table_rules` compatibility wrapper, the unowned `nftables_ruleset` adapter outside its three unit-test callers, and two `question_mark` diagnostics in `setup_iptables_family`.
- The dead root wrapper is removed while qf-firewall retains its canonical public verifier and routing retains the live owner-verifying wrapper. The unowned ruleset adapter is test-only. Iptables restore and post-install verification keep their order, return types, and exact errors through direct `?` propagation.
- Focused routing tests pass `24/24`, exact local `cargo clippy --workspace --locked --all-targets --features unsafe_rust -- -D warnings` passes, formatting and diff hygiene pass, and runtime guardrails report `Critical: 0` plus `Warnings: 0` in `scripts/out/audits/audit-runtime-guardrails-20260810_222807/audit-runtime-guardrails.log`. Hosted Linux x86 confirmation remains external.
- Final target/free space is `4,553,404 / 3,362,780 KiB`, below the cleanup threshold and above the build floor. Frontend and Tauri paths remain untouched; no frontend field or API projection is required.

## ChaCha20 SIMD counter-lane continuation (2026-08-10)

- Clippy Matrix run `31427636274`, `unsafe_rust` job `93583017456`, exposed `needless_range_loop` in the AVX-512 ChaCha20 counter-lane initializer. Review of the shared counter contract also found plain x86 x4 additions that panic near `u32::MAX` in debug builds while scalar, NEON, and AVX-512 use wrapping arithmetic.
- AVX-512 now initializes lanes through mutable iteration with enumerated offsets. The shared x86 SSE core uses `wrapping_add` for its three derived lanes, covering the SSE4.1, AVX, and AVX2 wrappers without changing vector rounds, serialization, feature dispatch, or ordinary counters.
- Four focused ChaCha20 tests pass with x4 and x16 scalar parity across counter wraparound. Exact local `cargo clippy --workspace --locked --all-targets --features unsafe_rust -- -D warnings` and formatting pass; runtime guardrails report `Critical: 0` plus `Warnings: 0` in `scripts/out/audits/audit-runtime-guardrails-20260810_223229/audit-runtime-guardrails.log`. Native x86 SIMD execution and hosted Linux x86 confirmation remain external.
- Final target/free space is `4,572,500 / 4,113,444 KiB`, below the cleanup threshold and above the build floor. Frontend and Tauri paths remain untouched; no frontend field or API projection is required.

## macOS PF test-helper ownership continuation (2026-08-10)

- Clippy Matrix run `31429261499`, job `93588364875`, exposed `RoutingManager::pf_rules` as dead in Linux test builds. Its only repository caller is `test_pf_rules_keep_ipv4_and_ipv6_in_one_anchor_ruleset`, which is macOS-only.
- The helper now shares the caller's `test` plus macOS target boundary. Its generated PF rules, platform behavior, and caller assertions are unchanged.
- Focused routing tests pass `24/24`, exact local `cargo clippy --workspace --locked --all-targets --features unsafe_rust -- -D warnings` passes, and formatting passes. Hosted Linux x86 confirmation remains external.
- Final target/free space is `4,569,848 / 3,214,704 KiB`, below the cleanup threshold and above the build floor. Frontend and Tauri paths remain untouched; no frontend field or API projection is required.

## Matrix-transpose SIMD iteration continuation (2026-08-10)

- Clippy Matrix run `31429261499` exposed the same three x86 diagnostics across six feature jobs: a collapsible AVX2 eligibility guard and indexed eight-lane load/store loops in `src/optimize/memory.rs`.
- AVX2 dispatch now combines the feature, four-byte element, and 8x8 shape requirements in one condition. The kernel iterates directly over vector slots with explicit offsets for both loads and stores, preserving every address, shuffle, permutation, fallback, and return condition.
- Exact local `cargo clippy --workspace --locked --all-targets --features "internal_wiedemann,unsafe_rust" -- -D warnings`, the equivalent `test-suite` profile, formatting, and diff hygiene pass; runtime guardrails report `Critical: 0` plus `Warnings: 0` in `scripts/out/audits/audit-runtime-guardrails-20260810_224150/audit-runtime-guardrails.log`. Native x86 execution and hosted Linux x86 confirmation remain external.
- Final target/free space is `5,192,724 / 2,570,584 KiB`, below the cleanup threshold and above the build floor. Frontend and Tauri paths remain untouched; no frontend field or API projection is required.

## Root unsafe SIMD parity ownership continuation (2026-08-10)

- Clippy Matrix run `31429261499` exposed five dead x86 functions in the root test-only `unsafe_rust` module. Exhaustive repository caller searches confirmed that the parallel GF(2^8), XOR, and entropy block had no external consumer; its only two test callers exercised the duplicate implementation itself.
- The orphan block and those duplicate tests are removed. qf-simd retains the canonical runtime-dispatched GF and XOR kernels plus parity coverage, while qf-cpu retains the entropy histogram consumed by qf-compress. UnsafeMemoryPool allocation, ownership, compression, and packet behavior are unchanged.
- Focused root Unsafe tests pass `31/31`. Exact local `cargo clippy --workspace --locked --all-targets --features "internal_wiedemann,unsafe_rust" -- -D warnings`, the `unsafe_rust,rust-tests` profile, strict workspace all-feature library/binary/example Clippy, formatting, diff hygiene, and documentation truth pass. Runtime guardrails report `Critical: 0` plus `Warnings: 0` at `scripts/out/audits/audit-runtime-guardrails-20260810_225615/audit-runtime-guardrails.log`. Native x86 execution and hosted Linux x86 confirmation remain external.
- The required pre-build `cargo clean` removed `27,574` generated files and `6.4 GiB`. Final target/free space is `1,933,732 / 5,416,236 KiB`, below the cleanup threshold and above the build floor. Frontend and Tauri paths remain untouched; no frontend field or API projection is required.

## io_uring payload iterator mutability continuation (2026-08-10)

- Clippy Matrix run `31430369954`, `io_uring` job `93592061877`, exposed `E0596` in `checked_payload_bytes`: the generic iterator binding was immutable although `Iterator::try_fold` consumes `&mut self`.
- The helper now declares its iterator parameter mutable. Item type, checked aggregate arithmetic, error ownership, four callers, packet and byte admission bounds, and io_uring runtime behavior remain unchanged.
- Exact local `cargo clippy --workspace --locked --all-targets --features io_uring -- -D warnings`, strict workspace all-feature library/binary/example Clippy, formatting, diff hygiene, and documentation truth pass. Runtime guardrails report `Critical: 0` plus `Warnings: 0` at `scripts/out/audits/audit-runtime-guardrails-20260810_230058/audit-runtime-guardrails.log`. The changed module is Linux-only and cfg-excluded on macOS, so hosted Ubuntu remains the authoritative compilation confirmation.
- Final target/free space is `1,940,164 / 5,240,264 KiB`, below the cleanup threshold and above the build floor. Frontend and Tauri paths remain untouched; no frontend field or API projection is required.

## SIMD GF backend telemetry ownership continuation (2026-08-10)

- CI run `31430369948`, Ubuntu `simd-selfcheck` job `93593190871`, failed because the x86 backend test expected `FEC_SSSE3_OPS` while qf-simd has no SSSE3 GF(2^8) path. Review also confirmed the extracted dispatcher omitted telemetry after its real AVX-512 GFNI and AVX2 kernels.
- The dispatcher increments `FEC_GFNI_OPS` or `FEC_AVX2_OPS` after the selected x86 kernel. The test uses the same runtime feature matrix, observes the matching counter, and skips explicitly only on scalar-only x86 hosts. Arithmetic, dispatch, buffers, and fallbacks are unchanged.
- qf-simd all-feature tests pass `61/61`; locked Linux-x86 qf-simd all-target/all-feature Clippy passes with warnings denied using Clang for sha2-asm; strict workspace all-feature library/binary/example Clippy, formatting, diff hygiene, and documentation truth pass. Runtime guardrails report `Critical: 0` plus `Warnings: 0` at `scripts/out/audits/audit-runtime-guardrails-20260810_231825/audit-runtime-guardrails.log`. The local root self-check rebuild was stopped before execution at `1,846,180 KiB` free, so hosted Ubuntu remains the authoritative runtime gate.
- `cargo clean` removed `20,229` generated files and `2.6 GiB` after the floor trigger. Final target/free space is `447,752 / 5,381,352 KiB`, below the cleanup threshold and above the build floor. Frontend and Tauri paths remain untouched; no frontend field or API projection is required.

## Fuzz Nightly preflight continuation (2026-08-10)

- CI run `31430369948`, fuzz job `93593190864`, installed and executed Nightly successfully during setup and contract verification, then falsely emitted `[FAIL] nightly rustc is unavailable` before the first sanitizer target.
- The runner used `rustup run nightly rustc -vV | grep -q nightly` under `set -o pipefail`. The early-exit grep closed the pipe after its first match, causing `rustup` to receive SIGPIPE and return status `141`; the preflight interpreted that nonzero pipeline as a missing compiler.
- `run-ci-fuzz.sh` now captures the complete version output before checking for the Nightly release marker. `verify-fuzz-contract.sh` requires the safe probe and rejects the former early-exit pipeline. Compiler selection, locked metadata, target inventory, sanitizer flags, corpora, durations, and libFuzzer commands are unchanged.
- Bash syntax, an AddressSanitizer-scoped Nightly probe, the complete six-target fuzz contract audit, and diff hygiene pass locally. Hosted Ubuntu still owns the authoritative six-target sanitizer execution. Frontend and Tauri paths remain untouched; no frontend field or API projection is required.

## x86 integration-test feature ownership continuation (2026-08-10)

- Clippy Matrix run `31433342119`, `tun-tests,rust-tests` job `93601734927`, exposed two stale x86 integration boundaries: `rt-ghash-sse-parity` could not reach its qf-crypto test override because the library owner required `cfg(test)`, and `rt-chacha-x16-parity` addressed `optimize::crypto` even though that compatibility namespace exports only the x4 API.
- GHASH test mutex ownership, its setter, and the selector read now compile on x86 under `test` or qf-crypto's forwarded `rust-tests` feature. Normal product builds retain no deterministic test override. The ChaCha20 x16 parity target calls the existing canonical `optimize::simd::crypto` owner; no compatibility alias or production API was added.
- Exact workspace all-target Clippy for `tun-tests,rust-tests`, Linux-x86 qf-crypto all-target Clippy with `rust-tests`, qf-crypto tests `140/140`, strict workspace all-feature library/binary/example Clippy, formatting, and diff hygiene pass. A full local Linux-x86 root integration build remains unavailable because `ring` and `zstd-sys` stop at the missing Linux C sysroot before root product code; hosted Linux owns authoritative integration-target compilation.
- Final target/free space is `1,708,496 / 4,944,712 KiB`, below the cleanup threshold and above the build floor. Frontend and Tauri paths remain untouched; no frontend field or API projection is required.

## Linux installer memory-lock capability continuation (2026-08-10)

- CI run `31433762470`, native installer job `93603557265`, reached a real AlmaLinux service start and then failed both the pre-drop TLS key `mlock` and required post-drop `mlockall` with `EPERM`. The unit exposed an unlimited memlock budget but omitted `CAP_IPC_LOCK`, and the nspawn guest bounding set omitted the same capability.
- The shipped unit, generated systemd default, and disposable nspawn guest now retain `CAP_IPC_LOCK` during privileged startup. The runtime still clears every capability after its identity transition; `LimitMEMLOCK=infinity` remains the authorization boundary for the deferred unprivileged process-wide lock. No ambient capability is added and fail-closed memory-lock policy is not weakened.
- The installer host harness statically requires both capability layers plus the unlimited service budget. Bash syntax, ShellCheck warning level, the extracted static contract, formatting, diff hygiene, and focused generated-systemd tests pass `8/8`. The destructive AlmaLinux 9 and Debian 12 nspawn lifecycle cannot execute on this macOS host; hosted Linux remains authoritative.
- The first cold focused test was stopped before crossing the 2-GiB free-space floor. `cargo clean` removed `23,923` generated files and `3.3 GiB`; the clean rerun passed. Frontend and Tauri paths remain untouched; no frontend field or API projection is required.

## Native client-assignment harness continuation (2026-08-11)

- CI run `31440846342`, native traffic job `93625105551`, passed the corrected TUN provisioning rollback proof and then stopped in phase 0 because the multi-client harness still supplied standalone client address, prefix, and MTU flags. Current runtime intentionally rejects those local overrides because the authenticated `ClientAssignment` is the sole standalone-client configuration source.
- Client startup now supplies only `--tun` and `--tun-name`; the server retains the five authoritative TUN network flags. The harness still verifies the three exact sequential IPv4/IPv6 allocations. Its PTB phase keeps the 1472-byte client/server carrier, temporarily raises only the client TUN to 1500 around the split-boundary probe, restores 1280 afterward, and leaves the server TUN at 1280.
- The runtime guard requires exactly one server-side occurrence of each address, prefix, and MTU flag plus the explicit PTB client-MTU transitions. Canonical documentation and MAP text now distinguish standalone authenticated assignment from generic embedded configuration and remove stale claims that the assignment control plane is missing.
- Bash syntax, ShellCheck warning level, documentation truth, runtime guardrails with zero findings, diff hygiene, and protected frontend/Tauri isolation pass. This macOS host cannot execute the root-only Linux namespace harness; hosted Linux remains the authoritative privileged rerun. No frontend field or API projection is required.

## Authenticated MASQUE generation continuation (2026-08-11)

- CI run `31441666954`, native traffic job `93627536456`, reached the authenticated standalone assignment exchange and proved that Core emitted QKey authentication on CONNECT-UDP without the client connection generation. The server therefore recorded the MASQUE stream with no valid generation and correctly refused assignment emission.
- Core now builds one canonical MASQUE extra-header set containing both `x-qf-auth` and the nonzero `x-qf-generation`. Ordinary H3 requests reuse the same generation injector, including case-insensitive duplicate removal, so the control and fallback carriers cannot diverge again.
- A Core regression proves the exact Auth plus generation header contract. A paired 1-RTT H3 regression uses 64-KiB QPACK, late server H3 initialization, the exact extra headers, full request/response event draining, and CONNECT-UDP acceptance without `FrameUnexpected`.
- Commit `0afb88afe207f94d44a908b26888c7b4fde60b53` is pushed with exact local, tracking, and remote parity. Both focused regressions pass. Exact default workspace all-target Clippy passes with warnings denied; formatting, diff hygiene, documentation truth, and runtime guardrails pass with `Critical: 0` and `Warnings: 0`. The low-space preflight triggered `cargo clean`, removing `10.4 GiB`; protected frontend and Tauri paths remain untouched. Hosted Linux remains the authoritative process-level assignment and native TUN rerun.

## Single H3 owner continuation (2026-08-11)

- CI run `31443285627`, native traffic job `93632229718`, proved that authenticated generation reached the server before both endpoints rejected a duplicated HTTP/3 control-stream prologue with `FrameUnexpected`.
- `transport::Connection::do_tls_handshake()` no longer creates a default H3 owner after ALPN. `QuicFuscateConnection` remains the sole persona-configured H3/MASQUE application owner, so each endpoint queues exactly one SETTINGS prologue. Transport-local compatibility H3 construction is restricted to tests and the `rust-tests` feature.
- A real local TLS, QKey, TUN-assignment smoke recorded CONNECT-UDP, accepted the authenticated MASQUE data plane, sent generation 1, and accepted the Disabled assignment without an HTTP/3 error. The paired full-drain H3 regression passes; exact default and `rust-tests` workspace all-target Clippy pass with warnings denied; formatting, documentation truth, runtime guardrails with zero findings, diff hygiene, and protected frontend/Tauri isolation pass.
- Commit `ed93c84ca662c1994eb04b60d5356fd41d5ccd7f` is pushed with exact local, tracking, and remote parity. Hosted Linux remains authoritative for the privileged PTB rerun. No frontend field or API projection is required.

## Native client carrier-MTU continuation (2026-08-11)

- CI run `31445762098`, native traffic job `93639519939`, passed H3/MASQUE authentication and assignment but retained only the 1,328-byte client request on the TUN capture. The client reported the frame as MASQUE-accepted while server routing and `packet_too_big` metrics remained unchanged.
- `send_client_tun_packet()` was still intersecting the confirmed tunnel carrier with `TunInterface`'s cached server-assigned MTU. The native harness intentionally raises the live client link from 1280 to 1500, but that external Linux mutation cannot update the cached 1280 value, so the client generated the observed PTB locally and never sent the probe to the server.
- Client uplink classification now uses the effective tunnel carrier for frames already read from the platform TUN. The platform remains the normal live-interface MTU enforcer; only packets above the carrier receive a local PTB. Typed disposition logging now distinguishes tunneled frames from local PTB responses.
- Commit `9c27136a6cc20f0c815d372f298d16bf59c65559` is pushed with exact local, tracking, and remote parity. The focused carrier-boundary regression passes. Exact default and `rust-tests` workspace all-target Clippy pass with warnings denied; formatting, documentation truth, runtime guardrails with zero findings, diff hygiene, and protected frontend/Tauri isolation pass. Hosted Linux owns the privileged server-PTB rerun.

## Fan-out scheduler telemetry ownership continuation (2026-08-11)

- CI run `31446527164`, native traffic job `93641906073`, passed H3/MASQUE and server PTB, then reported six `tun_downlink_backpressure_events_total{event="enqueued"}` before the throughput phase. Retained artifact metrics prove the counter stayed at six through throughput while retries and both pending gauges stayed zero.
- The three normal fan-out probes each target the other two authenticated clients. `LiveServerState::drain_client_fanout()` therefore made exactly six successful bounded DRR admissions, and the shared helper mislabeled those ordinary scheduler admissions as observed QUIC transport pressure.
- Scheduled TUN and client-fan-out admission now increments `quicfuscate_bandwidth_scheduler_enqueued_total`; only the direct `DgramQueueFull` path increments successful transport-backpressure admission. Queue bounds, DRR order, per-session accounting, retry/drop behavior, and the strict native quiescence gate are unchanged. A focused regression proves one scheduled admission exports the scheduler count while transport-backpressure enqueue remains zero.
- Focused regressions pass `2/2`; the complete server module passes `536/536`; exact default and `rust-tests` workspace all-target Clippy pass with warnings denied. Formatting, diff hygiene, documentation truth, runtime guardrails with zero findings, and protected frontend/Tauri isolation pass. Final target/free space is `3,931,684 / 4,351,980 KiB`, below the cleanup threshold and above the build floor. Hosted Linux owns the authoritative native rerun; no frontend field or API projection is required.

## Base native client-assignment harness continuation (2026-08-11)

- CI run `31448249573`, native traffic job `93646994311`, passed the corrected server PTB and both throughput phases, including the strict backpressure quiescence point, then failed when the separate traffic-analysis proof invoked the base TUN harness with standalone client address and netmask flags.
- The base harness now supplies only `--tun` and `--tun-name` to the standalone client. It no longer repairs server or client TUN state with post-start `ip addr add` and `ip link set`; instead it requires exact runtime-owned `10.0.1.1/24` server provisioning and authenticated `10.0.1.2/24` client assignment before TLS/H3/MASQUE traffic proof.
- Bash syntax, ShellCheck warning level, the exact server-only address-flag and no-client-address-flag contract, documentation truth, runtime guardrails with zero findings, diff hygiene, and protected frontend/Tauri isolation pass. The macOS host cannot execute the root-only namespace lifecycle; hosted Linux owns the authoritative base, traffic-analysis, and fingerprint rerun. No frontend field or API projection is required.

## qf-audit watchdog regression synchronization continuation (2026-08-11)

- CI run `31448249573`, macOS feature-matrix job `93646994355`, exposed a scheduling race in `durability_watchdog_marks_a_stalled_operation_terminal`: the test assumed that a newly spawned watchdog thread would publish its terminal state within a fixed 80 ms sleep.
- The regression now waits up to two seconds for the exact typed `AuditFailure::DurabilityTimeout` publication, sleeping one millisecond between observations. Production watchdog timing, cancellation, counters, and persistence-failure behavior are unchanged; the assertion still fails if the watchdog never publishes the required terminal state.
- The focused regression passes 50 consecutive reruns plus the initial focused run; the complete qf-audit suite passes `42/42`. Exact default and `rust-tests` workspace all-target Clippy pass with warnings denied. Frontend and Tauri paths remain untouched; no frontend field or API projection is required.

## Specialized FEC client-assignment harness continuation (2026-08-11)

- The uniform-loss, burst-loss, transition, and adversity TUN/FEC harnesses still supplied standalone client IPv4 and netmask overrides even though the authenticated server assignment is the sole accepted client configuration source.
- All four clients now start with only TUN enablement and interface-name flags. The harnesses no longer repair server or client TUN state after startup; each requires exact runtime-owned `10.0.1.1/24` server provisioning and authenticated `10.0.1.2/24` client assignment before handshake and loss proof.
- Bash syntax, ShellCheck warning level, exact server-only address-flag counts, absence of harness-side TUN repair, the existing lifecycle ownership regression's non-Linux skip, diff hygiene, documentation truth, runtime guardrails, and protected frontend/Tauri isolation pass. Native loss execution remains Linux-only and is not inferred from this macOS host; no frontend field or API projection is required.

## Bandwidth and CUBIC client-assignment harness continuation (2026-08-11)

- The authenticated three-client bandwidth proof and the CUBIC control still supplied standalone client address, prefix, and MTU flags and then forced TUN links up after startup. Those arguments are rejected by the current standalone client contract and the link mutation concealed incomplete runtime provisioning.
- Both harnesses now reserve address, prefix, and MTU flags for the server. Each client consumes its authenticated assignment, and the harness verifies the exact assigned IPv4, optional IPv6, prefix, MTU 1280, and UP state before starting data-plane measurements. No harness-side TUN link repair remains.
- Bash syntax, ShellCheck warning level, exact server-only flag counts, absence of client overrides and TUN link repair, documentation truth, runtime guardrails with zero findings, diff hygiene, and protected frontend/Tauri isolation pass. Native bandwidth and CUBIC execution remains Linux-only and is not inferred locally; no frontend field or API projection is required.

## Kill-switch and DNS client-assignment harness continuation (2026-08-11)

- The Linux kill-switch and DNS-leak proofs still supplied standalone client IPv4/netmask flags and repaired both TUN interfaces after startup. The current client rejects those flags because the authenticated server assignment owns its TUN configuration.
- Both clients now start without address overrides. Each harness requires the exact server and client `/24` assignments plus runtime UP state before firewall, tunnel, or DNS evidence; the former address replacement and link-up mutation is removed.
- Bash syntax, ShellCheck warning level, exact server-only address-flag counts, absence of harness-side TUN repair, documentation truth, runtime guardrails with zero findings, diff hygiene, and protected frontend/Tauri isolation pass. The separate DNS harness process/certificate ownership cleanup remains a distinct follow-up; native execution remains Linux-only and is not inferred locally. No frontend field or API projection is required.

## Windows Wintun client-assignment harness continuation (2026-08-11)

- The Windows-to-Omega Wintun proof still passed IPv4/netmask and IPv6/prefix overrides to the standalone client. Those flags are rejected on every platform because the authenticated server assignment owns client TUN configuration.
- The client now supplies only TUN enablement and the adapter name. Existing `Wait-ForTunnelAdapterReady` validation remains authoritative for the exact assigned IPv4/IPv6 addresses, adapter UP state, and both MTU observations; the expected address parameters remain evidence inputs rather than client configuration.
- Static argument inspection proves no prohibited TUN address/MTU flag remains and preserves QKey redaction at argument index 6. Documentation truth, runtime guardrails with zero findings, diff hygiene, and protected frontend/Tauri isolation pass. PowerShell and native Wintun execution are unavailable on this macOS host and remain external; no frontend field or API projection is required.

## DNS-leak harness ownership continuation (2026-08-11)

- The Linux DNS-leak proof used broad `pkill -f`, deleted fixed namespaces without proving ownership, overwrote server certificate/key fixtures under `config/local`, and reused fixed `/tmp` evidence names. A failed or concurrent run could therefore mutate unrelated project or process state.
- The harness now refuses pre-existing namespace/veth collisions, tracks each created namespace/link and exact server/client/tcpdump PID, uses bounded TERM/KILL cleanup, generates its leaf certificate only inside a private `mktemp` runtime, treats the configured CA pair as read-only input, and retains binary identity, logs, DNS outputs, and pcap data in one non-overwriting artifact directory. Temporary certificate, admin-socket, and resolver-mount state is removed only after exact path validation.
- Bash syntax, ShellCheck warning level, static no-broad-process-cleanup and no-repository-certificate-write contracts, exact server-only TUN address flags, documentation truth, runtime guardrails with zero findings, diff hygiene, and protected frontend/Tauri isolation pass. Native network-namespace and resolver-mount execution remains Linux-only and is not inferred locally; no frontend field or API projection is required.
