# Build artifacts
target/
**/target/
out/
**/out/

# IDE / editor
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# Git
.git/
.gitignore
.gitattributes

# CI / docs (not needed in the image)
.github/
docs/
*.md
!AGENTS.md

# Local secrets and runtime state (never bake into images)
config/local/
config/admin-auth.json
config/*.logging.json
config/*.qkeys.json
config/*.runtime.json
*.pem
*.key
*.crt
*.p12
*.pfx

# Node / frontend build artifacts
**/node_modules/
**/dist/
**/build/
**/.vite/
bun.lock
package.json

# Docker
Dockerfile
docker-compose.yml
.dockerignore

# Tauri / apps native build (excluded from workspace)
apps/tauri/src-tauri/target/
