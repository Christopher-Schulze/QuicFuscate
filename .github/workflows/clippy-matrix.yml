name: Clippy Matrix

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  clippy-matrix:
    name: Clippy Matrix Check
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        features:
          - ""
          - "unsafe_rust"
          - "unsafe_rust,compression_zstd_ffi"
          - "simd-selfcheck"
          - "internal_wiedemann"
          - "internal_wiedemann,unsafe_rust"
          - "internal_wiedemann,unsafe_rust,compression_zstd_ffi"
          - "io_uring"

    steps:
    - uses: actions/checkout@v7
      
    - name: Install Rust
      uses: dtolnay/rust-toolchain@stable
      with:
        components: clippy
        
    - name: Cache dependencies
      uses: actions/cache@v6
      with:
        path: |
          ~/.cargo/registry
          ~/.cargo/git
          target
        key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
        restore-keys: |
          ${{ runner.os }}-cargo-
          
    - name: "Run clippy check (features: ${{ matrix.features }})"
      run: |
        if [ -n "${{ matrix.features }}" ]; then
          cargo clippy --all-targets --features "${{ matrix.features }}" -- -D warnings
        else
          cargo clippy --all-targets -- -D warnings
        fi
